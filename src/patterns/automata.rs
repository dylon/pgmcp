//! Formal-languages and automata patterns: finite/pushdown/tree/ω-automata,
//! transducers, parser and lexer families, string-index automata (trie, DAWG,
//! suffix automaton, double-array, HAMT), edit-distance algorithms and
//! approximate-matching automata, phonetic encoders, abstract machines, term
//! rewriting, and probabilistic grammars.

use super::{PatternSeed, pat};

pub(super) fn seeds() -> Vec<PatternSeed> {
    vec![
        // ---------- finite_automaton ----------
        pat(
            "deterministic_finite_automaton",
            "Deterministic Finite Automaton (DFA)",
            "pattern",
            "finite_automaton",
            "Recognize a regular language via a 5-tuple (Q, Σ, δ, q0, F) with a single next state per (state, symbol).",
            "Express a regular-language recognizer that runs in O(n) per input with no backtracking.",
            "Ad-hoc string scanners blur tokenization rules and resist composition, minimization, and equivalence checking.",
            "Encode states and a total transition table δ: Q×Σ→Q; advance one symbol at a time; accept when the final state is in F.",
            "Linear-time recognition and standard tooling; state explosion is possible when alphabets or modes proliferate.",
            &["formal_languages_and_automata"],
            &["automaton", "dfa", "regular_language"],
            "https://en.wikipedia.org/wiki/Deterministic_finite_automaton",
        ),
        pat(
            "nondeterministic_finite_automaton",
            "Nondeterministic Finite Automaton (NFA)",
            "pattern",
            "finite_automaton",
            "FA where δ: Q×(Σ∪{ε})→2^Q permits multiple successor states and ε-transitions.",
            "Capture regular languages compactly during construction; defer determinization or simulate the NFA directly.",
            "Direct DFA construction from regexes/grammars produces large or unwieldy state sets up front.",
            "Build an NFA from the structural form of the specification; recognize by tracking the set of reachable states or by determinization.",
            "Concise representation aligned with regular expressions; recognition cost is O(|Q|·n) per input unless determinized.",
            &["formal_languages_and_automata"],
            &["automaton", "nfa", "regular_language"],
            "https://en.wikipedia.org/wiki/Nondeterministic_finite_automaton",
        ),
        pat(
            "thompson_nfa_construction",
            "Thompson NFA Construction",
            "pattern",
            "finite_automaton",
            "Inductively build an ε-NFA from a regular expression with constant-sized fragments per operator.",
            "Compile a regex into an automaton whose size is linear in the regex length.",
            "Ad-hoc regex compilers conflate parsing and construction, making maintenance and correctness analysis hard.",
            "Translate each AST node (ε, char class, concat, union, Kleene-star) into a small NFA fragment with explicit start/accept states wired together with ε-edges.",
            "Linear-size NFA aligned 1:1 with the AST; an ε-elimination or subset-construction pass is still needed to recover a runnable DFA.",
            &["formal_languages_and_automata"],
            &["nfa", "regex", "compilation"],
            "https://en.wikipedia.org/wiki/Thompson%27s_construction",
        ),
        pat(
            "subset_construction",
            "Subset / Powerset Construction",
            "pattern",
            "finite_automaton",
            "Determinize an NFA by treating each reachable subset of NFA states as a single DFA state.",
            "Convert any NFA (including ε-NFA) into an equivalent DFA suitable for linear-time matching.",
            "NFA simulation costs O(|Q|·n); ε-closure work duplicates across runs.",
            "Compute ε-closures, materialize each reached subset as a DFA state, and tabulate transitions until closure; mark a subset accepting if it contains any NFA accepting state.",
            "Yields a deterministic recognizer at the risk of exponential state-set growth; typically followed by minimization.",
            &["formal_languages_and_automata"],
            &["determinization", "nfa", "dfa"],
            "https://en.wikipedia.org/wiki/Powerset_construction",
        ),
        pat(
            "hopcroft_dfa_minimization",
            "Hopcroft DFA Minimization",
            "pattern",
            "finite_automaton",
            "Partition-refinement algorithm that minimizes a DFA in O(n log n) where n = |Q|·|Σ|.",
            "Compute the unique minimal DFA equivalent to a given DFA to shrink tables and enable equivalence checks.",
            "Naive equivalence-class merging is O(n²) and fragile under streaming refinement.",
            "Start with a partition of accepting vs non-accepting states; repeatedly split blocks by preimages under δ⁻¹(symbol) using a worklist that processes the smaller side of each split.",
            "Minimal DFA with proven log-factor cost; implementation requires careful worklist accounting to keep the bound.",
            &["formal_languages_and_automata"],
            &["minimization", "dfa", "partition_refinement"],
            "https://en.wikipedia.org/wiki/DFA_minimization#Hopcroft.27s_algorithm",
        ),
        pat(
            "brzozowski_dfa_minimization",
            "Brzozowski DFA Minimization",
            "pattern",
            "finite_automaton",
            "Reverse, determinize, reverse, determinize — yields the minimal DFA via two determinization passes.",
            "Obtain a minimal DFA with a short correctness proof, especially convenient when an NFA is already at hand.",
            "Hopcroft minimization has the better worst-case bound but is intricate to implement and audit.",
            "Apply reverse(); subset_construction(); reverse(); subset_construction(); the result is the minimal DFA.",
            "Elegant and easy to verify; potentially exponential intermediate DFAs make worst-case cost worse than Hopcroft.",
            &["formal_languages_and_automata"],
            &["minimization", "dfa", "brzozowski"],
            "https://en.wikipedia.org/wiki/DFA_minimization#Brzozowski.27s_algorithm",
        ),
        pat(
            "alphabet_equivalence_partition",
            "Alphabet Equivalence Partitioning",
            "pattern",
            "finite_automaton",
            "Group input symbols whose DFA behavior is identical into a single column to compress transition tables.",
            "Reduce 256-column byte tables to ~10–20 columns while preserving recognition behavior.",
            "Dense byte tables waste cache and memory when the DFA only distinguishes a handful of character classes.",
            "Compute an equivalence relation on Σ from the column patterns of δ; assign each class a small index; emit a (state, class) → state table plus a symbol-to-class map.",
            "Dramatic table-size and cache-footprint wins; the extra indirection costs one byte lookup per transition.",
            &["formal_languages_and_automata"],
            &["dfa", "compaction", "lexer"],
            "https://en.wikipedia.org/wiki/Equivalence_class",
        ),
        pat(
            "nfa_epsilon_elimination",
            "NFA ε-Elimination and Dead-State Pruning",
            "pattern",
            "finite_automaton",
            "Remove ε-transitions, then prune unreachable and dead states from an automaton.",
            "Get a smaller, ε-free automaton suitable for tight simulation or as an input to determinization.",
            "ε-transitions inflate runtime work and break invariants assumed by some algorithms.",
            "Compute ε-closure(q) for each state, retarget δ accordingly, then delete states unreachable from q0 or from which no accepting state is reachable.",
            "Cleaner automaton with no semantic loss; closure computation can be a quadratic preprocessing step.",
            &["formal_languages_and_automata"],
            &["nfa", "cleanup", "epsilon"],
            "https://en.wikipedia.org/wiki/Nondeterministic_finite_automaton#Removing_epsilon-transitions",
        ),
        // ---------- pushdown ----------
        pat(
            "pushdown_automaton",
            "Pushdown Automaton (PDA)",
            "pattern",
            "pushdown",
            "Finite automaton augmented with a stack; recognizes exactly the context-free languages.",
            "Recognize nested or balanced structure (parentheses, blocks, function calls) that DFAs cannot.",
            "Regular automata cannot count or match brackets without unbounded state.",
            "Define δ: Q × (Σ∪{ε}) × Γ → 2^(Q × Γ*) over input symbol and stack-top; accept by final state or empty stack.",
            "Captures CFLs at the cost of nondeterminism in general; deterministic PDAs (DPDAs) are weaker but parse fast.",
            &["formal_languages_and_automata"],
            &["pda", "context_free", "stack"],
            "https://en.wikipedia.org/wiki/Pushdown_automaton",
        ),
        pat(
            "visibly_pushdown_automaton",
            "Visibly Pushdown Automaton (VPA)",
            "pattern",
            "pushdown",
            "PDA over an alphabet partitioned into call/return/internal symbols; stack moves are determined by the symbol class.",
            "Recognize and verify nested-word languages with closure under union, intersection, and complement.",
            "General PDAs are not closed under intersection or complement, blocking compositional reasoning over CFL fragments.",
            "Constrain the stack discipline: call symbols push, return symbols pop, internal symbols leave the stack alone; this yields a robust class of nested-word languages.",
            "Powerful for XML-shaped languages, schema validation, and interprocedural verification; harder to fit when the alphabet is not naturally typed.",
            &["formal_languages_and_automata"],
            &["vpa", "nested_words", "verification"],
            "https://en.wikipedia.org/wiki/Visibly_pushdown_language",
        ),
        pat(
            "weighted_pushdown_system",
            "Weighted Pushdown System (WPDS)",
            "pattern",
            "pushdown",
            "Pushdown system whose transitions carry semiring weights, modeling interprocedural dataflow as a fixpoint.",
            "Compute precise interprocedural meet-over-all-paths solutions for analyses expressible over a semiring.",
            "Forward dataflow does not naturally handle context-sensitive call/return matching.",
            "Encode call/return as PDS rules with weights drawn from a semiring; solve via saturation (pre*, post*) over weighted regular languages.",
            "Handles unbounded call stacks with precise context sensitivity; specialized engines (e.g. WALi) are usually required for performance.",
            &["formal_languages_and_automata"],
            &["wpds", "interprocedural", "semiring"],
            "https://en.wikipedia.org/wiki/Pushdown_automaton",
        ),
        // ---------- tree_automaton ----------
        pat(
            "tree_automaton",
            "Tree Automaton (top-down and bottom-up)",
            "pattern",
            "tree_automaton",
            "Finite-state recognizer that labels nodes of a ranked tree as it traverses the structure.",
            "Recognize tree languages such as XML schemas, type-system shapes, and AST validity conditions.",
            "Word automata cannot describe constraints over hierarchical structure.",
            "Define transitions per node-label and per child arity: bottom-up automata assign states to children first, top-down threads a state from the root downward.",
            "Bottom-up automata are determinizable and closed under boolean operations; top-down automata are more natural for traversals but generally nondeterministic.",
            &["formal_languages_and_automata"],
            &["tree", "xml", "schema"],
            "https://en.wikipedia.org/wiki/Tree_automaton",
        ),
        pat(
            "alternating_automaton",
            "Alternating Finite Automaton",
            "pattern",
            "tree_automaton",
            "Finite automaton whose transition function returns a boolean formula over successor states (∧ and ∨).",
            "Capture languages exponentially more concisely than NFAs while staying in the same expressive class.",
            "NFAs blow up under intersection and complement; AFAs absorb those operations natively.",
            "Allow δ to return existential and universal moves; a run is an alternation tree all of whose accepting leaves accept.",
            "Compact representation with native intersection and complementation; conversion to DFA may be doubly exponential.",
            &["formal_languages_and_automata"],
            &["alternating", "afa", "model_checking"],
            "https://en.wikipedia.org/wiki/Alternating_finite_automaton",
        ),
        // ---------- temporal ----------
        pat(
            "buchi_omega_automaton",
            "Büchi Automaton (ω-automaton)",
            "pattern",
            "temporal",
            "Finite automaton over infinite words; accepts iff some accepting state is visited infinitely often.",
            "Decide LTL satisfiability and model-check liveness properties of reactive systems.",
            "Finite-word automata cannot express \"always eventually\" or \"infinitely often\" properties.",
            "Translate LTL formulas to Büchi automata, then take the product with the system and check emptiness of the resulting Büchi language.",
            "Foundation of automata-theoretic model checking; complementation and determinization (Safra) are notoriously intricate.",
            &["formal_languages_and_automata"],
            &["buchi", "omega", "ltl", "model_checking"],
            "https://en.wikipedia.org/wiki/B%C3%BCchi_automaton",
        ),
        // ---------- transducer ----------
        pat(
            "finite_state_transducer",
            "Finite-State Transducer (FST)",
            "pattern",
            "transducer",
            "Finite automaton whose transitions emit output symbols, computing a relation Σ* × Δ*.",
            "Translate strings between alphabets in a compositional, debuggable way (lexers, normalizers, morphological analyzers).",
            "Hand-written string transducers entangle parsing and emission and resist reuse.",
            "Annotate each transition with (input symbol, output symbol); compose, intersect with FAs, and invert as needed.",
            "Composable string-to-string translations with linear runtime; some operations require ε-removal and care with non-functional relations.",
            &["formal_languages_and_automata"],
            &["fst", "transducer", "translation"],
            "https://en.wikipedia.org/wiki/Finite-state_transducer",
        ),
        pat(
            "weighted_finite_state_transducer",
            "Weighted Finite-State Transducer (WFST)",
            "pattern",
            "transducer",
            "FST whose transitions carry weights from a semiring; computes weighted relations.",
            "Encode probabilistic, edit-distance, or shortest-path models compositionally.",
            "Composing probabilistic lexers/parsers ad hoc loses associativity and complicates beam search.",
            "Use semirings (tropical for shortest path, log for negative log-prob, real for probability) on transitions; combine WFSTs by composition and project.",
            "Backbone of ASR, MT, and approximate-matching pipelines; numerical stability and weight-pushing require attention.",
            &["formal_languages_and_automata"],
            &["wfst", "semiring", "asr"],
            "https://en.wikipedia.org/wiki/Finite-state_transducer#Weighted_automata",
        ),
        pat(
            "wfst_composition",
            "WFST Composition",
            "pattern",
            "transducer",
            "Cascade two WFSTs T1 ∘ T2 by pairing states whose transitions share input/output symbols.",
            "Build pipeline models by composing reusable building blocks (lexicon, language model, edit model).",
            "Re-implementing a full pipeline as one monolithic FST is unmaintainable.",
            "Take the product over pairs of transitions whose output (T1) matches input (T2); insert ε-filters to handle ε-transitions consistently.",
            "Modular construction with clear semantics; intermediate transducers can grow large without weight pushing and pruning.",
            &["formal_languages_and_automata"],
            &["wfst", "composition", "pipeline"],
            "https://en.wikipedia.org/wiki/Finite-state_transducer",
        ),
        pat(
            "two_way_transducer",
            "Two-Way / Multi-Tape Transducer",
            "pattern",
            "transducer",
            "Transducer whose read head can move both directions, or that reads several tapes in parallel.",
            "Express transformations that require lookahead, lookbehind, or coordinated multi-input processing.",
            "Standard one-way FSTs cannot express many natural transducers (reversal, copy, alignment) compactly.",
            "Allow head moves ±1 (two-way) or use multiple coordinated tape heads with synchronization conventions.",
            "Greater expressivity and compactness; equivalence and minimization become harder, and some classes are equivalent to one-way FSTs only up to translation.",
            &["formal_languages_and_automata"],
            &["transducer", "two_way", "multi_tape"],
            "https://en.wikipedia.org/wiki/Finite-state_transducer",
        ),
        // ---------- parser ----------
        pat(
            "lr_parser",
            "LR Parser",
            "pattern",
            "parser",
            "Bottom-up shift-reduce parser driven by action/goto tables compiled from a CFG.",
            "Parse a large class of unambiguous CFGs in linear time with reproducible tables.",
            "Hand-rolled bottom-up parsers are error-prone and hard to update when the grammar changes.",
            "Compute LR(k) item sets, derive action and goto tables, drive a stack-based shift-reduce loop guided by the lookahead.",
            "Linear time and well-understood error reporting; table sizes can balloon and conflict resolution is non-local.",
            &["formal_languages_and_automata"],
            &["parser", "lr", "shift_reduce"],
            "https://en.wikipedia.org/wiki/LR_parser",
        ),
        pat(
            "lalr_parser",
            "LALR(1) Parser",
            "pattern",
            "parser",
            "LR(1) variant that merges states with identical cores, trading some lookahead precision for smaller tables.",
            "Get most of LR(1)'s power with table sizes close to SLR(1).",
            "Full LR(1) tables are unwieldy for production languages; SLR(1) rejects too many practical grammars.",
            "Build the LR(1) collection, merge mergeable states, recompute lookahead sets; emit the merged action/goto tables.",
            "Workhorse of yacc/bison/lalrpop with tractable table sizes; some LR(1) grammars become non-LALR(1) after merging.",
            &["formal_languages_and_automata"],
            &["parser", "lalr", "yacc"],
            "https://en.wikipedia.org/wiki/LALR_parser",
        ),
        pat(
            "glr_parser",
            "Generalized LR (GLR) Parser",
            "pattern",
            "parser",
            "LR parser that forks at conflicts and shares stacks via a graph-structured stack; accepts any CFG.",
            "Parse ambiguous, context-sensitive, or evolving grammars (e.g., C++) where LR(1)/LALR(1) cannot.",
            "Real languages have grammar conflicts that neither LR nor LALR can resolve.",
            "Maintain a worklist of parser stacks merged via a GSS; advance all viable parses in lockstep and reconcile at reductions.",
            "Handles ambiguity natively and degrades gracefully to linear time on LR(1) inputs; ambiguous parses must be ranked or disambiguated downstream.",
            &["formal_languages_and_automata"],
            &["parser", "glr", "ambiguity"],
            "https://en.wikipedia.org/wiki/GLR_parser",
        ),
        pat(
            "earley_parser",
            "Earley Chart Parser",
            "pattern",
            "parser",
            "Top-down chart parser that handles any CFG in O(n³) general / O(n²) unambiguous / O(n) deterministic time.",
            "Recognize ambiguous or arbitrary CFGs with a single, uniform algorithm.",
            "LR/LALR parsers reject many practical grammars; rolling per-grammar parsers is costly.",
            "Maintain an Earley set per input position; apply predict, scan, complete operations until closure; reconstruct parses from completed items.",
            "Robust and uniform; constants are higher than LR and memory consumption is per-position chart storage.",
            &["formal_languages_and_automata"],
            &["parser", "earley", "chart"],
            "https://en.wikipedia.org/wiki/Earley_parser",
        ),
        pat(
            "pratt_operator_precedence_parser",
            "Pratt Operator-Precedence Parser",
            "pattern",
            "parser",
            "Top-down parser using per-token left/right binding powers (nud, led) to drive precedence climbing.",
            "Parse expressions with rich operator precedence, mixfix syntax, and easy extensibility.",
            "Recursive-descent expression parsers explode in size and are hard to extend with new operators.",
            "Bind each token to nud (null denotation) and led (left denotation) functions plus numeric binding powers; loop while next token's left binding power exceeds the caller's right binding power.",
            "Compact, extensible expression parsers; integrates well with hand-written top-down parsers but is less mechanically derived than LR.",
            &["formal_languages_and_automata"],
            &["parser", "pratt", "precedence"],
            "https://en.wikipedia.org/wiki/Operator-precedence_parser",
        ),
        pat(
            "recursive_descent_parser",
            "Recursive-Descent Parser",
            "pattern",
            "parser",
            "Top-down predictive parser with one mutually recursive procedure per non-terminal.",
            "Get readable, debuggable parsers tightly coupled to the grammar.",
            "Generated tables can be hard to debug or extend with custom error recovery.",
            "Write one parsing function per non-terminal; peek at the next token to choose alternatives; consume tokens with explicit expect/accept helpers.",
            "Easy to write, debug, and extend with semantic predicates; left recursion needs grammar rewriting and inefficient backtracking is a hazard.",
            &["formal_languages_and_automata"],
            &["parser", "recursive_descent", "top_down"],
            "https://en.wikipedia.org/wiki/Recursive_descent_parser",
        ),
        pat(
            "packrat_parser",
            "Packrat Parser",
            "pattern",
            "parser",
            "Memoized recursive-descent parser realizing PEG semantics in linear time and space.",
            "Get PEG's deterministic, composable matching with guaranteed linear performance.",
            "Naive PEG implementations suffer exponential blowup on left-factoring choices.",
            "Memoize every (rule, position) pair on first evaluation and reuse on revisit; treat ordered choice as commit-on-first-success.",
            "Linear time at the cost of O(n·|G|) memory; deterministic semantics differ from CFGs in subtle ways.",
            &["formal_languages_and_automata", "functional_programming"],
            &["parser", "peg", "packrat", "memoization"],
            "https://en.wikipedia.org/wiki/Parsing_expression_grammar#Implementing_parsers_from_parsing_expression_grammars",
        ),
        pat(
            "graph_structured_stack",
            "Graph-Structured Stack (GSS)",
            "pattern",
            "parser",
            "Shared-prefix stack representation that lets GLR/Earley parsers merge equivalent partial parses.",
            "Avoid exponential blowup when multiple LR parser stacks share suffixes.",
            "Maintaining one stack per fork is exponential in the worst case for ambiguous grammars.",
            "Represent stacks as a DAG where edges carry stack symbols; merge nodes whose state and parents coincide; iterate shift/reduce against the DAG.",
            "Brings worst-case ambiguous parsing down to cubic; bookkeeping for parent edges and reductions over the DAG is intricate.",
            &["formal_languages_and_automata"],
            &["gss", "glr", "parser"],
            "https://en.wikipedia.org/wiki/Graph-structured_stack",
        ),
        pat(
            "incremental_glr_treesitter",
            "Incremental GLR Parsing (tree-sitter-style)",
            "pattern",
            "parser",
            "Reparse only the subtrees affected by an edit using a cached previous parse tree and span deltas.",
            "Support live editing of source code with sub-millisecond reparses on large files.",
            "Re-parsing the whole file on every keystroke is too slow for editor-grade tooling.",
            "Compute the dirty input range from edit deltas, descend into the cached parse tree, splice in the freshly parsed nodes, and rebalance summaries.",
            "Massive latency wins for editors and LSPs; the incremental algorithm and tree representation are complex to implement correctly.",
            &["formal_languages_and_automata"],
            &["parser", "incremental", "tree_sitter", "editor"],
            "https://tree-sitter.github.io/tree-sitter/",
        ),
        pat(
            "panic_mode_error_recovery",
            "Panic-Mode / Synchronization Error Recovery",
            "pattern",
            "parser",
            "On a syntax error, skip tokens until a sentinel set is reached, then resume parsing.",
            "Continue parsing after a syntax error to report cascading problems and offer better diagnostics.",
            "Stopping at the first parse error wastes information and frustrates users.",
            "Define synchronization tokens (e.g. `;`, `}`, statement starters); on error, skip until one is found and resume with the appropriate non-terminal.",
            "Simple to implement and effective for diagnostics; may produce spurious follow-on errors and need tuning per language.",
            &["formal_languages_and_automata"],
            &["parser", "error_recovery", "diagnostics"],
            "https://en.wikipedia.org/wiki/Recursive_descent_parser#Error_recovery",
        ),
        // ---------- lexer ----------
        pat(
            "maximal_munch_lexer",
            "Maximal-Munch Lexer",
            "pattern",
            "lexer",
            "Greedy longest-match scanner with a `last_accept` snapshot for backtracking on mismatch.",
            "Tokenize input deterministically with the canonical \"longest match wins\" rule.",
            "Eager-emit lexers misclassify keywords vs identifiers and operators of differing lengths.",
            "Run the DFA forward, recording the most recent accepting position; on stuck transition, rewind to last_accept and emit the corresponding token kind.",
            "Standard lexer semantics with predictable behavior; ambiguities between equal-length tokens still require tie-breaking rules.",
            &["formal_languages_and_automata"],
            &["lexer", "maximal_munch", "tokenization"],
            "https://en.wikipedia.org/wiki/Maximal_munch",
        ),
        pat(
            "dfa_codegen_lexer",
            "Table-Driven vs Direct-Coded DFA Lexer",
            "pattern",
            "lexer",
            "Choose between a dense δ table indexed by (state, symbol) and emitted switch/goto-style code per state.",
            "Pick the codegen strategy that matches the host language's branch-prediction and cache behavior.",
            "One-size-fits-all lexer codegen leaves performance on the table.",
            "Profile both: table-driven lexers minimize code size and let JITs prefetch; direct-coded lexers let the host compiler specialize comparisons and inline character-class checks.",
            "Direct-coded lexers often beat tables on modern CPUs; tables are more cache-friendly for very large alphabets and easier to mutate at runtime.",
            &["formal_languages_and_automata"],
            &["lexer", "codegen", "dfa"],
            "https://en.wikipedia.org/wiki/Lexical_analysis",
        ),
        // ---------- string_index ----------
        pat(
            "trie_prefix_tree",
            "Trie / Digital Tree",
            "pattern",
            "string_index",
            "Tree whose edges carry single symbols of the key, supporting O(|key|) lookup, insert, and prefix search.",
            "Store a set or map keyed by strings with shared-prefix compression and prefix-range queries.",
            "Hashing loses prefix structure; sorted arrays cost log n per probe and can't do prefix range.",
            "Build child links keyed by the next symbol; mark terminal nodes; walk one symbol at a time to find/insert/delete.",
            "Cheap prefix queries and shared structure; per-node overhead dominates for sparse keys, motivating compressed variants.",
            &["formal_languages_and_automata"],
            &["trie", "prefix", "dictionary"],
            "https://en.wikipedia.org/wiki/Trie",
        ),
        pat(
            "radix_tree",
            "Radix / Compressed Trie",
            "pattern",
            "string_index",
            "Trie with single-child chains collapsed into edges labeled by substrings.",
            "Cut internal-node count when keys share long common prefixes.",
            "A vanilla trie wastes memory on single-child chains.",
            "Store substring labels on edges; split an edge when an insert diverges; merge back to a child-only chain on delete.",
            "Compact memory layout with the same O(|key|) lookup; split/merge bookkeeping adds code complexity.",
            &["formal_languages_and_automata"],
            &["trie", "radix", "compression"],
            "https://en.wikipedia.org/wiki/Radix_tree",
        ),
        pat(
            "patricia_trie",
            "PATRICIA Trie",
            "pattern",
            "string_index",
            "Bitwise radix tree that stores only the bit positions where keys branch.",
            "Index variable-length bit strings (IPv4/IPv6 prefixes, IDs) with minimum node count.",
            "Byte-oriented tries are wasteful for sparse bit-keyed data.",
            "Each internal node records a bit index to test; leaves hold the full key; traversal compares the indicated bit and descends accordingly.",
            "Very compact bit-indexed structure; care must be taken with edge cases (single-key tries, key-length boundaries).",
            &["formal_languages_and_automata"],
            &["trie", "patricia", "bitwise"],
            "https://en.wikipedia.org/wiki/Radix_tree#PATRICIA",
        ),
        pat(
            "adaptive_radix_tree",
            "Adaptive Radix Tree (ART)",
            "pattern",
            "string_index",
            "Radix tree whose node fan-out adapts (4/16/48/256) to the density of children at each level.",
            "Get hash-table-like point-lookup speed with trie-like prefix and range queries.",
            "Fixed-fan-out radix trees waste memory at sparse levels or pay pointer-chasing at dense ones.",
            "Pick a node layout per child count (linear array, sorted array with SIMD search, sparse index, dense 256-array); upgrade/downgrade on insert/delete.",
            "Excellent cache behavior and key-density adaptivity; multiple node variants increase implementation surface.",
            &["formal_languages_and_automata"],
            &["trie", "art", "in_memory_db"],
            "https://db.in.tum.de/~leis/papers/ART.pdf",
        ),
        pat(
            "hash_array_mapped_trie",
            "Hash Array-Mapped Trie (HAMT)",
            "pattern",
            "string_index",
            "Persistent trie indexed by hash chunks; each internal node stores a bitmap of present children and a compact child array.",
            "Implement persistent immutable maps with near-O(log32 n) operations and structural sharing across versions.",
            "Persistent maps with naive copy-on-write are too expensive for large collections.",
            "Slice the hash into 5- or 6-bit chunks; at each level use a bitmap + popcount to index into a dense child slice; update by path-copying the touched spine.",
            "Foundation of Clojure/Scala persistent maps; bitmap node manipulation is intricate and small maps may prefer flat structures.",
            &["formal_languages_and_automata", "functional_programming"],
            &["trie", "hamt", "persistent", "immutable"],
            "https://en.wikipedia.org/wiki/Hash_array_mapped_trie",
        ),
        pat(
            "bitwise_trie_with_bitmap",
            "Bitwise Trie with Bitmap",
            "pattern",
            "string_index",
            "Trie whose children at each level are addressed by bits of the key and packed via a popcount bitmap.",
            "Get HAMT-style compact addressing for arbitrary key bytes (paths, symbol sequences, atom strings).",
            "Byte-keyed tries with sparse fan-out waste pointer slots at every node.",
            "At each level take a fixed bit-slice of the next key chunk; use a bitmap to mark present children and popcount to index a dense array; specialize node layouts (dense byte / tiny / line list / bridge) by density.",
            "Cache-friendly, polymorphic node layouts; balancing layout transitions vs cost of polymorphic dispatch requires careful design.",
            &["formal_languages_and_automata"],
            &["trie", "bitmap", "pathmap"],
            "https://en.wikipedia.org/wiki/Bitwise_trie_with_bitmap",
        ),
        pat(
            "double_array_trie",
            "Double-Array Trie",
            "pattern",
            "string_index",
            "Static trie packed into two parallel integer arrays BASE and CHECK supporting O(1) transitions.",
            "Get the fastest possible read-only trie lookups with minimal memory overhead.",
            "Pointer-based tries pay dereference and cache-miss costs that hurt read-heavy workloads.",
            "For each transition (s, c): next = BASE[s] + c, valid iff CHECK[next] == s; build via offset search packing children contiguously.",
            "Excellent read performance for static dictionaries; updates are expensive enough that the structure is typically rebuilt offline.",
            &["formal_languages_and_automata"],
            &["trie", "double_array", "aoe"],
            "https://en.wikipedia.org/wiki/Double-array_trie",
        ),
        pat(
            "directed_acyclic_word_graph",
            "Directed Acyclic Word Graph (DAWG)",
            "pattern",
            "string_index",
            "Minimal DFA recognizing a finite set of words with suffix sharing across the dictionary.",
            "Store a large word set with both prefix and suffix compression for fast membership and traversal.",
            "Tries lose the chance to share suffixes; full DFAs of word sets can be huge without minimization.",
            "Build a trie, then minimize bottom-up via Mihov's incremental algorithm (or offline Hopcroft) merging suffix-equivalent nodes.",
            "Dramatic compression for languages with rich morphology; updates require either rebuilding or specialized dynamic-DAWG algorithms.",
            &["formal_languages_and_automata"],
            &["dawg", "minimal_dfa", "dictionary"],
            "https://en.wikipedia.org/wiki/Deterministic_acyclic_finite_state_automaton",
        ),
        pat(
            "suffix_automaton",
            "Suffix Automaton (DAWG of Factors)",
            "pattern",
            "string_index",
            "Smallest DFA recognizing all substrings of a text; ≤ 2n-1 states built in online O(n).",
            "Index a text for fast substring, factor-count, and longest-common-substring queries.",
            "Suffix trees solve the same problems but use 4-8× the memory of suffix automata.",
            "Maintain link, len, transitions per state; extend one symbol at a time using suffix-link cloning to keep the automaton minimal.",
            "Compact and linear-time online construction; the link/clone update rule is subtle and easy to get wrong.",
            &["formal_languages_and_automata"],
            &["suffix_automaton", "substring", "indexing"],
            "https://en.wikipedia.org/wiki/Suffix_automaton",
        ),
        pat(
            "cdawg_compact_dawg",
            "Compact / Symmetric Compact DAWG (CDAWG / SCDAWG)",
            "pattern",
            "string_index",
            "Path-compressed suffix automaton (CDAWG) or its symmetric variant exposing both prefix and suffix extension.",
            "Index large texts with both factor membership and bidirectional extension in compact memory.",
            "Suffix automata waste states on unary chains for long unique substrings.",
            "Collapse non-branching chains in the suffix automaton; for SCDAWG, build the analogous structure for the reversed text and link the two via shared factor nodes.",
            "Even more compact than suffix automaton with bidirectional queries; algorithms and implementation are decidedly more complex.",
            &["formal_languages_and_automata"],
            &["cdawg", "scdawg", "suffix"],
            "https://en.wikipedia.org/wiki/Compact_directed_acyclic_word_graph",
        ),
        pat(
            "merkleized_trie",
            "Merkleized / Content-Addressed Trie",
            "pattern",
            "string_index",
            "Trie whose nodes are keyed and shared by a cryptographic hash of their content.",
            "Get structural sharing across versions and verifiable Merkle-style proofs of inclusion.",
            "Ordinary tries cannot prove inclusion or share storage across rebuilt copies.",
            "Hash each subtree bottom-up; intern nodes by their hash; serialize proofs as the sibling-hash path from leaf to root.",
            "Powerful for content-addressed stores and blockchains; hash recomputation on every update has a non-trivial cost.",
            &["formal_languages_and_automata", "distributed_systems"],
            &["merkle", "trie", "content_addressed"],
            "https://en.wikipedia.org/wiki/Merkle_tree",
        ),
        // ---------- string_matching ----------
        pat(
            "aho_corasick_multi_pattern",
            "Aho-Corasick Multi-Pattern Matching",
            "pattern",
            "string_matching",
            "Trie of all patterns augmented with failure links so a single text pass finds all occurrences in O(n + m + z).",
            "Locate every occurrence of any pattern from a fixed pattern set in linear time over the text.",
            "Running each pattern's matcher independently costs O(n · |P|).",
            "Build a trie over the patterns; compute a failure link from each node to the deepest proper suffix that is also a trie prefix; walk the text, following failure links on mismatches.",
            "Classic multi-pattern matcher used in IDS, lexers, dictionary scanners; failure links and output links must be carefully precomputed.",
            &["formal_languages_and_automata"],
            &["aho_corasick", "multi_pattern", "trie"],
            "https://en.wikipedia.org/wiki/Aho%E2%80%93Corasick_algorithm",
        ),
        pat(
            "bk_tree",
            "BK-Tree (Burkhard-Keller)",
            "pattern",
            "string_matching",
            "Metric tree on strings keyed by discrete edit distance, exploiting the triangle inequality for k-NN pruning.",
            "Answer approximate-membership queries (\"all dictionary words within edit distance k\") efficiently.",
            "Scanning the dictionary linearly is too slow when dictionaries are large or queries are repeated.",
            "Insert each word as a child of an existing node under the edge labeled by the integer distance to that node; query by descending only into children whose distance label is in [d(q, node) - k, d(q, node) + k].",
            "Effective for moderate edit budgets and small alphabets; performance degrades for high-k queries or non-discrete metrics.",
            &["formal_languages_and_automata"],
            &["bk_tree", "metric_tree", "approximate"],
            "https://en.wikipedia.org/wiki/BK-tree",
        ),
        pat(
            "ngram_index_filter",
            "N-gram Index Filter",
            "pattern",
            "string_matching",
            "Pre-filter candidate strings by counting q-gram overlap with the query.",
            "Cheaply bound the candidate set for an expensive verifier (edit distance, alignment, automaton intersection).",
            "Running expensive verifiers against an entire dictionary is wasteful when most candidates have no chance of matching.",
            "Index strings by their q-grams; for a query, compute its q-grams and select candidates sharing at least ⌈(|s|-q+1) - k·q⌉ overlapping q-grams (k = edit budget).",
            "Cuts verifier work by orders of magnitude with no false negatives; choosing q and overlap thresholds is workload-dependent.",
            &["formal_languages_and_automata"],
            &["ngram", "filter", "approximate"],
            "https://en.wikipedia.org/wiki/N-gram",
        ),
        pat(
            "dawg_bloom_negative_prefilter",
            "DAWG Bloom-Filter Negative Pre-filter",
            "pattern",
            "string_matching",
            "Bloom filter sitting in front of a DAWG to reject non-members without traversing the automaton.",
            "Avoid touching the DAWG (and its cache footprint) on the common case of dictionary misses.",
            "Each DAWG traversal pays cache misses and conditional branches even when the answer is \"not present\".",
            "Insert every dictionary word into a Bloom filter sized to the desired false-positive rate; on lookup, query the Bloom first and only descend into the DAWG on a hit.",
            "Big wins on miss-heavy workloads; the Bloom filter must be regenerated when the dictionary changes and tuned for FP rate vs memory.",
            &["formal_languages_and_automata"],
            &["bloom", "dawg", "prefilter"],
            "https://en.wikipedia.org/wiki/Bloom_filter",
        ),
        // ---------- edit_distance ----------
        pat(
            "wagner_fischer_dp",
            "Wagner-Fischer Dynamic Programming",
            "pattern",
            "edit_distance",
            "Classic O(m·n) DP for Levenshtein distance with row-rolling O(min(m, n)) space.",
            "Compute exact edit distance and an alignment between two strings.",
            "Naive enumeration of edits is exponential.",
            "Fill a DP table d[i][j] = min(d[i-1][j]+1, d[i][j-1]+1, d[i-1][j-1] + (a_i != b_j)); roll two rows to drop a factor of m in memory.",
            "Foundation of edit-distance work; quadratic time is too slow for long strings without further structure.",
            &["formal_languages_and_automata"],
            &["edit_distance", "wagner_fischer", "dp"],
            "https://en.wikipedia.org/wiki/Wagner%E2%80%93Fischer_algorithm",
        ),
        pat(
            "myers_bit_parallel_edit_distance",
            "Myers Bit-Parallel Edit Distance",
            "pattern",
            "edit_distance",
            "O(m·n / w) bitmask DP for edit distance, where w is the machine word size.",
            "Compute Levenshtein distance ~32-64× faster than Wagner-Fischer when m fits in a few words.",
            "Wagner-Fischer's per-cell work dominates short-string workloads where SIMD/word parallelism is wasted.",
            "Encode the DP frontier as bitmasks Pv/Mv (positive/negative column changes); update with bitwise ops per text symbol using precomputed pattern masks.",
            "Massive speedup on short patterns and binary alphabets; restricted to integer-valued edit costs.",
            &["formal_languages_and_automata"],
            &["edit_distance", "bit_parallel", "myers"],
            "https://doi.org/10.1145/316542.316550",
        ),
        pat(
            "simd_vectorized_edit_distance",
            "SIMD-Vectorized Edit Distance",
            "pattern",
            "edit_distance",
            "Banded DP whose anti-diagonals are computed with SSE/AVX/NEON vector lanes.",
            "Squeeze out 4-16× speedup on commodity CPUs by processing multiple DP cells per instruction.",
            "Scalar DP wastes lane parallelism that modern CPUs provide for free.",
            "Compute DP cells along anti-diagonals so that lane data dependencies are absent within a step; pick lane width by runtime CPU dispatch.",
            "Excellent throughput when bands are wide enough to amortize setup; pathological short patterns underutilize wide vectors.",
            &["formal_languages_and_automata", "parallel_programming"],
            &["edit_distance", "simd", "vectorization"],
            "https://en.wikipedia.org/wiki/Single_instruction%2C_multiple_data",
        ),
        pat(
            "damerau_levenshtein_distance",
            "Damerau-Levenshtein Distance",
            "pattern",
            "edit_distance",
            "Edit distance that adds the transposition of two adjacent characters as a unit-cost operation.",
            "Better model real typing and OCR errors where neighbor swaps are common.",
            "Pure Levenshtein assigns swaps a cost of two, penalizing realistic typos.",
            "Augment the DP recurrence with d[i-1][j-1] + 1 when (a_i = b_{j-1}) ∧ (a_{i-1} = b_j); choose restricted (OSA) or full Damerau-Levenshtein semantics.",
            "More forgiving distance for human-typing models; the full (non-restricted) variant is harder to compute and uses extra DP layers.",
            &["formal_languages_and_automata"],
            &["edit_distance", "damerau", "typo"],
            "https://en.wikipedia.org/wiki/Damerau%E2%80%93Levenshtein_distance",
        ),
        pat(
            "jaro_winkler_similarity",
            "Jaro / Jaro-Winkler Similarity",
            "pattern",
            "edit_distance",
            "Match-and-transposition-count similarity in [0, 1] with a configurable prefix bonus.",
            "Score record-linkage candidates (especially short names) more usefully than raw edit distance.",
            "Levenshtein assigns equal cost regardless of position, but humans weight prefixes more heavily.",
            "Count matches within a window |max(m, n)|/2 - 1; compute Jaro from (matches, transpositions, lengths); add Winkler's prefix bonus up to four shared leading characters.",
            "Workhorse for fuzzy name matching; not a metric (no triangle inequality), so cannot be used for metric indexes like BK-trees.",
            &["formal_languages_and_automata"],
            &["similarity", "jaro", "winkler", "record_linkage"],
            "https://en.wikipedia.org/wiki/Jaro%E2%80%93Winkler_distance",
        ),
        pat(
            "move_split_merge_metric",
            "Move-Split-Merge (MSM) Metric",
            "pattern",
            "edit_distance",
            "Time-series distance with move, split, and merge operations replacing the classic substitute/insert/delete.",
            "Compute a true metric for time-series similarity with no warping bias.",
            "Dynamic Time Warping is not a metric and tolerates pathological alignments.",
            "Define costs c_move (point-to-point), c_split (split a value into a run), c_merge (merge a run); run DP analogous to Levenshtein over the new operation set.",
            "Metric property enables triangle-inequality indexing; harder to tune than DTW for some workloads.",
            &["formal_languages_and_automata"],
            &["time_series", "metric", "msm"],
            "https://doi.org/10.1109/TKDE.2012.88",
        ),
        pat(
            "lb_keogh_dtw_lower_bound",
            "LB_Keogh-style Lower-Bound Pruning",
            "pattern",
            "edit_distance",
            "Cheap envelope-based lower bound on warping distances used to prune candidates before exact DP.",
            "Skip the full O(n²) DP on the vast majority of candidates in nearest-neighbor warping search.",
            "Computing exact DTW/MSM against every candidate dominates runtime even with early-abandon.",
            "Compute upper/lower envelopes of the query within the warping band; sum squared deviations of a candidate outside the envelopes as the lower bound; only do exact DP when the bound is below the current best.",
            "Order-of-magnitude speedups for k-NN warping queries; bound tightness depends on warping bandwidth and series variability.",
            &["formal_languages_and_automata"],
            &["time_series", "lower_bound", "keogh"],
            "https://en.wikipedia.org/wiki/Dynamic_time_warping",
        ),
        // ---------- approximate_match ----------
        pat(
            "universal_levenshtein_automaton",
            "Universal Levenshtein Automaton",
            "pattern",
            "approximate_match",
            "Parameter-free DFA recognizing the bounded edit-distance neighborhood of any query word via characteristic-vector encoding.",
            "Match approximately against a dictionary in O(|text|) with no per-query automaton rebuild.",
            "Building a fresh Levenshtein NFA per query is too slow for interactive lookups.",
            "Precompute the universal automaton offline; at query time, encode the symbol-match window as a characteristic bit vector and dispatch into the corresponding state.",
            "Fastest known online approximate matcher for fixed k; non-trivial construction with subtle subsumption rules.",
            &["formal_languages_and_automata"],
            &["levenshtein", "automaton", "approximate"],
            "https://doi.org/10.1023/A:1014184110234",
        ),
        pat(
            "parameterized_levenshtein_automaton",
            "Parameterized Levenshtein Automaton",
            "pattern",
            "approximate_match",
            "Per-query automaton over Levenshtein positions (i, e) annotated with characteristic vectors.",
            "Match a single query against many candidates with one query-specific automaton.",
            "Building a full position automaton from scratch is wasteful when the alphabet is large.",
            "Maintain reachable position sets ordered by lex; advance by symbol using characteristic vectors that encode the next k+1 matches.",
            "Compact and reusable per query; coordination with subsumption is required to keep state sets minimal.",
            &["formal_languages_and_automata"],
            &["levenshtein", "automaton", "parametric"],
            "https://doi.org/10.1023/A:1014184110234",
        ),
        pat(
            "levenshtein_position_subsumption",
            "Subsumption-Based Levenshtein State Minimization",
            "pattern",
            "approximate_match",
            "Online removal of Levenshtein positions dominated by other positions in the same state under the subsumption order.",
            "Keep state sizes minimal during Levenshtein-automaton simulation.",
            "Without subsumption, position sets grow combinatorially and dominate runtime.",
            "After each transition, drop any position (i, e) for which another position (i', e') ≤^χ_s (i, e) is present.",
            "Critical for practical performance; reasoning about correctness of the subsumption order is delicate.",
            &["formal_languages_and_automata"],
            &["levenshtein", "subsumption", "minimization"],
            "https://en.wikipedia.org/wiki/Levenshtein_automaton",
        ),
        pat(
            "wallbreaker_pigeonhole_match",
            "WallBreaker Pigeonhole Approximate Match",
            "pattern",
            "approximate_match",
            "Split the query into k+1 (or 2k+1) pieces; exact-match each piece; verify by bidirectional extension.",
            "Use exact-match infrastructure (suffix array, FM-index, DAWG) to drive approximate matching with edit budget k.",
            "Approximate matchers running directly against a dictionary cost more than exact-match plus a small verifier.",
            "Partition the query into k+1 contiguous pieces; for each piece, find exact occurrences in the index; for each hit, verify by computing edit distance on a window around the hit, extending bidirectionally.",
            "Excellent throughput when k is small and the index supports fast exact search; piece-count tuning is workload-dependent.",
            &["formal_languages_and_automata"],
            &["pigeonhole", "wallbreaker", "approximate"],
            "https://doi.org/10.1007/978-3-642-39286-5_18",
        ),
        pat(
            "product_automaton_intersection",
            "Product Automaton / Intersection Traversal",
            "pattern",
            "approximate_match",
            "Walk a dictionary DFA in lockstep with a Levenshtein automaton, advancing along compatible pairs of edges.",
            "Enumerate all dictionary words within edit distance k in time proportional to the joint search size.",
            "Brute-force dictionary scans waste work on irrelevant paths.",
            "Maintain a worklist of (dict_state, lev_state) pairs; for each character of the dictionary edge, advance both automata; emit when both reach an accepting state.",
            "Optimal asymptotic algorithm for dictionary-bounded approximate match; pair-state explosion can occur for high k.",
            &["formal_languages_and_automata"],
            &["product_automaton", "intersection", "dictionary"],
            "https://en.wikipedia.org/wiki/Levenshtein_automaton",
        ),
        // ---------- phonetic ----------
        pat(
            "soundex_phonetic",
            "Soundex",
            "pattern",
            "phonetic",
            "Classical consonant-class hash that maps English names to a 4-character code.",
            "Group similar-sounding English names (Robert/Rupert) for record-linkage.",
            "Exact spelling comparison misses common phonetic equivalences.",
            "Keep the first letter; map remaining consonants to digit classes (b/f/p/v→1, c/g/j/k/q/s/x/z→2, ...); collapse adjacent duplicates; pad/truncate to length 4.",
            "Extremely cheap and universal; English-centric and famously imprecise.",
            &["formal_languages_and_automata"],
            &["phonetic", "soundex", "record_linkage"],
            "https://en.wikipedia.org/wiki/Soundex",
        ),
        pat(
            "metaphone_phonetic",
            "Metaphone / Double Metaphone",
            "pattern",
            "phonetic",
            "Context-sensitive English phonetic encoder with single and double-code variants for ambiguous pronunciations.",
            "Get more accurate English phonetic equivalence than Soundex while staying lightweight.",
            "Soundex misses many real phonetic equivalences and has no concept of pronunciation context.",
            "Apply a rule set keyed on character n-grams in their context (e.g. \"GH\" silent in some positions); Double Metaphone outputs two alternate codes when pronunciation is ambiguous.",
            "Strong general-purpose English phonetic key; rule sets are language-specific and pre-2000s in pedigree.",
            &["formal_languages_and_automata"],
            &["phonetic", "metaphone"],
            "https://en.wikipedia.org/wiki/Metaphone",
        ),
        pat(
            "nysiis_phonetic",
            "NYSIIS",
            "pattern",
            "phonetic",
            "American name-matching encoder with explicit prefix/suffix and positional substitution rules.",
            "Get phonetic equivalence tuned for the New York State Identification & Intelligence System name corpus.",
            "Soundex collisions are unhelpful for many American name patterns.",
            "Apply prefix rules (MAC→MCC, KN→NN, ...), iterate positional substitutions over the remaining characters, truncate to a fixed length.",
            "Better recall than Soundex for many American names; rule set is somewhat ad-hoc and English-centric.",
            &["formal_languages_and_automata"],
            &["phonetic", "nysiis"],
            "https://en.wikipedia.org/wiki/New_York_State_Identification_and_Intelligence_System",
        ),
        pat(
            "caverphone_phonetic",
            "Caverphone",
            "pattern",
            "phonetic",
            "NZ-English phonetic encoder developed for the Caversham historical-records project.",
            "Get phonetic equivalence tuned for late-19th-century NZ-English name spellings.",
            "Soundex/Metaphone drift for accents and orthographies far from American English.",
            "Apply Caverphone's substitution rules (vowel deletion, consonant clustering) and truncate to a fixed length to produce a key.",
            "Targeted accuracy for its corpus; limited applicability outside its design domain.",
            &["formal_languages_and_automata"],
            &["phonetic", "caverphone"],
            "https://en.wikipedia.org/wiki/Caverphone",
        ),
        pat(
            "cologne_phonetic",
            "Cologne Phonetic (Kölner Phonetik)",
            "pattern",
            "phonetic",
            "Phonetic encoder for German optimized for German pronunciation rules.",
            "Match German names and words that have varying spellings (Meyer/Maier/Mayer).",
            "Soundex misses German-specific patterns (umlauts, sch-, -ch-, c/k variation).",
            "Apply a rule table mapping each letter or digraph (in context) to a digit code; collapse repeats; drop zeros except in leading position.",
            "Strong for German corpora; not portable to other languages.",
            &["formal_languages_and_automata"],
            &["phonetic", "cologne", "german"],
            "https://en.wikipedia.org/wiki/Cologne_phonetics",
        ),
        pat(
            "beider_morse_phonetic",
            "Beider-Morse Phonetic Matching",
            "pattern",
            "phonetic",
            "Multi-language genealogical phonetic-matching system producing multiple weighted codes per name.",
            "Match historical names across European languages and orthographic conventions.",
            "Single-code phonetic encoders collapse too aggressively for cross-language genealogy.",
            "Detect the source language, apply language-specific rules, and emit a set of phonetic codes representing plausible pronunciations.",
            "Best-in-class recall for genealogy at the cost of significant per-name work and a large rules database.",
            &["formal_languages_and_automata"],
            &["phonetic", "beider_morse", "genealogy"],
            "https://en.wikipedia.org/wiki/Daitch%E2%80%93Mokotoff_Soundex",
        ),
        pat(
            "daitch_mokotoff_phonetic",
            "Daitch-Mokotoff Soundex",
            "pattern",
            "phonetic",
            "Extension of Soundex with rules for Slavic and Yiddish-derived names.",
            "Match Jewish and Eastern-European names where Soundex collapses important distinctions.",
            "Soundex was designed for English and loses fidelity on E. European patterns.",
            "Maintain a rule table that produces 6-digit codes with multiple emissions per ambiguous letter sequence.",
            "Better recall than Soundex for its target corpus; rule set is large and historical.",
            &["formal_languages_and_automata"],
            &["phonetic", "daitch_mokotoff"],
            "https://en.wikipedia.org/wiki/Daitch%E2%80%93Mokotoff_Soundex",
        ),
        // ---------- abstract_machine ----------
        pat(
            "warren_abstract_machine",
            "Warren Abstract Machine (WAM)",
            "pattern",
            "abstract_machine",
            "Heap, stack, and choice-point machine specialized for Prolog/unification execution.",
            "Run Prolog efficiently with structure sharing, choice points, and last-call optimization.",
            "Naive interpreters for resolution and unification are too slow for real Prolog workloads.",
            "Implement WAM instructions (get/put/unify family, try/retry/trust, deallocate, etc.) over a heap + stack + trail + choice-point stack; compile Prolog clauses to WAM code.",
            "Foundation of every serious Prolog system; instruction-level reasoning is intricate and tied tightly to Prolog semantics.",
            &["formal_languages_and_automata", "logic_programming"],
            &["wam", "prolog", "abstract_machine"],
            "https://en.wikipedia.org/wiki/Warren_Abstract_Machine",
        ),
        pat(
            "cesk_abstract_machine",
            "CESK / CEK Abstract Machine",
            "pattern",
            "abstract_machine",
            "Operational-semantics machine with Control, Environment, Store, and Kontinuation components.",
            "Define small-step semantics for lambda-calculus-like languages in a way that supports abstract interpretation.",
            "Big-step semantics conflate steps that abstract interpreters need to discriminate.",
            "Represent program states as 4-tuples (C, E, S, K); define a transition function over each construct; specialize to CEK by dropping the explicit store for pure languages.",
            "Cleanly separates evaluation context from environment and store; foundation for abstract-machine-based analyzers and interpreters.",
            &["formal_languages_and_automata", "functional_programming"],
            &["cesk", "cek", "operational_semantics"],
            "https://en.wikipedia.org/wiki/CEK_Machine",
        ),
        pat(
            "tiered_bytecode_vm_jit",
            "Tiered Bytecode VM with JIT",
            "pattern",
            "abstract_machine",
            "Multi-tier execution stack: interpreter → baseline compiler → optimizing JIT, with on-stack replacement at hot loops.",
            "Get fast startup and warm-up while still hitting peak performance on hot code.",
            "A single-tier interpreter is too slow; a single-tier optimizing JIT pays compilation cost up front.",
            "Begin in an interpreter that profiles call counts and types; promote hot methods to a baseline compiler; further promote ultra-hot methods to an optimizing JIT; install OSR entries so loops can transition mid-execution.",
            "Excellent startup-and-peak trade-off in V8/HotSpot/CoreCLR; tier-transition logic and deoptimization are major sources of complexity.",
            &["formal_languages_and_automata"],
            &["jit", "vm", "tiered"],
            "https://en.wikipedia.org/wiki/Tracing_just-in-time_compilation",
        ),
        // ---------- pattern_match ----------
        pat(
            "unification_first_order",
            "First-Order Unification (Robinson's Algorithm)",
            "pattern",
            "pattern_match",
            "Compute the most-general unifier of two first-order terms or fail.",
            "Engine for type inference, logic programming, term rewriting, and theorem proving.",
            "Hand-coded matchers conflate substitution and structural recursion and fail under variable aliasing.",
            "Decompose pairs of terms recursively; on variable-vs-term, bind subject to occurs check; on function symbols of equal arity, recurse pairwise; fail otherwise.",
            "Universal substitution algorithm; without occurs check, naive unification can build cyclic terms or loop forever.",
            &["formal_languages_and_automata", "logic_programming"],
            &["unification", "robinson", "mgu"],
            "https://en.wikipedia.org/wiki/Unification_(computer_science)",
        ),
        pat(
            "term_rewriting_system",
            "Term Rewriting System",
            "pattern",
            "pattern_match",
            "Set of rewrite rules l → r over terms, applied directionally to compute normal forms.",
            "Reduce expressions to normal forms (simplifier, compiler intermediate, equational engine).",
            "Hand-coded simplifiers tangle rule order, confluence, and termination.",
            "Define rules with variables; on each step, find a redex (rule-match), apply the substitution; iterate until no redex remains; prove confluence (Church-Rosser) and termination (well-founded order) where possible.",
            "Mature theory of confluence and termination; non-confluent systems can produce different answers depending on strategy.",
            &["formal_languages_and_automata"],
            &["rewriting", "trs", "normal_form"],
            "https://en.wikipedia.org/wiki/Rewriting",
        ),
        pat(
            "abstract_reduction_system",
            "Abstract Reduction System",
            "pattern",
            "pattern_match",
            "Pair (A, →) where → is a binary relation on A; abstracts term rewriting, β-reduction, and graph rewriting.",
            "Reason uniformly about confluence, termination, and normal forms across diverse rewriting calculi.",
            "Calculus-specific proofs of confluence/termination duplicate identical reasoning.",
            "State theorems (Church-Rosser, Newman's lemma) at the ARS level; instantiate to concrete systems by supplying the relation.",
            "Foundation of rewriting theory; concrete instantiations still require their own proofs of orientation and ordering.",
            &["formal_languages_and_automata"],
            &["rewriting", "ars", "confluence"],
            "https://en.wikipedia.org/wiki/Abstract_rewriting_system",
        ),
        pat(
            "multitrie_wildcard_match",
            "MultiTrie with Wildcards and Sub-Expressions",
            "pattern",
            "pattern_match",
            "Trie that indexes S-expression atoms using tokens for parenthesis open/close, wildcards, and ordinary symbols, with precomputed parenthesis-distance metadata.",
            "Match patterns containing wildcards and sub-expressions against an atom-space efficiently.",
            "Plain symbol tries cannot represent unification-style wildcards or nested sub-expression structure.",
            "Tokenize each indexed expression into (Exact symbol, Wildcard, LeftPar, RightPar); insert into a trie; on lookup, follow wildcard branches whenever a sub-expression of any structure may match, using precomputed parenthesis distances to skip irrelevant subtrees.",
            "Powerful structural pattern index for atom-spaces and meta-programming; index size and traversal complexity can grow rapidly without careful pruning.",
            &["formal_languages_and_automata"],
            &["trie", "multitrie", "atom_space"],
            "https://en.wikipedia.org/wiki/Trie",
        ),
        // ---------- graph_match ----------
        pat(
            "code_property_graph",
            "Code Property Graph (CPG)",
            "pattern",
            "graph_match",
            "Unified graph combining AST, CFG, DFG, PDG, and call-graph edges for whole-program static analysis.",
            "Query and reason about code with one graph database instead of stitching multiple analyses by hand.",
            "Running AST + CFG + DFG + PDG analyses independently complicates joint queries (e.g. taint flow that respects control flow).",
            "Build the AST; layer CFG edges over statements; overlay DFG edges between defs and uses; add PDG dependence edges; add call-graph edges; store as a property graph queryable with Gremlin/Cypher.",
            "Foundation of Joern-style vulnerability hunting and many bug-prediction analyses; graph builds are expensive and language-specific.",
            &["formal_languages_and_automata"],
            &["cpg", "graph", "static_analysis"],
            "https://en.wikipedia.org/wiki/Code_property_graph",
        ),
        pat(
            "vf2_subgraph_isomorphism",
            "VF2 / VF3 Subgraph Isomorphism",
            "pattern",
            "graph_match",
            "Backtracking subgraph-isomorphism algorithms with feasibility-pruning rules over partial mappings.",
            "Detect a pattern graph (e.g., a design pattern, a security smell) inside a larger code graph.",
            "Naive subgraph search is super-exponential and needs aggressive pruning.",
            "Maintain a partial mapping; extend by candidate pairs satisfying syntactic and look-ahead feasibility rules (Fcore, Fin, Fout); backtrack on failure.",
            "Strong general-purpose subgraph matcher; tuning depends heavily on graph density and attribute presence.",
            &["formal_languages_and_automata"],
            &["subgraph_isomorphism", "vf2", "vf3"],
            "https://en.wikipedia.org/wiki/Subgraph_isomorphism_problem",
        ),
        // ---------- probabilistic ----------
        pat(
            "weighted_automaton_semiring",
            "Semiring-Weighted Automaton",
            "pattern",
            "probabilistic",
            "Finite automaton whose transitions carry weights drawn from an arbitrary semiring.",
            "Unify probabilistic, tropical (shortest-path), log, and boolean automata under a single algebra.",
            "Ad-hoc weighted variants of automata duplicate algorithms with subtly different operators.",
            "Define the automaton over a semiring (S, ⊕, ⊗, 0, 1); compute path weights by ⊗ along the path and aggregate parallel paths by ⊕.",
            "One framework, many instantiations; numerical stability and best-path heuristics depend on semiring choice.",
            &["formal_languages_and_automata"],
            &["weighted_automaton", "semiring"],
            "https://en.wikipedia.org/wiki/Weighted_automaton",
        ),
        pat(
            "pcfg_probabilistic_grammar",
            "Probabilistic Context-Free Grammar (PCFG)",
            "pattern",
            "probabilistic",
            "CFG whose productions carry probabilities summing to 1 per non-terminal; assigns a probability to each parse tree.",
            "Score parses and disambiguate parses in NLP, code-completion, and program-synthesis contexts.",
            "Plain CFGs allow many parses with no preference; choosing arbitrarily produces poor results.",
            "Annotate each production A → α with P(A → α); compute parse probability by multiplying production probabilities; train via inside-outside or supervised counts.",
            "Cheap and interpretable parse scorer; assumes context independence that real languages violate.",
            &["formal_languages_and_automata"],
            &["pcfg", "probabilistic", "grammar"],
            "https://en.wikipedia.org/wiki/Stochastic_grammar",
        ),
        pat(
            "kneser_ney_ngram_smoothing",
            "Kneser-Ney N-gram Smoothing",
            "pattern",
            "probabilistic",
            "Absolute-discounting n-gram smoothing using continuation probabilities for backoff.",
            "Get state-of-the-art n-gram LM perplexity without resorting to neural models.",
            "Naive maximum-likelihood n-gram estimates assign zero probability to unseen events.",
            "Discount each observed n-gram count by a constant; redistribute the freed mass via a continuation probability that counts the number of distinct contexts in which a word appears.",
            "Best classical n-gram smoother; modified Kneser-Ney improves further and is the de facto baseline.",
            &[
                "formal_languages_and_automata",
                "machine_learning_engineering",
            ],
            &["ngram", "smoothing", "kneser_ney"],
            "https://en.wikipedia.org/wiki/Kneser%E2%80%93Ney_smoothing",
        ),
        // ---------- streaming ----------
        pat(
            "streaming_automaton_bounded_memory",
            "Streaming Automaton (Bounded-Memory Online Recognizer)",
            "pattern",
            "streaming",
            "Process arbitrarily long input with state size bounded independently of input length.",
            "Recognize or transform unbounded streams (logs, network traffic, sensor data) without buffering.",
            "Algorithms that need O(n) memory cannot run over genuinely unbounded streams.",
            "Restrict state representation to fixed-size summaries (sketches, fixed-arity automata states); emit decisions as input arrives.",
            "Constant memory and bounded latency; expressiveness is bounded by what can be summarized in fixed state.",
            &["formal_languages_and_automata"],
            &["streaming", "automaton", "online"],
            "https://en.wikipedia.org/wiki/Streaming_algorithm",
        ),
        pat(
            "newton_iteration_semiring_fixpoint",
            "Newton Iteration over Semirings",
            "pattern",
            "streaming",
            "Quadratic-convergence fixed-point iteration for systems of semiring equations (WPDS, dataflow, generating functions).",
            "Solve dataflow and interprocedural analyses faster than linear Kleene iteration.",
            "Kleene iteration converges linearly and can be slow for deep recursion or dense graphs.",
            "Decompose the fixed-point system as F(x) = c ⊕ x ⊗ A; iterate x_{k+1} = c ⊕ x_k ⊗ A ⊕ Δ(x_k) where Δ is the Jacobian-like correction; convergence is doubly-fast on idempotent commutative semirings.",
            "Big speedups on real analyses; the algebra and convergence analysis demand investment.",
            &["formal_languages_and_automata"],
            &["fixpoint", "newton", "wpds"],
            "https://doi.org/10.1109/LICS.2007.36",
        ),
        // ---------- constrained_decoding ----------
        pat(
            "constrained_decoding_grammar_mask",
            "Grammar-Constrained Decoding",
            "pattern",
            "constrained_decoding",
            "Mask LLM/sampler logits at each token step by the set of tokens that keep the output accepted by a grammar.",
            "Guarantee well-formed structured output (JSON, SQL, code) from an autoregressive model.",
            "Post-hoc validation of model outputs is slow, lossy, and offers no recovery.",
            "Maintain the grammar/automaton parser state as tokens are emitted; at each step, compute the prefix-acceptance mask over the model's vocabulary and zero out forbidden logits before sampling.",
            "Hard guarantees on output structure; the mask computation must be fast (incremental parser state) to keep tokens-per-second high.",
            &[
                "formal_languages_and_automata",
                "machine_learning_engineering",
            ],
            &["llm", "constrained_decoding", "grammar"],
            "https://en.wikipedia.org/wiki/Constrained_decoding",
        ),
        pat(
            "wfst_export_lm",
            "WFST Export of Language Model",
            "pattern",
            "constrained_decoding",
            "Export an n-gram or PCFG language model as a WFST with backoff ε-transitions.",
            "Compose a language model with acoustic models, edit-distance models, or grammar constraints in a single decoder.",
            "Per-model decoders cannot be combined without re-implementing each pipeline.",
            "Emit one state per LM history; transition on each next-token symbol with the appropriate weighted edge; add backoff ε-transitions from k-gram histories to (k-1)-gram histories with appropriate weights.",
            "Modular ASR/MT-style decoders; FST size grows with the LM and may require pruning or on-the-fly composition.",
            &["formal_languages_and_automata"],
            &["wfst", "lm", "decoder"],
            "https://en.wikipedia.org/wiki/Weighted_finite-state_transducer",
        ),
        // ---------- weighted automata families (Tier 1) ----------
        pat(
            "weighted_finite_state_acceptor",
            "Weighted Finite-State Acceptor (WFSA)",
            "pattern",
            "probabilistic",
            "Finite automaton where every transition and final state carries a weight from a semiring (⊕, ⊗); the acceptance value of input x is the semiring sum ⊕ over all accepting paths of the ⊗-product of edge weights along each path.",
            "Recognize weighted languages where multiple parses or interpretations co-exist and must be ranked, marginalized, or aggregated.",
            "Unweighted DFAs/NFAs return only yes/no — they lose probability, cost, count, and ambiguity information needed for statistical and quantitative reasoning.",
            "Parameterize the automaton by a semiring W; assign w∈W to every transition and final state; evaluate ⊕ over accepting paths of ⊗ along each path. Choose the semiring per query: Boolean for reachability, Tropical for shortest cost, Probability for marginal mass, Counting for ambiguity count.",
            "Subsumes the WFST-as-acceptor case (single-tape Mohri 1997); composition, determinization, ε-removal, and minimization are well-defined on the appropriate semiring subclasses (idempotent, ⊕-complete, Star).",
            &[
                "formal_languages_and_automata",
                "machine_learning_engineering",
            ],
            &["wfsa", "weighted_automaton", "semiring", "acceptor"],
            "https://en.wikipedia.org/wiki/Weighted_automaton",
        ),
        pat(
            "weighted_pushdown_automaton",
            "Weighted Pushdown Automaton (WPDA)",
            "pattern",
            "pushdown",
            "Pushdown automaton whose stack-manipulating transitions carry semiring weights; the run-value is the ⊕ over runs of the ⊗ along the run.",
            "Recognize weighted context-free languages — statistical CFGs, weighted matched-paren languages, cost-annotated nested structures — as automata rather than as program-analysis systems.",
            "Reps/Lal/Kidd WPDS is framed for static analysis (poststar/prestar); when the goal is straightforward weighted CFL recognition, the program-analysis framing is heavyweight.",
            "5-tuple (Q, Σ, Γ, δ, F) plus semiring W; transitions push/pop stack symbols and carry w∈W; configuration weight is ⊗ along the run, run-value is ⊕ across accepting runs. Droste/Dziadek/Kuich 2019 'simple reset' normal form simplifies algorithm presentation.",
            "Equivalent in expressiveness to weighted context-free grammars under the same semiring; degenerates to a WFSA when the stack alphabet is unary; sits below WPDS in the analysis hierarchy.",
            &["formal_languages_and_automata"],
            &["wpda", "weighted_automaton", "pushdown", "context_free"],
            "https://en.wikipedia.org/wiki/Pushdown_automaton",
        ),
        pat(
            "weighted_buchi_automaton",
            "Weighted Büchi Automaton (WBA)",
            "pattern",
            "temporal",
            "Büchi ω-automaton over a semiring; the value of an infinite run is the ⊕ over accepting runs of the ⊗-weight along the (infinite) run, evaluated in a Star/complete semiring.",
            "Quantitative model-checking of liveness/safety over infinite words — extending Boolean ω-acceptance to costs, probabilities, mean-payoffs, and energies.",
            "Classical Büchi only answers whether an accepting run exists — too coarse for mean-payoff games, energy automata, expected response time, and quantitative LTL.",
            "Equip a Büchi automaton with semiring-weighted transitions; total accepting weight on an infinite run requires the semiring to be Star (or ω-continuous) for the ⊗-product to converge.",
            "Subsumes mean-payoff, discounted-payoff, energy automata, and probabilistic Büchi; the semiring choice determines decidability — emptiness is PTIME on tropical-Star but undecidable for arbitrary semirings.",
            &[
                "formal_languages_and_automata",
                "machine_learning_engineering",
            ],
            &[
                "wba",
                "buchi",
                "omega_automaton",
                "weighted_automaton",
                "model_checking",
            ],
            "https://link.springer.com/book/10.1007/978-3-642-01492-5",
        ),
        pat(
            "weighted_visibly_pushdown_automaton",
            "Weighted Visibly Pushdown Automaton (WVPA)",
            "pattern",
            "pushdown",
            "VPA whose call/internal/return transitions carry semiring weights; the structural restriction (call→push, return→pop, internal→neutral) is preserved.",
            "Extend VPA's strong closure and decidability properties (Boolean closure, equivalence, intersection) to weighted languages over nested structured input (XML, balanced calls).",
            "Plain VPA only answers yes/no; structured nested recognition over weighted/quantitative criteria (nesting depth cost, parenthesis-matching probability) is not expressible.",
            "Partition Σ = Σ_call ⊎ Σ_int ⊎ Σ_ret; transitions over (Σ, Γ) carry semiring weights; matched call/return pairs ⊗ across the matched span. The visibility constraint preserves the structural-pushdown closure.",
            "Retains VPA's Boolean-closure, intersection, and decidable equivalence on commutative idempotent and fields-of-fractions semirings; subsumes weighted nested-word recognition.",
            &["formal_languages_and_automata"],
            &["wvpa", "vpa", "visibly_pushdown", "weighted_automaton"],
            "https://dl.acm.org/doi/10.1145/1007352.1007390",
        ),
        pat(
            "weighted_alternating_automaton_polynomial_transitions",
            "Weighted Alternating Automaton (Polynomial Transitions)",
            "pattern",
            "tree_automaton",
            "Alternating automaton whose transitions are polynomial expressions Σ_i ⊗_j q_j over the semiring; existential branching ⊕-sums sibling alternatives, universal branching ⊗-combines children.",
            "Model fork/join computations and game-like reachability where each branch carries a quantitative weight and both kinds of branching co-exist.",
            "Boolean alternating automata cannot express quantitative fork/join — e.g., parallel cost where one branch is a sum and another is a choice; weighted-and-alternating hybridization isn't trivially reducible to either weighted or alternating alone.",
            "Transition: state × Σ → polynomial in the semiring over successor states; runs are trees; the value of a run is the polynomial evaluated bottom-up under ⊕/⊗. Kostolányi/Mišún 2018 give the canonical formulation and the polynomial reduction.",
            "Subsumes weighted NFAs (no universal) and weighted tree automata (no existential branching); decidability of emptiness and equivalence depends on the semiring's algebraic properties.",
            &["formal_languages_and_automata"],
            &[
                "waa",
                "alternating",
                "weighted_automaton",
                "polynomial_transitions",
            ],
            "https://doi.org/10.1016/j.ic.2018.04.002",
        ),
        pat(
            "weighted_tree_automaton",
            "Weighted Tree Automaton (WTA)",
            "pattern",
            "tree_automaton",
            "Bottom-up tree automaton over a semiring; the value of a tree is the ⊕ over all accepting state assignments of the ⊗-product over node-transition weights.",
            "Recognize, rank, and aggregate ranked trees — parse trees, derivations, ASTs, MT alignments — with quantitative weights for ambiguity scoring and structural learning.",
            "Unweighted tree automata answer membership only; statistical parsers, tree-to-tree MT, and AST-quality scoring require a numerical value per tree, not a Boolean.",
            "δ: Q^k × Σ_k → W for each k-ary node label; tree value computed bottom-up by ⊗-combining child state-values and then ⊕ over all consistent state assignments. Maletti 2010 gives the canonical algorithms.",
            "Subsumes PCFG when the semiring is probability and the WTA is regular; bottom-up determinization is decidable on commutative idempotent semirings (Borchardt 2004); foundation for weighted tree-to-tree transducers.",
            &[
                "formal_languages_and_automata",
                "machine_learning_engineering",
            ],
            &["wta", "tree_automaton", "weighted_automaton", "semiring"],
            "https://www.grammars.grlmc.com/TATA/",
        ),
        pat(
            "parity_alternating_tree_automaton",
            "Parity Alternating Tree Automaton (PATA)",
            "pattern",
            "tree_automaton",
            "Alternating tree automaton with a parity acceptance condition (priority Ω: Q → ℕ); a run is accepted iff the maximum priority appearing infinitely often is even.",
            "Compile the modal μ-calculus into a tree-acceptance device that admits parity-game solvers and yields decidable, complexity-graded model checking.",
            "Direct μ-calculus model checking requires repeated nested fixed-point computations; the algorithmic literature is much richer for parity games than for raw μ-calculus.",
            "Map ⟨a⟩ to existential branching and [a] to universal branching; map μ-bound variables to odd priorities and ν-bound to even; emptiness reduces to a parity-game decision over the product of the automaton and the model.",
            "Quasi-polynomial parity-game algorithms (Calude/Jain/Khoussainov/Li/Stephan 2017) yield improved μ-calculus model-checking complexity; PATA is the standard translation target.",
            &["formal_languages_and_automata"],
            &[
                "pata",
                "alternating",
                "tree_automaton",
                "parity",
                "mu_calculus",
            ],
            "https://doi.org/10.1006/inco.1995.1149",
        ),
        pat(
            "weighted_multi_tape_automaton",
            "Weighted Multi-Tape Automaton (K-tape)",
            "pattern",
            "transducer",
            "K-tape automaton: each transition reads/writes K synchronized symbols and carries a semiring weight; the run-value is ⊕ over runs of ⊗ along the run.",
            "Synchronously process K input streams with weights — joint relations between K languages, K-way translation, K-way constraints across channels.",
            "Independent per-tape FSTs lose the synchronization constraint; ad-hoc product constructions over WFSTs don't generalize weights cleanly across K > 2 tapes.",
            "Transition: state × Σ^K → state × W; pair_n synchronously aligns K tapes; projection onto a single tape recovers a WFSA; K=2 degenerates to a WFST.",
            "Subsumes 2-tape WFST and 1-tape WFSA; projection, intersection on individual tapes, and K-way Cartesian alignment are well-defined; useful for multi-channel join-pattern analysis.",
            &["formal_languages_and_automata"],
            &["wmta", "multi_tape", "weighted_automaton", "synchronous"],
            "https://doi.org/10.1007/978-3-540-30586-6_19",
        ),
        pat(
            "multiset_automaton",
            "Multiset Automaton (MSA)",
            "pattern",
            "multiset_automaton",
            "Automaton that consumes a multiset (bag) of input tokens rather than an ordered sequence; transitions carry weights from a featured-multiset semiring (e.g. ℕ₀^F with cardinality-bounded composition).",
            "Recognize and weight bags of features (resources, tokens, capabilities) where order is irrelevant — multiset rewriting, resource analysis, capability-based access control.",
            "Sequence automata enforce order, which over-restricts when only multiplicity matters; plain set semantics loses cardinality information.",
            "Transitions consume sub-multisets δ ⊆ Σ^♯; state updates ⊗-combine featured-multiset weights; emptiness reduces to integer-linear feasibility for tropical-multiset semirings.",
            "Useful in capability-based static analysis, Petri-net relatives, and tropical-multiset cost analyses; expressive but emptiness can be undecidable in the unrestricted case.",
            &["formal_languages_and_automata"],
            &["msa", "multiset", "weighted_automaton", "resource_analysis"],
            "https://en.wikipedia.org/wiki/Multiset",
        ),
        pat(
            "cost_register_automaton",
            "Cost Register Automaton (CRA)",
            "pattern",
            "streaming",
            "Streaming finite automaton augmented with cost registers R; each transition reads a symbol and updates registers by semiring expressions of prior register values; output is a register at the final state.",
            "Compute streaming quantitative aggregations over inputs in constant memory and one pass — sum, max, sliding window, expected value — without retaining input history.",
            "Plain WFSAs compute path-values via ⊗ along the run; many quantitative streaming queries (sum, average, max-over-window) don't factor along a single path and need register-based accumulators.",
            "Δ: Q × Σ × R → Q × Expr(R, W) where Expr is a copyless arithmetic expression over the semiring; final output reads a selected register; copyless restriction gives decidable equivalence (Alur/D'Antoni 2013).",
            "Captures regular cost functions decidably; equivalent in expressiveness to MSO-definable cost functions for certain semirings; the copyless restriction is essential for decidability.",
            &[
                "formal_languages_and_automata",
                "machine_learning_engineering",
            ],
            &[
                "cra",
                "register",
                "streaming",
                "weighted_automaton",
                "semiring",
            ],
            "https://doi.org/10.4230/LIPIcs.FSTTCS.2013.341",
        ),
        pat(
            "symbolic_finite_transducer",
            "Symbolic Finite Transducer (SFT)",
            "pattern",
            "transducer",
            "Finite-state transducer where transitions are guarded by predicates over an effective Boolean algebra and produce outputs by Boolean-algebra terms, supporting infinite/symbolic alphabets.",
            "Recognize and transduce over alphabets too large to enumerate (Unicode codepoints, integer intervals, character classes) by lifting transitions to predicate-guarded form.",
            "Classical FSTs require explicitly enumerated finite alphabets; Unicode-range string transducers blow up in state size with per-character transition tables.",
            "Parameterize by a decision procedure for the Boolean algebra (⊥/¬/∧/∨ + decidable membership); transitions use predicates and term constructors instead of concrete symbols; minterm partitioning yields a deterministic equivalent.",
            "Composition, equivalence, and pre-image are decidable iff the underlying Boolean algebra is; production tooling in the Symbolic Automata library and in the mona/spot tradition.",
            &["formal_languages_and_automata"],
            &[
                "sft",
                "symbolic_transducer",
                "boolean_algebra",
                "transducer",
            ],
            "https://doi.org/10.1145/3093333.3009834",
        ),
        pat(
            "symbolic_finite_automaton",
            "Symbolic Finite Automaton (SFA)",
            "pattern",
            "finite_automaton",
            "Finite automaton over an effective Boolean algebra alphabet; transitions are guarded by Boolean-algebra predicates rather than concrete symbols.",
            "Recognize regular languages over very large or infinite alphabets (Unicode, integer ranges) without per-symbol state explosion.",
            "Concrete-alphabet automata over Unicode or large interval alphabets produce unmanageable transition tables; range-based encoding is ad hoc and breaks composition.",
            "δ: Q × Pred(B) → Q where B is an effective Boolean algebra with decidable ⊥/¬/∧/∨ and decidable membership; minterm partitioning produces a deterministic equivalent and supports complement, intersection, equivalence.",
            "Recognition, intersection, complement, equivalence are decidable iff B is; subsumes character-class lexers and predicate-dispatch trees; foundation for the modern symbolic-regex engines.",
            &["formal_languages_and_automata"],
            &["sfa", "symbolic_automaton", "boolean_algebra", "regex"],
            "https://doi.org/10.1145/2103656.2103674",
        ),
        pat(
            "weighted_two_way_transducer",
            "Weighted Two-Way Transducer (W2T)",
            "pattern",
            "transducer",
            "Two-way finite transducer (head can move left or right on the input tape) whose transitions carry semiring weights.",
            "Capture transductions that require revisiting input — reverse passes, copy/duplicate, look-ahead — with quantitative weights.",
            "One-way FSTs cannot express transductions like 'reverse the input' or 'duplicate each word'; their weighted variants inherit the same expressiveness limitation.",
            "5-tuple with HeadDirection ∈ {L, R, S} and weights from a semiring; runs are simulated via crossing sequences; emptiness, composition with one-way WFSTs, and functionality testing are decidable on appropriate semirings (Feng/Maletti 2022).",
            "Strictly more expressive than one-way WFSTs (Engelfriet/Hoogeboom 2001 for the unweighted case); useful for cross-channel constraint propagation and join-pattern analysis in concurrent calculi.",
            &["formal_languages_and_automata"],
            &["w2t", "two_way_transducer", "weighted_automaton"],
            "https://doi.org/10.1016/j.ic.2023.105020",
        ),
        pat(
            "probabilistic_automaton_logsumexp",
            "Probabilistic Automaton (log-semiring, Viterbi, Baum-Welch)",
            "pattern",
            "probabilistic",
            "Probabilistic finite automaton evaluated in the log-semiring (⊕ = log-sum-exp, ⊗ = +) for marginalization or the tropical semiring (⊕ = max, ⊗ = +) for Viterbi decoding; transition probabilities learned via Baum-Welch / EM.",
            "Implement HMMs and probabilistic FSAs in numerically stable log-space with one algorithmic skeleton across forward (marginal), Viterbi (decode), and forward-backward (learn).",
            "Raw-probability multiplication along long paths underflows; ad-hoc rescaling complicates code; algorithm specialization for each query (decode vs marginal vs learner) duplicates implementations.",
            "Encode probabilities in log-space; swap the semiring per query: log for marginals, tropical for Viterbi, expectation for entropy; reuse forward-backward as the gradient skeleton for EM/Baum-Welch.",
            "Numerical stability across long sequences; one set of automaton operations covers HMM, MEMM, structured-perceptron, and CRF decoding; the semiring choice _is_ the query choice.",
            &[
                "formal_languages_and_automata",
                "machine_learning_engineering",
            ],
            &[
                "pa",
                "hmm",
                "log_semiring",
                "viterbi",
                "baum_welch",
                "probabilistic",
            ],
            "https://doi.org/10.1109/5.18626",
        ),
        pat(
            "register_automaton_data_aware",
            "Register Automaton (data-aware)",
            "pattern",
            "register_automaton",
            "Finite automaton extended with k value-registers; transitions are gated by equality/comparison tests between input data values and register contents.",
            "Recognize data-language properties — uniqueness of identifiers, scope correctness, pairing of resources — without finite-alphabet abstraction.",
            "Plain FAs assume a fixed finite alphabet; data-rich properties (open scope, fresh name, equality across positions) cannot be encoded into Boolean state.",
            "Equip the FA with k registers; transitions test equality between the current data value and a register, optionally update some register; emptiness is decidable for unrestricted RAs but inclusion is undecidable in general.",
            "Captures fresh-name analysis, scope correctness, bounded-resource pairing; sub-classes (fresh-register, weak, strong) trade decidability against expressiveness; foundational for nominal automata.",
            &["formal_languages_and_automata"],
            &["ra", "register_automaton", "data_language", "fresh_names"],
            "https://doi.org/10.1016/0304-3975(94)90242-9",
        ),
        pat(
            "nominal_automaton_orbit_finite",
            "Nominal Automaton (orbit-finite)",
            "pattern",
            "register_automaton",
            "Automaton with an orbit-finite state space (finite up to permutation of a name set 𝔸); transitions equivariant under the name-permutation group.",
            "Recognize languages over data-with-symmetry — names, fresh resources, scope — where states must be invariant under renaming.",
            "Register automata expose register-internals; data languages with permutation-invariance (π-calculus channels, fresh identifiers) want quotient states by name-orbits, not concrete register tuples.",
            "Work in the category of nominal sets; states are orbit-finite under the name-group action; transitions are equivariant; orbit-finite emptiness and minimization are decidable; orbit-finite quotient yields a canonical representative.",
            "Cleaner theory than register automata for fresh-name properties; tight links to π-calculus, nominal logic, and the Fraenkel-Mostowski model of fresh names.",
            &["formal_languages_and_automata"],
            &[
                "na",
                "nominal_automaton",
                "orbit_finite",
                "fresh_names",
                "pi_calculus",
            ],
            "https://doi.org/10.1017/CBO9781139084673",
        ),
        pat(
            "extended_weighted_pushdown_system",
            "Extended Weighted Pushdown System (EWPDS)",
            "pattern",
            "pushdown",
            "WPDS augmented with merging functions that combine the caller-context weight with the callee-summary weight at each call/return pair.",
            "Capture interprocedural dataflow analyses with scoped local state (caller locals, calling-context) that vanilla WPDS cannot express soundly.",
            "Plain WPDS handles inter-procedural reachability but cannot model caller-local variables that must be 'reset' across call boundaries — the merge step is essential for sound local-variable semantics.",
            "Attach a MergeFunction<W>: W × W → W at call/return rules that combines caller and callee weights; poststar/prestar saturation algorithms generalize cleanly; reduces to WPDS when the merge is trivial ⊗.",
            "Captures SLAM/BLAST-style Boolean-program analyses with locals; required for sound caller-local semantics in pi-calculus-style scoping (PNew/PInput); slightly higher analysis cost than WPDS.",
            &["formal_languages_and_automata"],
            &[
                "ewpds",
                "wpds",
                "pushdown",
                "merge_function",
                "interprocedural",
            ],
            "https://doi.org/10.1007/978-3-540-77050-3_27",
        ),
        // ---------- weighted-automata algorithms + runtime substrate (Tier 2) ----------
        pat(
            "recovery_wfst_min_cost_repair",
            "Recovery WFST (minimum-cost parse repair)",
            "pattern",
            "transducer",
            "WFST whose weights are ProductWeight<TropicalWeight, EditWeight>; Viterbi finds the minimum-cost parse-repair (skip/delete/substitute/insert) for malformed input.",
            "Recover from parse errors with provably minimum-cost edits while preserving parser state — enabling graceful recovery in IDE/LSP workflows.",
            "Ad-hoc panic-mode recovery throws away parser context and produces unhelpful error spans; without weights, multiple plausible repairs cannot be ranked.",
            "Encode parse cost + edit cost as a lex-ordered product semiring; build a WFST whose transitions represent the {skip, delete, substitute, insert} repair operations; run Viterbi over the parse-state × input lattice.",
            "Yields one canonical minimum-cost recovery per error; cost decomposition (parse-quality vs edit-distance) allows tunable preferences; depends on a Mohri-style WFST infrastructure.",
            &["formal_languages_and_automata"],
            &[
                "recovery_wfst",
                "viterbi",
                "product_weight",
                "error_recovery",
                "transducer",
            ],
            "https://en.wikipedia.org/wiki/Weighted_finite-state_transducer",
        ),
        pat(
            "prediction_wfst_per_category_dispatch",
            "Prediction WFST (per-category dispatch)",
            "pattern",
            "transducer",
            "Per-grammar-category WFST encoding parser-action weights for next-symbol dispatch; predictions are scored by log-pushed weights for optimal beam pruning.",
            "Decide the highest-confidence parsing action at each cursor position by lifting per-category dispatch logic into a weighted-transducer composition pipeline.",
            "Ad-hoc dispatch tables produce inconsistent action ranking; without weights, ambiguous categories all fire equally and stall the parser.",
            "Build one WFST per non-terminal category; transitions carry tropical weights derived from rule probabilities; log-push to canonical form so that beam pruning is optimal (Mohri 2002).",
            "Predictable disambiguation; composable with recovery and constrained-decoding WFSTs in a single Mohri-style pipeline; log-pushing is required to make pruning sound.",
            &["formal_languages_and_automata"],
            &[
                "prediction_wfst",
                "tropical",
                "log_push",
                "dispatch",
                "beam_pruning",
            ],
            "https://en.wikipedia.org/wiki/Weighted_finite-state_transducer",
        ),
        pat(
            "optimization_transducer_cascade",
            "Optimization Transducer Cascade",
            "pattern",
            "transducer",
            "Pipeline of WFST optimization passes (dead-state elimination, state minimization, beam pruning, weight normalization) applied to a fixed point.",
            "Reduce a built dispatch/recovery WFST to its minimal canonical form for runtime performance without altering its weighted language.",
            "Hand-crafted WFSTs accrue dead states and redundant arcs; per-pass ad-hoc removal misses interactions across optimizations.",
            "Compose a sequence of OptimizationPass impls (each is a transducer of WFSTs); iterate to a fixed point; verify language-equivalence after each pass.",
            "Order-independent under confluent passes; quadratic-worst-case fixed-point cost is acceptable because dispatch WFSTs are small; pass-equivalence proofs become a maintenance asset.",
            &["formal_languages_and_automata"],
            &[
                "optimization_pass",
                "fixed_point",
                "wfst",
                "transducer_cascade",
            ],
            "https://en.wikipedia.org/wiki/Weighted_finite-state_transducer",
        ),
        pat(
            "composition_verification_transducer",
            "Composition Verification Transducer",
            "pattern",
            "transducer",
            "Verification pipeline applied to WFST compositions, checking language containment, no-spurious-actions, weight-consistency, and dispatch-determinism preservation.",
            "Ensure that combining two grammar dispatch WFSTs (e.g., via cross-grammar reuse) preserves the language and weight semantics each defines.",
            "Naïve WFST composition can introduce spurious paths, weight inflation, or determinism loss — silent regressions in dispatch behavior.",
            "After composition, run a fixed property suite per pair (containment, spurious-action absence, weight consistency, determinism preservation) before promoting the result.",
            "Catches composition regressions at build time; cheap relative to the runtime cost it avoids; the properties are themselves declarative transducer-level invariants.",
            &["formal_languages_and_automata"],
            &[
                "composition_verification",
                "wfst",
                "property_check",
                "dispatch",
            ],
            "https://en.wikipedia.org/wiki/Weighted_finite-state_transducer",
        ),
        pat(
            "wpds_poststar_prestar_saturation",
            "WPDS poststar / prestar saturation",
            "pattern",
            "pushdown",
            "Saturation-based algorithm computing the post* (forward-reachable) or pre* (backward-reachable) configurations of a WPDS, represented as a weighted P-automaton.",
            "Solve interprocedural reachability and dataflow problems over context-free call/return graphs by leveraging weighted-pushdown saturation.",
            "Direct fixed-point iteration over pushdown configurations is intractable; an automaton-based representation of the reachable set is the canonical compact form.",
            "Build a P-automaton encoding the initial configuration set; saturate by applying WPDS rules until no new transitions are added; the resulting P-automaton represents post* (or pre*) with semiring weights.",
            "PSPACE in general; PTIME for bounded-stack abstractions; foundation of model-checking tools for Boolean programs (Moped, WPDS++, the Weighted-PDS-Solver).",
            &["formal_languages_and_automata"],
            &["poststar", "prestar", "saturation", "wpds", "reachability"],
            "https://doi.org/10.1007/978-3-540-77050-3_27",
        ),
        pat(
            "wpds_walker_reactive_runtime_parser",
            "WPDS Walker (reactive runtime parser)",
            "pattern",
            "parser",
            "Reactive runtime parser driven by a WPDS step-engine: each event (State × Symbol → Transition) is processed by a fork-aware walker maintaining a beam over weighted configurations.",
            "Use the WPDS as the executable parser (not just a static-analysis device), producing weighted parse traces with lexicographic-weight tiebreaking and fork/join support.",
            "Standard WPDS use is offline (model-check, then discard); building an interactive parser on top requires reactive driving, fork management, and incremental commit/abort semantics.",
            "Define WpdsStepEngine: State × Event → Transition; the walker maintains a fork frontier, applies transitions across all live branches, prunes by lex-min, and commits a winner once the beam consolidates.",
            "Yields a uniform runtime for parse-with-recovery, parse-with-prediction, and parse-with-disambiguation; novel framing — the WPDS becomes the parser, not just the verifier; lexicographic tiebreaking is essential for determinism.",
            &["formal_languages_and_automata"],
            &[
                "wpds_walker",
                "reactive_parser",
                "fork",
                "lexicographic_weight",
            ],
            "https://en.wikipedia.org/wiki/Pushdown_automaton",
        ),
        pat(
            "wpds_incremental_session_lsp_didchange",
            "WPDS Incremental Session (LSP didChange)",
            "pattern",
            "parser",
            "WPDS-walker variant that maintains a checkpoint per parse-boundary token, supporting invalidate-after-position and incremental reparse from the nearest valid checkpoint on each LSP didChange edit.",
            "Re-parse only the affected suffix of a document on each editor edit, preserving the WPDS frontier up to the edit point and replaying only the suffix.",
            "Full reparse on every keystroke is wasteful; ad-hoc range-incremental parsing duplicates state machinery; LSPs need predictable latency on small edits.",
            "Checkpoint the WPDS configuration at every parse-boundary token; on didChange(pos), discard checkpoints ≥ pos and resume parsing from the nearest earlier checkpoint.",
            "Latency proportional to suffix length, not document length; checkpoint footprint grows with token count but caps at an LSP-tunable history; integrates with tree-sitter-style incremental semantic highlighting.",
            &["formal_languages_and_automata"],
            &[
                "incremental_parser",
                "lsp",
                "didchange",
                "wpds",
                "checkpoint",
            ],
            "https://en.wikipedia.org/wiki/Language_Server_Protocol",
        ),
        pat(
            "gss_runtime_wpds_substrate",
            "GSS as Runtime WPDS Substrate",
            "pattern",
            "pushdown",
            "Graph-Structured Stack (Tomita 1986) extended with semiring-weighted edges, used as the runtime substrate for WPDS configuration sharing in a fork-capable walker.",
            "Share suffix configurations across forked WPDS branches without duplicating stack content, while preserving per-branch weights and supporting weighted SPPF output.",
            "Each fork in a GLR-style runtime parser must avoid duplicating stack content for memory efficiency; combining a GSS with weighted WPDS configurations is a non-trivial substrate problem.",
            "Nodes represent stack-content equivalence classes; weighted edges carry semiring weights; fork creates new tip nodes sharing a common suffix; reduce collapses tips into shared GSS nodes.",
            "Quadratic-worst-case GLR memory becomes linear in the number of live frontiers; weighted variant enables ambiguity-preserving parsing; SPPF output reads naturally from the GSS at end-of-input.",
            &["formal_languages_and_automata"],
            &["gss", "weighted_stack", "wpds_substrate", "fork"],
            "https://en.wikipedia.org/wiki/Graph-structured_stack",
        ),
        pat(
            "algebraic_program_analysis_tarjan_path_expr",
            "Algebraic Program Analysis (Tarjan path expression)",
            "pattern",
            "pushdown",
            "Interprocedural dataflow analysis via Tarjan path-expression evaluation over a semiring; the CFG is decomposed into SCCs and recursively summarized as path expressions.",
            "Compute dataflow facts at every program point in O(|E| · α(|V|)) per query rather than per-fixpoint-iteration.",
            "Standard worklist-based dataflow is iteration-bound and inflexible; expressing the analysis as an algebraic path expression decouples evaluation strategy from definition.",
            "Compute the SCC decomposition; build a path expression PE per SCC using Tarjan's algorithm; evaluate PE under the analysis semiring; combine across SCCs via interprocedural composition.",
            "Reusable across many analyses (constant propagation, range, taint); analysis correctness reduces to semiring-axiom verification; foundation of the SeaHorn and ICRA tool families.",
            &["formal_languages_and_automata"],
            &[
                "algebraic_analysis",
                "tarjan",
                "path_expression",
                "semiring",
                "interprocedural",
            ],
            "https://doi.org/10.1145/3290362",
        ),
        pat(
            "affine_relation_analysis_wpds_weight",
            "Affine Relation Analysis (WPDS weight domain)",
            "pattern",
            "pushdown",
            "WPDS weight domain whose elements are vector spaces of affine relations (encoded as (n+1)×(n+1) matrices over the rationals); subsumes constant-propagation, copy-constant, and linear-constant propagation.",
            "Express interprocedural affine analyses (variables related by affine equalities) as a WPDS weight domain so that poststar saturation computes affine summaries automatically.",
            "Per-analysis bespoke implementations of affine reasoning duplicate matrix arithmetic and basis maintenance; embedding into WPDS lifts all algorithm machinery from analyzer to weight domain.",
            "Each weight is a basis of an affine subspace; ⊗ is composition of affine relations (matrix product modulo basis reduction); ⊕ is union of subspaces; saturation yields per-procedure affine summaries.",
            "Sound interprocedural affine reasoning at O(n^ω) matrix-op cost per ⊗; subsumes several classical analyses uniformly; the cost of basis reduction dominates in practice.",
            &["formal_languages_and_automata"],
            &["ara", "affine_relations", "wpds_weight", "vector_space"],
            "https://doi.org/10.1007/978-3-540-77050-3_27",
        ),
        pat(
            "relational_weight_domain_boolean_program",
            "Relational Weight Domain (Boolean program)",
            "pattern",
            "pushdown",
            "WPDS weight domain whose elements are binary relations over a state-variable set (sets of (G, G) pairs); ⊗ is relational composition, ⊕ is union.",
            "Model Boolean programs (SLAM/BLAST style) interprocedurally where each weight encodes the input-output transition relation on Boolean state variables.",
            "Naïve product-of-Boolean-states blows up the state set exponentially; encoding the relation succinctly as a (small) set of (G, G) pairs sidesteps the explosion.",
            "Use HashSet<(G, G)> as the weight; ⊗ composes (∃z. (a,z) ∈ R₁ ∧ (z,b) ∈ R₂); ⊕ unions; verify that the underlying relation is closed under the semiring operations.",
            "Memory bounded by the number of reachable input-output pairs (Reps/Lal/Kidd 2007 Def 7); foundation of model-check-by-WPDS for Boolean programs.",
            &["formal_languages_and_automata"],
            &[
                "relational_weight",
                "wpds_weight",
                "boolean_program",
                "slam",
            ],
            "https://doi.org/10.1007/978-3-540-77050-3_27",
        ),
        pat(
            "provenance_semiring_n_x_polynomials",
            "Provenance Semiring N[X] (polynomial how-provenance)",
            "pattern",
            "probabilistic",
            "Semiring N[X] of polynomials with natural-number coefficients over a finite set X of provenance variables; ⊕ = polynomial sum, ⊗ = polynomial product.",
            "Track 'how-provenance' through query/computation pipelines so that the answer to any query records the multiplicity and lineage of each source contribution.",
            "Bag-semantics provenance loses identity (which source produced which row); set-semantics loses cardinality; the right semiring is polynomials over variables identifying sources.",
            "Annotate each source row with a fresh variable x_i ∈ X; lift query operators to polynomial operations over N[X]; the result polynomial encodes which combinations of which sources contributed how often.",
            "Most general provenance semiring (specializes to all weaker semirings by homomorphism); foundation of provenance-aware databases (Green/Karvounarakis/Tannen 2007) and probabilistic-query semantics.",
            &["formal_languages_and_automata", "declarative_programming"],
            &[
                "provenance",
                "semiring",
                "n_x",
                "polynomial",
                "how_provenance",
            ],
            "https://doi.org/10.1145/1265530.1265535",
        ),
        pat(
            "forward_backward_semiring_marginalization",
            "Forward-Backward Semiring Marginalization",
            "pattern",
            "finite_automaton",
            "Two-pass dynamic-programming algorithm over a weighted automaton: forward pass computes the prefix-marginal weight at each state, backward pass the suffix-marginal; the product is the exact per-state marginal.",
            "Compute exact state-marginal weights in a WFSA — for HMM forward marginals, gradient passes in EM/Baum-Welch, or expectation-semiring evaluation.",
            "Per-path enumeration is exponential; per-state marginalization requires careful prefix/suffix decomposition that ad-hoc code gets wrong.",
            "Forward pass: α(q) = ⊕_p α(p) ⊗ δ(p, σ, q); backward pass symmetrically with β; per-state marginal = α(q) ⊗ β(q); generalizes to any commutative semiring.",
            "Time O(|Q|^2 · n); generalizes Viterbi (tropical), inside-outside (log), and gradient computation for EM (expectation semiring) under one skeleton.",
            &[
                "formal_languages_and_automata",
                "machine_learning_engineering",
            ],
            &[
                "forward_backward",
                "marginalization",
                "baum_welch",
                "semiring",
                "dynamic_programming",
            ],
            "https://doi.org/10.1109/5.18626",
        ),
        pat(
            "weight_pushing_log_normalization",
            "Weight Pushing (log-space normalization)",
            "pattern",
            "finite_automaton",
            "WFST normalization that pushes weights toward (or away from) the initial state until the weight of every input prefix is uniquely determined by the prefix itself.",
            "Produce a canonical form of a WFST under which optimal beam pruning is guaranteed sound — without weight pushing, beam pruning can prune the eventual best path.",
            "Naïvely pruning low-weight partial paths can lose the globally best path when weights are accumulated late along the run; weight pushing redistributes weights to expose them early.",
            "Compute the shortest-distance from each state to a final state (Bellman-Ford on the tropical semiring); push these distances forward so each transition carries the marginal cost-to-go.",
            "Beam pruning becomes optimal post-pushing; required precondition for many WFST optimizations; computation cost O(|E| + |V| log |V|) per pushing pass.",
            &["formal_languages_and_automata"],
            &[
                "weight_pushing",
                "log_push",
                "normalization",
                "beam_pruning",
                "mohri",
            ],
            "https://www.aclweb.org/anthology/J02-1003",
        ),
        pat(
            "token_lattice_lex_dag",
            "Token Lattice / Lex DAG",
            "pattern",
            "finite_automaton",
            "Weighted DAG over alternative tokenizations of a single input string; Viterbi finds the best total tokenization under a lex-tiebreaking product weight.",
            "Carry lex-time ambiguity from the lexer into the parser as a structured object, preserving every plausible tokenization rather than committing early to one.",
            "Greedy maximal-munch lexers commit to one tokenization at lex time; ambiguous tokens (`a`/`an`, `foo//` vs `foo /`) require lookahead the lexer doesn't have.",
            "Lex into a token DAG whose edges represent alternative tokens spanning a substring with a per-edge weight; the parser receives the DAG; Viterbi over (parse-weight × tokenization-weight) selects the best.",
            "Cleanly defers lex/parse ambiguity to a unified weighted decision; zero-overhead for the common no-ambiguity case (single-path DAG); memory cost is O(positions × max-overlap).",
            &["formal_languages_and_automata"],
            &[
                "lex_dag",
                "token_lattice",
                "viterbi",
                "tokenization",
                "ambiguity",
            ],
            "https://en.wikipedia.org/wiki/Lexical_analysis",
        ),
        pat(
            "abstract_cesk_dyck_state_graph",
            "Abstract CESK + Dyck State Graph",
            "pattern",
            "abstract_machine",
            "Abstract-CESK machine for operational-semantics analysis, with stack heights tracked by a Dyck-state-graph reduction that makes the analysis pushdown-precise rather than k-CFA-imprecise.",
            "Achieve pushdown-precision in operational-semantics analyses — distinguishing 'returns to caller A' from 'returns to caller B' — without paying the cost of full WPDS saturation.",
            "Standard k-CFA approximates stack heights, losing return-precision and yielding spurious flow through call/return imbalances.",
            "Lift the CESK transition system into an abstract domain; track stack frames via a Dyck state graph (Earl et al. 2013) whose accepted strings represent valid call/return matched sequences.",
            "Pushdown-precise control-flow analysis of higher-order programs at the cost of an extra graph reachability per analysis query; tight links to WPDS theory.",
            &["formal_languages_and_automata"],
            &["abstract_cesk", "dyck_graph", "pushdown_analysis", "aam"],
            "https://doi.org/10.1145/1932681.1863558",
        ),
        // ---------- foundational semirings (Tier 3) ----------
        pat(
            "semiring_algebra_principle",
            "Semiring Algebra (parameterizing weighted computation)",
            "principle",
            "probabilistic",
            "Algebraic structure (W, ⊕, ⊗, 0̄, 1̄) where ⊕ is associative-commutative with identity 0̄, ⊗ is associative with identity 1̄, ⊗ distributes over ⊕, and 0̄ ⊗ x = x ⊗ 0̄ = 0̄.",
            "Parameterize automata, parsers, and graph algorithms by the choice of weight algebra — recovering Boolean reachability, shortest paths, marginal probability, counting, and many others from one code skeleton.",
            "Bespoke per-algorithm implementations (Dijkstra, Bellman-Ford, Viterbi, forward, count-paths) duplicate machinery; the right abstraction is the underlying algebra.",
            "Express the algorithm over an abstract semiring W; specialize by choosing W: Boolean → reachability, Tropical (min, +) → shortest path, Probability → marginal, Counting (ℕ, +, ×) → path count.",
            "Algorithm correctness reduces to semiring-axiom satisfaction; performance specialization is per-semiring; idempotency, completeness, and Star properties determine which algorithms apply.",
            &["formal_languages_and_automata"],
            &["semiring", "algebra", "weighted_automaton"],
            "https://link.springer.com/book/10.1007/978-3-642-01492-5",
        ),
        pat(
            "tropical_semiring",
            "Tropical Semiring (min, +)",
            "pattern",
            "probabilistic",
            "Semiring (ℝ∪{+∞}, min, +, +∞, 0); ⊕ = min picks the minimum cost, ⊗ = + accumulates cost along a run.",
            "Recover shortest-path / minimum-cost algorithms by plugging the tropical semiring into a generic weighted-automaton skeleton.",
            "Hand-rolled shortest-path code (Dijkstra, Bellman-Ford, A*) does not compose with other analyses; lifting it to a semiring framework unlocks composition with marginal, counting, and lex semirings.",
            "Use (ℝ∪{+∞}, min, +, +∞, 0) as the weight; all path-summary algorithms become specializations of forward / shortest-distance over the tropical algebra.",
            "Idempotent (⊕ = min is idempotent), commutative, ⊕-complete; Viterbi decoding is forward on tropical; equivalent to log-semiring after the max-product → max-sum transform.",
            &["formal_languages_and_automata"],
            &["tropical", "semiring", "min_plus", "shortest_path"],
            "https://en.wikipedia.org/wiki/Tropical_semiring",
        ),
        pat(
            "log_semiring",
            "Log Semiring (log-sum-exp, +)",
            "pattern",
            "probabilistic",
            "Semiring (ℝ∪{+∞}, log-sum-exp, +, +∞, 0); ⊕ = log-sum-exp, ⊗ = +; encodes probabilities in log-space for numerical stability.",
            "Compute exact probability marginals over a WFSA in numerically stable log-space, avoiding underflow on long paths.",
            "Raw probability multiplication underflows after ~50 transitions in double precision; ad-hoc rescaling complicates downstream code.",
            "Use log-probabilities; ⊕ = log-sum-exp via the softmax-style trick (a ⊕ b = max(a,b) + log(1 + exp(-|a-b|))); ⊗ = +; final answer = exp(result) only if needed.",
            "Numerical stability across arbitrarily long paths; ⊕ is slightly more expensive than tropical; the forward algorithm on HMMs is exactly forward on the log-semiring.",
            &[
                "formal_languages_and_automata",
                "machine_learning_engineering",
            ],
            &["log_semiring", "log_sum_exp", "hmm", "probabilistic"],
            "https://en.wikipedia.org/wiki/LogSumExp",
        ),
        pat(
            "viterbi_semiring",
            "Viterbi Semiring (max, ×) / (max, +)",
            "pattern",
            "probabilistic",
            "Semiring ([0, 1], max, ×, 0, 1) — or equivalently tropical after the log transform — that yields the maximum-probability path through a WFSA via the Viterbi algorithm.",
            "Recover Viterbi decoding (single best path) as forward propagation over the max-product (or max-sum after log) semiring.",
            "Forward over the probability semiring computes total marginal; computing the most-likely single path requires a different ⊕ (max instead of sum).",
            "Swap ⊕ from sum to max while keeping ⊗ = ×; forward propagation now tracks the max-product along each path; back-pointers reconstruct the best path.",
            "Identical algorithm to forward up to semiring choice; back-pointer storage adds O(|Q| · n) memory; the semiring is idempotent (max is idempotent), so order of computation does not matter.",
            &[
                "formal_languages_and_automata",
                "machine_learning_engineering",
            ],
            &["viterbi", "semiring", "max_product", "decoding"],
            "https://doi.org/10.1109/PROC.1973.9030",
        ),
        pat(
            "arctic_max_plus_semiring",
            "Arctic Semiring (max, +)",
            "pattern",
            "probabilistic",
            "Semiring (ℝ∪{−∞}, max, +, −∞, 0) — the dual of tropical; ⊕ = max picks the maximum gain, ⊗ = + accumulates gain along a run.",
            "Compute longest paths, maximum scores, longest accepted prefixes — the dual of shortest-path applications.",
            "Shortest-path algorithms over the tropical semiring can be inverted to longest-path only when the graph has no positive cycles; expressing it as an 'arctic semiring' makes the requirement explicit.",
            "Use (ℝ∪{−∞}, max, +); idempotent like tropical; well-suited to maximum-likelihood (via log) and longest-prefix problems.",
            "Idempotent, commutative, ⊕-complete on DAGs; converges on cycle-free graphs only; equivalent to negated tropical.",
            &["formal_languages_and_automata"],
            &["arctic", "max_plus", "semiring", "longest_path"],
            "https://en.wikipedia.org/wiki/Tropical_semiring",
        ),
        pat(
            "entropy_expectation_semiring",
            "Entropy / Expectation Semiring",
            "pattern",
            "probabilistic",
            "Semiring (ℝ × ℝ, ⊕_E, ⊗_E, (0, 0), (1, 0)) that simultaneously computes a marginal probability and the entropy of the distribution over accepting paths.",
            "Get gradients and entropy as a side-product of a single forward pass over a WFSA — without a second pass or hand-coded back-propagation.",
            "Computing entropy or per-parameter expectations classically requires forward + backward + per-step accumulation; encoding the expectation directly into a semiring removes the second pass.",
            "Pair (p, p·log(p)) per partial path; ⊕ adds both fields; ⊗ multiplies marginals and accumulates entropies via the chain rule (p₁·p₂, p₁·E₂ + p₂·E₁).",
            "Entropy, expected log-likelihood, and gradients computed in one forward pass; the encoding is general — works for any 'sum of expected functions over paths' (Eisner 2002).",
            &[
                "formal_languages_and_automata",
                "machine_learning_engineering",
            ],
            &["entropy_semiring", "expectation", "eisner", "gradient"],
            "https://www.aclweb.org/anthology/W02-1612",
        ),
        pat(
            "n_best_paths_semiring",
            "N-Best Paths Semiring",
            "pattern",
            "probabilistic",
            "Semiring of sorted N-element multisets over a base semiring; ⊕ merges two lists keeping the top-N, ⊗ pairwise-combines lists keeping the top-N of the product.",
            "Compute the top-N best paths through a WFSA via a single forward pass under the N-best semiring.",
            "Computing top-N paths classically requires Eppstein's algorithm or specialized heap structures; expressing it as a semiring unifies it with all forward-algorithm specializations.",
            "Weight = sorted Vec<W, len ≤ N>; ⊕ merges and truncates; ⊗ does pairwise combine and truncates; specializes to tropical at N=1, recovers Eppstein at N=k.",
            "Forward-algorithm yields N-best directly; ⊗ cost is O(N²) per edge — practical for small N (≤ 50); foundational for k-best parsing and lattice rescoring.",
            &[
                "formal_languages_and_automata",
                "machine_learning_engineering",
            ],
            &["n_best", "semiring", "top_k", "lattice_rescoring"],
            "https://doi.org/10.1145/258549.258560",
        ),
        pat(
            "product_lexicographic_semiring",
            "Product / Lexicographic Semiring",
            "pattern",
            "probabilistic",
            "Lexicographic product of two semirings (W₁, W₂); ⊕ = element-wise, ⊗ = left-projection-times (W₁'s ⊗ on the primary, W₂'s ⊗ tiebreaks).",
            "Combine a primary objective (parse-cost, edit-cost) with a tiebreaker (rule index, source priority) so that ties in the primary semiring are broken deterministically.",
            "Tied weights in a single semiring lead to non-deterministic best-path selection; ad-hoc tiebreaking complicates analysis-correctness reasoning.",
            "Define W = W₁ × W₂; ⊗ uses the standard product on W₁ but a left-projection product on W₂ that picks the W₂ value associated with the smaller W₁; ⊕ uses standard element-wise sum.",
            "Deterministic best-path selection under ties; semiring axioms preserved if W₁ and W₂ are; mettail-rust's LexicographicWeight = (TropicalWeight, src_idx, rule_idx) is a concrete instantiation.",
            &["formal_languages_and_automata"],
            &[
                "product_semiring",
                "lexicographic",
                "tiebreaker",
                "beam_pruning",
            ],
            "https://en.wikipedia.org/wiki/Lexicographic_order",
        ),
        pat(
            "tensor_bilinear_semiring",
            "Tensor (Bilinear) Semiring",
            "pattern",
            "probabilistic",
            "Bilinear tensor product of two semirings W₁ and W₂ — formal sums of elementary tensors w₁ ⊗ w₂ — supporting bilinear interaction between two simultaneous analyses.",
            "Combine two semirings where the interaction is bilinear (not lexicographic, not independent product) — e.g., weighted-relation × weighted-acceptor.",
            "Independent product W₁ × W₂ keeps the two analyses fully decoupled; lexicographic product treats one as primary; bilinear product allows true interaction between the two algebras.",
            "Weights = formal sums Σ_i (w₁ᵢ ⊗ w₂ᵢ); ⊕ concatenates formal sums; ⊗ pairwise bilinear-multiplies and rebuilds; canonical form requires reducing equivalent tensors.",
            "Genuinely bilinear interaction; element representation is exponentially larger than independent product; useful when neither component subordinates the other.",
            &["formal_languages_and_automata"],
            &["tensor_semiring", "bilinear", "formal_sum"],
            "https://en.wikipedia.org/wiki/Tensor_product",
        ),
        pat(
            "derivation_carrying_semiring",
            "Derivation-Carrying Semiring (ambiguity-preserving)",
            "pattern",
            "probabilistic",
            "Multiset semiring DerivationWeight<W, D> = Vec<(W, D)> over a free derivation monoid D; ⊕ = multiset union (does NOT disambiguate), ⊗ = pairwise concatenation.",
            "Carry full derivation traces alongside numeric weights so that ambiguity-preserving parsing yields every plausible derivation, not just the best.",
            "Standard ⊕ on numeric semirings collapses ambiguous derivations to a single weight, destroying trace information; tracing requires an explicit non-collapsing ⊕.",
            "Pair each weight with a derivation (an opaque trace token) and store them in a multiset; ⊕ unions multisets without merging equal weights; ⊗ pairwise-combines each (w₁, d₁) × (w₂, d₂); a SemiringRef variant escapes the Copy bound.",
            "Ambiguity-preserving — every accepted derivation is recoverable; multiset size can grow combinatorially; downstream consumers must explicitly choose a disambiguation policy.",
            &["formal_languages_and_automata", "functional_programming"],
            &[
                "derivation_semiring",
                "multiset",
                "trace",
                "ambiguity_preserving",
            ],
            "https://en.wikipedia.org/wiki/Multiset",
        ),
        // ---------- formal-language companions (Tier 4) ----------
        pat(
            "kleene_algebra_with_tests",
            "Kleene Algebra with Tests (KAT)",
            "principle",
            "parser",
            "Kleene algebra (K, +, ·, *, 0, 1) augmented with a Boolean subalgebra of tests; KAT proves equivalence of propositional program fragments by purely algebraic manipulation.",
            "Express and verify propositional Hoare-style program-equivalence proofs (e.g., 'this loop is the same as that one') algebraically rather than by full deductive reasoning.",
            "Hoare logic requires logical reasoning at every step; purely algebraic verification is more automatable but classical Kleene algebra lacks tests.",
            "Augment KA with a Boolean subalgebra B for tests; programs and tests combine via concatenation and choice; equivalence is decidable in PSPACE (Kozen 1997).",
            "Decidable propositional Hoare logic; foundation of NetKAT and the formal verification of forwarding-table updates; weaker than first-order Hoare but much more automatable.",
            &["formal_languages_and_automata", "declarative_programming"],
            &["kat", "kleene_algebra", "hoare_logic", "verification"],
            "https://en.wikipedia.org/wiki/Kleene_algebra_with_tests",
        ),
        pat(
            "petri_net_vass",
            "Petri Net / Vector Addition System with States (VASS)",
            "pattern",
            "abstract_machine",
            "Petri net (P, T, F, M₀) — places, transitions, flow relation, initial marking — equivalently a Vector Addition System with States — used to model concurrent systems and resource allocation.",
            "Model concurrent / resource-bounded computation with bag-semantics for tokens and analyze reachability, coverability, liveness, and fairness.",
            "Classical FSMs cannot express concurrent token-passing; CSP/CCS use process algebras with different proof techniques; Petri nets give a graph-theoretic foundation.",
            "Bipartite graph (places, transitions) with flow weights; a transition fires when all input places have ≥ required tokens; firing consumes inputs and produces outputs; marking evolves over firing sequences.",
            "Reachability is decidable (EXPSPACE-complete, Mayr 1981); coverability is EXPSPACE-complete; the Karp-Miller tree provides effective coverability decision; foundation of business-process modeling and concurrent-protocol verification.",
            &["formal_languages_and_automata"],
            &["petri_net", "vass", "concurrency", "reachability"],
            "https://en.wikipedia.org/wiki/Petri_net",
        ),
        pat(
            "e_graph_equality_saturation",
            "E-Graph + Equality Saturation",
            "pattern",
            "pattern_match",
            "E-graph: a data structure representing an equivalence class of expressions; equality saturation rewrites the e-graph by adding equivalences without choosing among alternatives until extraction time.",
            "Run multiple rewrite rules in parallel without committing to an extraction order — enabling globally optimal term rewriting (Willsey 2021, the egg library).",
            "Term-rewriting systems must choose a rewrite order, which can prevent reaching globally-optimal forms; backtracking is expensive.",
            "Represent each expression class as an e-node (operator + child e-classes); rewrites add equalities to the e-graph rather than replace; extract the optimal term post-saturation via a cost function over the e-graph.",
            "Globally optimal extraction (under the chosen cost function); saturation is bounded by the e-graph size; foundational for verified compilers (egg, ruler, herbie) and decision procedures (Nelson-Oppen).",
            &["formal_languages_and_automata"],
            &["e_graph", "equality_saturation", "egg", "term_rewriting"],
            "https://doi.org/10.1145/3434304",
        ),
        pat(
            "weighted_mso_logic_buchi_elgot_trakhtenbrot",
            "Weighted Monadic Second-Order Logic (Büchi-Elgot-Trakhtenbrot)",
            "principle",
            "probabilistic",
            "Weighted Monadic Second-Order logic over finite/infinite words; Droste-Gastin 2007 generalizes the Büchi-Elgot-Trakhtenbrot theorem to weighted automata under any commutative semiring.",
            "Specify quantitative properties of words/runs as MSO formulas; compile them automatically to weighted automata that decide them.",
            "Hand-rolled WFSA construction for quantitative properties is error-prone; MSO gives a high-level specification language but classical MSO is Boolean.",
            "Weighted MSO: atomic formulas evaluate to semiring values; conjunction = ⊗, disjunction = ⊕, existential quantification = ⊕, universal = ⊗; compilation to weighted automata is constructive.",
            "Decidable specification of quantitative properties; explicit semantics for second-order quantification over weighted runs; basis of many quantitative-verification toolchains.",
            &["formal_languages_and_automata"],
            &[
                "weighted_mso",
                "droste_gastin",
                "buchi_elgot",
                "quantitative_verification",
            ],
            "https://doi.org/10.1016/j.tcs.2007.05.001",
        ),
        pat(
            "ltl_to_buchi_compilation",
            "LTL → Büchi Compilation",
            "pattern",
            "temporal",
            "Compilation pipeline from Linear Temporal Logic formulas to non-deterministic Büchi automata that recognize all ω-words satisfying the formula.",
            "Reduce LTL model-checking to ω-language emptiness, leveraging Büchi-automaton machinery for the actual decision procedure.",
            "Direct LTL semantics requires interpreting nested temporal operators at each step; ω-automaton machinery is much better-developed and supports product/intersection.",
            "Compile LTL formula φ to non-deterministic Büchi NBA_φ via tableau construction (Gerth/Peled/Vardi/Wolper 1995) or via alternating automata (Vardi 1995); model-check by emptiness of NBA_φ × Kripke-structure.",
            "LTL model-checking decidable in PSPACE; foundation of SPIN, NuSMV, and Spot; compilation can blow up exponentially in formula nesting depth.",
            &["formal_languages_and_automata"],
            &[
                "ltl",
                "buchi",
                "model_checking",
                "vardi_wolper",
                "temporal_logic",
            ],
            "https://doi.org/10.1109/LICS.1986.4",
        ),
        pat(
            "presburger_arithmetic_decision_nfa",
            "Presburger Arithmetic Decision via NFA (Bartzis-Bultan)",
            "pattern",
            "finite_automaton",
            "Decision procedure for Presburger arithmetic (the first-order theory of ⟨ℕ, +⟩) via construction of a deterministic finite automaton over the alphabet {0,1}^k where k is the number of free variables.",
            "Decide quantified linear-integer-arithmetic formulas without resorting to deep theorem proving — exploiting the regular-language correspondence of Presburger-definable sets.",
            "Quantifier elimination for Presburger is doubly-exponential in worst case; the regular-language view (Büchi 1960) provides a more uniform machinery and effective tooling.",
            "Encode integers in binary, least-significant-bit-first; each Presburger formula corresponds to a DFA over {0,1}^k recognizing tuples satisfying the formula; ∃ becomes projection, ∧ becomes intersection.",
            "Decidable in PSPACE for unbounded variables; exponential lower bound (Fischer-Rabin 1974); foundation of LASH, MONA, and modern bit-vector decision procedures.",
            &["formal_languages_and_automata", "declarative_programming"],
            &["presburger", "decision_procedure", "nfa", "bartzis_bultan"],
            "https://en.wikipedia.org/wiki/Presburger_arithmetic",
        ),
        pat(
            "cegar_counterexample_guided_abstraction_refinement",
            "Counterexample-Guided Abstraction Refinement (CEGAR)",
            "pattern",
            "abstract_machine",
            "Iterative model-checking loop: build a coarse abstraction A, model-check φ on A; if A ⊨ φ accept; if A produces a counterexample, refine A to eliminate spurious ones.",
            "Scale model-checking to large systems by working on small abstractions, refining only as needed when spurious counterexamples appear.",
            "Direct model-checking of large systems is intractable; uniform-precision abstractions either lose too much (false alarms) or scale too poorly (refinement everywhere).",
            "Loop {build A, check A ⊨ φ, if ¬ then extract counterexample, test if spurious in concrete, if spurious refine A by adding distinguishing predicates}; terminates when A entails φ or a real counterexample is found.",
            "Practical model-checking of multi-million-state systems; convergence not guaranteed in general; foundation of SLAM, BLAST, CBMC, CPAchecker.",
            &["formal_languages_and_automata"],
            &[
                "cegar",
                "abstraction_refinement",
                "model_checking",
                "clarke",
            ],
            "https://doi.org/10.1145/876638.876643",
        ),
        pat(
            "termination_via_dependency_pairs",
            "Termination Analysis via Dependency Pairs",
            "pattern",
            "pattern_match",
            "Termination analysis for term-rewriting systems via dependency pairs: extract the recursive-call relation, decompose into strongly-connected components, prove each SCC has no infinite chain.",
            "Decide termination of TRSs that are not obviously well-founded by reducing to a chain-based argument over a smaller dependency-pair relation.",
            "Direct termination orderings (KBO, RPO, LPO) fail on many natural TRSs; dependency pairs decouple termination of the rewriting from termination of the recursive structure.",
            "For each rewrite rule l → r, every defined symbol in r yields a dependency pair (l, r'); termination is equivalent to absence of infinite chains in the dependency-graph SCCs (Arts/Giesl 2000).",
            "Termination decidable for many practical TRSs where direct orderings fail; foundation of AProVE and TTT2; combined with semantic-path-ordering for higher-order termination.",
            &["formal_languages_and_automata"],
            &["termination", "dependency_pairs", "trs", "arts_giesl"],
            "https://doi.org/10.1016/S0304-3975(99)00207-8",
        ),
        pat(
            "confluence_critical_pair_joinability",
            "Confluence via Knuth-Bendix Critical Pairs",
            "pattern",
            "pattern_match",
            "Knuth-Bendix critical-pair analysis: a term-rewriting system is locally confluent iff every critical pair is joinable; combined with termination this gives global confluence.",
            "Decide confluence of TRSs (whether all reduction sequences yield the same normal form) by examining a finite set of critical overlaps between rules.",
            "Direct confluence checking (forall t, ∀ reductions converge) is unbounded; Knuth-Bendix reduces it to a finite critical-pair joinability check.",
            "Enumerate critical pairs (overlaps between left-hand-sides of rewrite rules); check each pair joins to a common reduct; locally-confluent + terminating ⟹ globally confluent (Newman's lemma).",
            "Decides confluence for terminating TRSs; KB completion procedure can add rules to make a TRS confluent if possible; foundation of completion-based proof assistants and the dedukti/lambdapi tradition.",
            &["formal_languages_and_automata"],
            &[
                "confluence",
                "critical_pairs",
                "knuth_bendix",
                "term_rewriting",
                "newmans_lemma",
            ],
            "https://en.wikipedia.org/wiki/Knuth%E2%80%93Bendix_completion_algorithm",
        ),
    ]
}
