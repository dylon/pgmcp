# Formal verification index

Specs and proofs covering pgmcp's safety-critical state machines and
correctness invariants. Modeled after libgrammstein's
`formal/README.md` traceability table.

## Traceability Ledger

| Artifact | Purpose | Verification mode |
| --- | --- | --- |
| `search-tools-traceability.md` | First search-tool verification slice: most-used search/read tools, telemetry project-attribution fix, local regression gates, and inherited contracts from `liblevenshtein-rust/`, `libdictenstein/`, `lling-llang/`, and `libgrammstein/`. | Evidence ledger for `tla/SearchToolScoping.tla`, local regression tests, and dependency proof commands. |
| `inventory-and-concurrency-traceability.md` | Follow-up slice for project-inventory scoping and the ARTrie eviction-thread self-join crash found in libdictenstein. | Evidence ledger for `tla/ProjectInventoryScoping.tla`, libdictenstein `BackgroundWorkerLifecycle.tla`, focused nextest partitions, and ARTrie lifecycle regressions. |
| `work-item-progress-traceability.md` | High-use tracker progress slice for `work_item_record_progress`: append-only agent activity, percent clamping, and trust-boundary provenance. | Evidence ledger for `tla/WorkItemProgressLog.tla` and `work_items_tags_progress_smoke`. |
| `experiment-artifacts-traceability.md` | High-use experiment artifact slice for `experiment_log_artifact`: normalized kind, parser dispatch, metric preservation, and content hashing. | Evidence ledger for `tla/ExperimentArtifactCapture.tla` and `tool_experiments_integration`. |
| `complexity-hotspots-traceability.md` | High-use complexity ranking slice for `complexity_hotspots`: bounded limits, duplicate-name fail-closed behavior, and project-id-scoped ranking rows. | Evidence ledger for `tla/ComplexityHotspotsScoping.tla` and `oracle_complexity_hotspots`. |
| `fuzzy-search-bounds-traceability.md` | High-use fuzzy-search slice for `fuzzy_symbol_search`: bounded edit-distance/result-window requests and project-local trie result soundness, reusing dependency proof corpora. | Evidence ledger for `tla/FuzzySearchBounds.tla`, `tool_fuzzy_search_uses_persistent_trie`, and `src/fuzzy/limits.rs` unit tests. |
| `phonetic-symbol-search-traceability.md` | Phonetic fuzzy slice for `phonetic_symbol_search`: normalized query/project boundary, finite query cap, shared distance/limit clamps, project-local persistent symbol trie use, and inherited phonetic/trie proof contracts. | Evidence ledger for `tla/PhoneticSymbolSearchBoundary.tla` and `phonetic_search_and_correct_query` once sibling dependency compilation is restored. |
| `correct-query-traceability.md` | High-use fuzzy/WFST adapter slice for `correct_query`: normalized bounds, finite LM weights, project-id keyed trie/model artifacts, slug-collision isolation, and read-only execution. | Evidence ledger for `tla/CorrectQueryBoundary.tla`, filtered `phonetic_search_and_correct_query`, and fuzzy/HybridLM artifact-path regressions. |
| `hybrid-search-boundary-traceability.md` | High-use hybrid retrieval addendum for `hybrid_search`: bounded signed limits, finite/nonnegative weights, bounded third-leg edit distance, normalized optional filters, leg degradation, no DB writes, and no retained locks. | Evidence ledger for `tla/HybridSearchBoundary.tla` and pure `tool_hybrid_search` helper tests once sibling dependency compilation is restored. |
| `memory-search-nodes-traceability.md` | High-use memory slice for `memory_search_nodes`: literal substring matching, scope-safe aggregation, exact observation counts, and bounded limits. | Evidence ledger for `tla/MemorySearchNodesScope.tla`, `memory_eval`, and the escaped-pattern unit test. |
| `compare-files-traceability.md` | High-use similarity slice for `compare_files`: duplicate project reference fail-closed behavior and one-to-one chunk alignment over resolved files. | Evidence ledger for `tla/CompareFilesResolution.tla`, `query_smoke_queries`, and `oracle_similarity_tools`. |
| `memory-add-observations-traceability.md` | High-use memory append slice for `memory_add_observations`: ambiguous entity names fail closed, missing/expired entities do not write, and inserts preserve dedupe/provenance. | Evidence ledger for `tla/MemoryAddObservations.tla` and `memory_phase2_3`. |
| `memory-create-entities-traceability.md` | High-use memory write slice for `memory_create_entities`: bounded/normalized requests, invalid no-write behavior, duplicate active identity rejection, deterministic advisory-lock ordering, race-safe active entity identities, and active-observation dedupe. | Evidence ledger for `tla/MemoryCreateEntitiesAtomicity.tla` and `memory_phase2_3`. |
| `memory-create-relations-traceability.md` | Memory relation-write slice for `memory_create_relations`: bounded/normalized requests, ambiguous endpoint rejection, deterministic advisory-lock ordering, race-safe active relation triples, and accurate inserted-vs-resolved reporting. | Evidence ledger for `tla/MemoryCreateRelationsAtomicity.tla` and `memory_phase2_3` once sibling dependency compilation is restored. |
| `memory-hybrid-search-traceability.md` | Memory hybrid retrieval slice for `memory_hybrid_search`: normalized request boundary, 1024d embedding guard, scope/tier filter soundness, duplicate-free fused rows, and no RRF rank inflation from multi-membership rows. | Evidence ledger for `tla/MemoryHybridSearchScope.tla` and `memory_phase3_2` once sibling dependency compilation is restored. |
| `dependency-graph-traceability.md` | High-use graph slice for `dependency_graph`: normalized/validated request modes, duplicate-name fail-closed behavior, exact focus-file resolution, project-id-consistent graph rows, stale-edge rejection, and bounded rendering. | Evidence ledger for `tla/DependencyGraphScope.tla` and `oracle_dependency_graph`. |
| `circular-dependencies-traceability.md` | High-use graph slice for `circular_dependencies`: bounded cycle search, duplicate-name fail-closed behavior, project-id-scoped import cycles, and project telemetry. | Evidence ledger for `tla/CircularDependenciesScope.tla` and `oracle_circular_dependencies`. |
| `design-metrics-traceability.md` | High-use architecture slice for `design_metrics`: bounded limits, project-id scoped file/function/effect rows, literal module/file filters, duplicate-name fail-closed behavior, and project telemetry. | Evidence ledger for `tla/DesignMetricsScope.tla` and `oracle_design_metrics`. |
| `technical-debt-analysis-traceability.md` | High-use prediction slice for `technical_debt_analysis`: bounded limits, duplicate-name fail-closed behavior, project-id-scoped debt rows, TODO inclusion semantics, and project telemetry. | Evidence ledger for `tla/TechnicalDebtAnalysisScope.tla` and `oracle_technical_debt_analysis`. |
| `bug-prediction-traceability.md` | High-use prediction slice for `bug_prediction`: bounded limits, duplicate-name fail-closed behavior, project-id-scoped metric rows, score-kind consistency, bug-prone effect-symbol scoping, and project telemetry. | Evidence ledger for `tla/BugPredictionScope.tla` and `oracle_bug_prediction`. |
| `experiment-measurement-record-traceability.md` | High-use experiment measurement slice for `experiment_record_measurement`: validation, bounded inserts, atomic writes, rollback, and single-lock run upserts. | Evidence ledger for `tla/ExperimentMeasurementRecord.tla` and `tool_experiments_integration`. |
| `test-coverage-gaps-traceability.md` | High-use topic/quality slice for `test_coverage_gaps`: duplicate-name fail-closed behavior, project-id-scoped topic/coverage/effect rows, threshold classification, and project telemetry. | Evidence ledger for `tla/TestCoverageGapsScope.tla` and `oracle_test_coverage_gaps`. |
| `doc-coverage-gaps-traceability.md` | High-use topic/quality slice for `doc_coverage_gaps`: duplicate-name fail-closed behavior, project-id-scoped doc-topic/effect rows, threshold classification, and normalized project output. | Evidence ledger for `tla/DocCoverageGapsScope.tla` and `oracle_doc_coverage_gaps`. |
| `engineering-scorecard-traceability.md` | High-use scorecard slice for `engineering_scorecard` and quality-history snapshots: normalized input, project-id-scoped god-file/graph metrics, SCC import-cycle detection, duplicate-name cron snapshot safety, and bounded skipped-finding mode. | Evidence ledger for `tla/EngineeringScorecardScope.tla`, `oracle_engineering_scorecard`, and `quality_history_cron_registered`. |
| `quality-report-traceability.md` | Reporting/aggregation slice for `quality_report`: local validation before work, single resolved project identity for aggregate/history, bounded refresh/trend controls, and canonical envelope formats. | Evidence ledger for `tla/QualityReportBoundary.tla` and `quality_report_e2e` once sibling dependency compilation is restored. |
| `rename-oracle-traceability.md` | Fuzzy adapter slice for `rename_oracle`: bounded normalized caller-supplied symbol names, deterministic candidate dedupe, empty-set no-build behavior, and streaming best-candidate selection over inherited fuzzy engines. | Evidence ledger for `tla/RenameOracleBounds.tla` and `oracle_rename_oracle` once sibling dependency compilation is restored. |
| `secret-detection-traceability.md` | Security scan slice for `secret_detection`: finite/clamped thresholds, bounded findings, streaming content scan, explicit stream drop before enrichment, normalized response params, and read-only execution. | Evidence ledger for `tla/SecretDetectionScan.tla` and `tool_sota_phase6` once sibling dependency compilation is restored. |
| `security-audit-tools-traceability.md` | Security-audit slice for `taint_analysis`, `injection_candidates`, `crypto_misuse`, and `unsafe_deserialization`: strict project/kind validation, predicate-before-cap filtering, bounded streaming scans, bounded effect enrichment, read-only execution, and no retained locks. | Evidence ledger for `tla/SecurityAuditBoundary.tla` and `tool_sota_phase6` once sibling dependency compilation is restored. |
| `semver-break-audit-traceability.md` | API/fuzzy slice for `semver_break_audit`: bounded commit windows/output, public-symbol cap, current `git_commit_chunks.content` schema, project-scoped scans, and streaming rename selection. | Evidence ledger for `tla/SemverBreakAuditBounds.tla` and `tool_sota_phase7_to_11` once sibling dependency compilation is restored. |
| `substring-search-traceability.md` | In-memory exact-search slice for `substring_search`: bounded caller haystacks, deterministic dedupe, empty-haystack no-build behavior, exact membership preservation, and inherited suffix-automaton correctness. | Evidence ledger for `tla/SubstringSearchBounds.tla` and `oracle_substring_search` once sibling dependency compilation is restored. |
| `find-misplaced-code-traceability.md` | High-use topic/quality slice for `find_misplaced_code`: normalized/bounded mismatch threshold, duplicate-name fail-closed behavior, project-id-scoped topic rows, same-project effects, and read-only execution. | Evidence ledger for `tla/FindMisplacedCodeScope.tla` and `oracle_find_misplaced_code`. |
| `public-api-surface-traceability.md` | High-use API-contract slice for `public_api_surface`: normalized input, strict format validation, bounded full-list limit, uncapped summary counts, project-id symbol scoping, and read-only execution. | Evidence ledger for `tla/PublicApiSurfaceScope.tla` and `oracle_public_api_surface`. |
| `architecture-quality-traceability.md` | High-use architecture slice for `architecture_quality`: normalized detail mode, duplicate-name fail-closed behavior, project-id-consistent metric rows, stale-edge rejection, and N/A mean semantics. | Evidence ledger for `tla/ArchitectureQualityScope.tla` and `oracle_architecture_quality`. |
| `architecture-violations-traceability.md` | High-use architecture slice for `architecture_violations`: normalized severity mode, duplicate-name fail-closed behavior, project-id-scoped import graph construction, stale-edge rejection, and bounded reports. | Evidence ledger for `tla/ArchitectureViolationsScope.tla` and `oracle_architecture_violations`. |
| `design-smell-detection-traceability.md` | High-use architecture slice for `design_smell_detection`: normalized smell filters, bounded limits, duplicate-name fail-closed behavior, project-id scoped file/topic/co-change/effect rows, and stale metric rejection. | Evidence ledger for `tla/DesignSmellDetectionScope.tla` and `oracle_design_smell_detection`. |
| `coupling-cohesion-report-traceability.md` | High-use architecture slice for `coupling_cohesion_report`: normalized sort mode, bounded module depth/output, duplicate-name fail-closed behavior, project-id-scoped import graph construction, and stale-edge rejection. | Evidence ledger for `tla/CouplingCohesionScope.tla` and `oracle_coupling_cohesion_report`. |
| `find-orphans-traceability.md` | High-use topic/quality slice for `find_orphans`: validated detail mode, bounded limits, duplicate-name fail-closed behavior, project-id-scoped orphan rows, language filtering, and same-project enrichment. | Evidence ledger for `tla/FindOrphansScope.tla` and `oracle_find_orphans`. |
| `hot-path-audit-traceability.md` | High-use workflow slice for `hot_path_audit`: finite percentile thresholds, bounded limits, duplicate-name fail-closed behavior, project-id-consistent metric rows, and same-project effect-symbol enrichment. | Evidence ledger for `tla/HotPathAuditScope.tla` and `oracle_hot_path_audit`. |
| `memory-semantic-search-traceability.md` | High-use memory retrieval slice for `memory_semantic_search`: blank-query/tier validation, 1024d vector guard, bounded limits, scope/tier filtering, and duplicate-free observations. | Evidence ledger for `tla/MemorySemanticSearchScope.tla` and `memory_phase3_2`. |
| `memory-raptor-search-traceability.md` | High-use memory retrieval slice for `memory_raptor_search`: blank-query/scope/level validation, 1024d vector guard, bounded k/ef_search, read-only execution, and per-level top-k RAPTOR retrieval. | Evidence ledger for `tla/MemoryRaptorSearchScope.tla` and `memory_phase6_7`. |
| `memory-unified-search-traceability.md` | High-use memory-graph retrieval slice for `memory_unified_search`: pre-embed query/filter validation, closed-vocabulary node-type normalization, 1024d vector guard, bounded k/ef_search, filter-sound rows, and read-only execution. | Evidence ledger for `tla/MemoryUnifiedSearchBoundary.tla` and `memory_phase6_7`. |
| `session-mandate-promotion-traceability.md` | Session-mandate read/promotion slice for `session_mandates` and `promote_session_mandate`: status/cwd scoping, bounded reads, input validation, idempotent durable promotion, and non-blocking file append locks. | Evidence ledger for `tla/SessionMandatePromotion.tla` and `oracle_session_mandates`. |
| `ontology-create-concept-traceability.md` | High-use ontology authoring slice for `ontology_create_concept`: bounded normalized names, facet validation, per-name advisory-lock serialization, candidate-only agent creates, canonical metadata preservation, and actual persisted metadata in responses. | Evidence ledger for `tla/OntologyCreateConceptAtomicity.tla` and `oracle_ontology_tools`. |
| `ontology-tree-traceability.md` | Ontology hierarchy read slice for `ontology_tree`: blank-root/facet validation, bounded recursive traversal, active concept filtering, corrupt-cycle suppression, duplicate-free edges, and read-only execution. | Evidence ledger for `tla/OntologyTreeScope.tla`, `oracle_ontology_trie_accel`, and `oracle_ontology_tools` once sibling dependency compilation is restored. |
| `find-coupled-files-traceability.md` | High-use co-change slice for `find_coupled_files`: normalized/bounded request parameters, strict project-id scoping, duplicate-name fail-closed behavior, bulk-commit exclusion, and read-only execution. | Evidence ledger for `tla/FindCoupledFilesScope.tla` and `oracle_find_coupled_files`. |
| `send-sync-violations-traceability.md` | High-use concurrency-safety slice for `send_sync_violations`: strict project resolution, bounded limits, streaming regex scans, Rust/project scoped matches, unsafe-symbol enrichment scoping, and read-only/no-lock execution. | Evidence ledger for `tla/SendSyncViolationsScan.tla` and filtered `tool_sota_phase5` tests. |
| `concurrency-audit-tools-traceability.md` | Concurrency-audit slice for `lockset_races` and `blocking_in_async`: strict project resolution, finite scan bounds, streaming file scans, project-scoped effect enrichment, read-only execution, and no retained locks. | Evidence ledger for `tla/ConcurrencyAuditScopes.tla` and filtered `tool_sota_phase5` tests once sibling dependency compilation is restored. |
| `work-item-set-status-traceability.md` | High-use tracker lifecycle slice for `work_item_set_status`: input normalization, agent trust boundary, row-lock transition recheck, atomic status/history writes, and concurrent transition serialization. | Evidence ledger for `tla/WorkItemSetStatusAtomicity.tla` and `work_items_smoke`. |
| `work-item-create-traceability.md` | High-use tracker creation slice for `work_item_create`: normalized input, bounded priority/weight, bug-only metadata, strict project resolution, parent locking, and atomic item/bug-sidecar writes. | Evidence ledger for `tla/WorkItemCreateAtomicity.tla` and filtered `work_items_smoke` tests. |
| `work-item-add-criterion-traceability.md` | Tracker trust-boundary slice for `work_item_add_criterion`: closed-vocabulary criterion validation, bounded text inputs, no-write rejection, and no evidence/status mutation. | Evidence ledger for `tla/WorkItemAddCriterionBoundary.tla` and filtered `work_items_smoke` once sibling dependency compilation is restored. |
| `work-item-record-evidence-traceability.md` | Tracker trust-boundary slice for `work_item_record_evidence`: bounded manual-evidence inputs, post-insert evidence stats, no trusted-source evidence, and no status mutation. | Evidence ledger for `tla/WorkItemRecordEvidenceBoundary.tla` and filtered `work_items_smoke` once sibling dependency compilation is restored. |
| `work-item-attempt-verify-traceability.md` | Tracker gatekeeper slice for `work_item_attempt_verify`: manual evidence rejection, trusted-evidence requirement, status preservation on failure, and post-transition verification stats. | Evidence ledger for `tla/WorkItemAttemptVerifyGate.tla` and filtered `work_items_smoke` once sibling dependency compilation is restored. |
| `work-item-defer-reinstate-traceability.md` | User-authority tracker slice for `work_item_defer` and `work_item_reinstate`: token-before-transaction behavior, atomic negotiation/status writes, rollback on transition failure, and post-commit status stats. | Evidence ledger for `tla/WorkItemDeferReinstateAtomicity.tla` and filtered `work_items_smoke` once sibling dependency compilation is restored. |
| `work-item-link-experiment-traceability.md` | Tracker/experiment bridge slice for `work_item_link_experiment`: bounded bridge inputs, all-or-nothing auto-create/bridge/criterion writes, post-commit create stats, and same-item criterion seed serialization. | Evidence ledger for `tla/WorkItemLinkExperimentAtomicity.tla` and filtered `work_items_smoke` once sibling dependency compilation is restored. |
| `work-item-crud-read-update-traceability.md` | High-use tracker read/update slice for `work_item_get`, `work_item_list`, and `work_item_update`: public-id normalization, fail-closed list filters, bounded list/update values, bug-only metadata, and atomic row/sidecar updates. | Evidence ledger for `tla/WorkItemCrudReadUpdate.tla` and filtered work-item smoke tests. |
| `centrality-analysis-traceability.md` | High-use graph slice for `centrality_analysis`: duplicate-name fail-closed behavior, metric validation, bounded limits, project-id-scoped metric rows, and project-id-consistent enrichment. | Evidence ledger for `tla/CentralityAnalysisScope.tla` and `oracle_centrality_analysis`. |
| `index-stats-traceability.md` | High-use inventory slice for `index_stats`: stable JSON envelope, live snapshot preservation, exact DB count reporting, localized DB-count failure, and optional effect enrichment. | Evidence ledger for `tla/IndexStatsEnvelope.tla` and filtered `mcp_tool_smoke` tests. |
| `mcp-tool-telemetry-traceability.md` | High-use inventory slice for `mcp_tool_telemetry`: normalized filters, bounded lookback/raw limits, aggregation validation, project-filter soundness across aggregations, and normalized telemetry attribution. | Evidence ledger for `tla/McpToolTelemetryFilters.tla` and filtered `query_smoke_mcp_tools` tests. |
| `unsafe-clusters-traceability.md` | High-use quality slice for `unsafe_clusters`: unique project resolution, bounded/ranked Rust regex rows, scoped unsafe effect symbols, and typed collector project/file consistency. | Evidence ledger for `tla/UnsafeClustersScope.tla` and filtered `tool_sota_phase5` tests. |
| `panic-paths-traceability.md` | Safety/concurrency-adjacent slice for `panic_paths`: closed entry filters, bounded limits, stale metric rejection across metric/symbol/file joins, scoped `may_panic` effects, and read-only execution. | Evidence ledger for `tla/PanicPathsScope.tla` and filtered `tool_sota_phase5` tests. |
| `ontology-invariants-file-traceability.md` | Read-side ontology slice for `ontology_invariants_for_file`: literal file-path matching, ambiguous-path fail-closed behavior, missing-file empty responses, resolved-file invariant scoping, and read-only execution. | Evidence ledger for `tla/OntologyInvariantsFileScope.tla` and filtered `oracle_ontology_tools` tests. |
| `adoption-report-traceability.md` | High-use adoption telemetry slice for `adoption_report`: normalized output format, bounded lookback window, real-client allowlist, old-row exclusion, family classification, distinct sessions, and RLM subset accounting. | Evidence ledger for `tla/AdoptionReportTelemetry.tla` and filtered `query_smoke_mcp_tools` tests. |
| `prediction-reporting-traceability.md` | High-use prediction/reporting slice for `code_on_fire` and `documented_tech_debt`: normalized filters, bounded limits, project-id scoping, stale metric rejection, and documented-debt finding filters. | Evidence ledger for `tla/PredictionReportingScope.tla` and filtered prediction/reporting integration tests. |
| `trigger-cron-serialization-traceability.md` | High-use operational slice for `trigger_cron`: normalized dispatch, shared heavy-cron lock, non-blocking busy response, no queueing, and project forwarding for scoped cron jobs. | Evidence ledger for `tla/TriggerCronSerialization.tla` and filtered trigger-cron tests. |
| `code-summarize-traceability.md` | High-use summarization slice for `code_summarize`: unique project resolution, validated scope/detail/path, shared path scoping across file channels, metric project consistency, topic/effect scoping, and brief-detail behavior. | Evidence ledger for `tla/CodeSummarizeScope.tla` and filtered code-summarize tests. |
| `work-item-search-ingest-bulk-traceability.md` | High-use tracker slice for `work_item_search`, `work_item_ingest_plan`, and `work_item_bulk`: strict project scoping, 1024d search guard, bounded atomic plan ingestion, explicit-target normalization, and agent-only bulk status transitions. | Evidence ledger for `tla/WorkItemSearchIngestBulk.tla` and filtered work-item smoke tests. |
| `experiment-open-get-traceability.md` | High-use experiment lifecycle slice for `experiment_open` and `experiment_get`: normalized open/get inputs, enum/project validation, atomic experiment+hypothesis creation, and non-partial get results. | Evidence ledger for `tla/ExperimentOpenGetAtomicity.tla` and `tool_experiments_integration`. |
| `experiment-decide-traceability.md` | High-use experiment decision slice for `experiment_decide`: normalized/bounded decision input, invalid no-write behavior, same-arm rejection, and atomic result/verdict/status publication. | Evidence ledger for `tla/ExperimentDecideAtomicity.tla` and `tool_experiments_integration`. |
| `fuzzy-grep-adapter-traceability.md` | Fuzzy-search adapter slice for `fuzzy_grep`: bounded caller-supplied haystacks, non-wrapping distance clamps, explicit token-budget validation, finite reported matches, and read-only execution. | Evidence ledger for `tla/FuzzyGrepAdapterBounds.tla` and `g10_fuzzy_grep_tools`. |
| `find-duplicates-traceability.md` | Similarity slice for `find_duplicates`: finite request bounds, overflow-safe fetch windows, bounded cluster output, filtered cross-language clone rows, and read-only execution. | Evidence ledger for `tla/FindDuplicatesBounds.tla` and `oracle_similarity_tools`. |
| `refactoring-report-traceability.md` | Similarity/reporting slice for `refactoring_report`: finite similarity validation, bounded project/language/output filters, overflow-safe fetch windows, bounded candidate output, and read-only execution. | Evidence ledger for `tla/RefactoringReportBounds.tla` and filtered `mcp_tool_smoke` tests. |
| `io-hotpath-traceability.md` | Concurrency/performance slice for `io_hotpath`: unique project resolution, finite scan/output bounds, stale metric rejection, scoped effect-symbol hints, and read-only execution. | Evidence ledger for `tla/IoHotpathScope.tla` and filtered `tool_sota_phase5` tests. |
| `api-stability-traceability.md` | API-contract slice for `api_stability`: finite commit-window/output bounds, migrated commit-chunk column usage, resolved-project scoping, same-project effect enrichment, and read-only execution. | Evidence ledger for `tla/ApiStabilityScope.tla` and filtered `tool_sota_phase7_to_11` tests. |
| `community-detection-traceability.md` | Graph-analysis slice for `community_detection`: duplicate-name fail-closed behavior, closed graph-type validation, bounded finite resolution, stale cross-project edge exclusion, numeric community envelope, and same-project effect enrichment. | Evidence ledger for `tla/CommunityDetectionScope.tla` and `oracle_community_detection`. |
| `dead-code-reachability-traceability.md` | Call-graph reachability slice for `dead_code_reachability`: normalized bounds, test-root opt-in, exact/bare-name edge policy, same-project source/target symbol traversal, bounded BFS, and read-only execution. | Evidence ledger for `tla/DeadCodeReachabilityScope.tla` and `oracle_dead_code_reachability`. |
| `experiment-search-traceability.md` | Experiment retrieval slice for `experiment_search`: closed query/filter normalization, bounded limits, vector/FTS fallback parity, active-verdict filtering, scoped rows, and read-only execution. | Evidence ledger for `tla/ExperimentSearchScope.tla` and `oracle_experiment_search`. |
| `memory-open-nodes-traceability.md` | Memory read slice for `memory_open_nodes`: bounded exact-name normalization, active entity/observation reads, active relation endpoint filtering, deterministic output, and read-only execution. | Evidence ledger for `tla/MemoryOpenNodesScope.tla` and `oracle_memory_open_nodes`. |
| `reindex-serialization-traceability.md` | Operational reindex slice for destructive-row clearing: language-token validation, non-blocking serialization, scoped language deletion, cancellation ordering, and bounded full-delete batches. | Evidence ledger for `tla/ReindexSerializationScope.tla`, `oracle_reindex`, and `reindex_serialization`. |
| `work-item-claim-traceability.md` | Tracker collaboration slice for `work_item_claim`: agent-id validation, row-CAS ownership, blocked/terminal no-write behavior, lease bounds, claim ledger/presence atomicity, and two-agent race safety. | Evidence ledger for `tla/WorkItemClaimAtomicity.tla`, `oracle_work_item_claim`, and filtered `work_items_smoke`. |
| `work-item-tree-traceability.md` | Tracker read slice for `work_item_tree`: public-id validation, bounded recursive traversal, cycle suppression, deterministic ordering, duplicate-free rows, read-only behavior, and query-stat accounting. | Evidence ledger for `tla/WorkItemTreeScope.tla`, `oracle_work_item_tree`, and filtered `work_items_smoke`. |
| `deadlock-candidates-traceability.md` | Legacy concurrency slice for `deadlock_candidates`: resolved-project file scans, scoped lock-order edges/cycles, scoped mutex/effect enrichment, and read-only execution. | Evidence ledger for `tla/DeadlockCandidatesScope.tla`, `oracle_deadlock_candidates`, and filtered `tool_sota_phase5`. |
| `deadlock-analysis-traceability.md` | Concurrency/deadlock slice for `deadlock_cycles`, `lock_order_graph`, and `channel_deadlock`: finite confidence handling, bounded outputs, resolved-project sync skeleton rows, operational lock/channel proofs, read-only execution, and no retained locks. | Evidence ledger for `tla/DeadlockAnalysisBoundary.tla`, `tla/LockOrderDeadlock.tla`, `tla/ChannelDeadlock.tla`, Rocq deadlock proofs, and filtered `tool_concurrency_deadlock` once sibling dependency compilation is restored. |
| `change-impact-analysis-traceability.md` | Graph/change-analysis slice for `change_impact_analysis`: unique project resolution, normalized file/depth input, same-project file-level channels, intentional cross-project dependents, and read-only execution. | Evidence ledger for `tla/ChangeImpactScope.tla`, `oracle_change_impact_analysis`, and filtered `tool_graph_integration`. |
| `doc-code-drift-traceability.md` | Doc/code embedding-drift slice for `doc_code_drift`: normalized project/threshold/limit input, SQL-bounded rows, resolved-project drift/effect channels, and read-only execution. | Evidence ledger for `tla/DocCodeDriftScope.tla`, `oracle_doc_code_drift`, and filtered `tool_sota_phase4`. |
| `experiment-list-traceability.md` | Experiment read/list slice for `experiment_list`: enum filter validation, bounded pagination, project scoping, newest-first ordering, and read-only execution. | Evidence ledger for `tla/ExperimentListScope.tla`, `oracle_experiment_list`, and filtered `tool_experiments_integration`. |
| `experiment-render-ledger-traceability.md` | Experiment ledger render/write slice for `experiment_render_ledger`: lookup validation, safe relative ledger paths, safe filenames, dry-run/write separation, atomic publish, and no DB mutation. | Evidence ledger for `tla/ExperimentRenderLedgerScope.tla` and `oracle_experiment_render_ledger` once sibling dependency compilation is restored. |
| `coordination-tools-traceability.md` | Coordination/client/A2A request-boundary slice for `coordinate_dependency_block`, `coordination_respond`, `suggest_worktree`, `project_dependents`, `project_dependencies`, `active_clients`, `client_project_matrix`, `a2a_send_message`, and `a2a_inbox`: fail-closed project resolution (blank/unknown/duplicate), closed `coordination_respond` / `MessageKind` vocabularies, the agent/`resolved` trust boundary, live-edge direction scoping, optional client-filter trim + window clamps, and at-most-once mailbox delivery. | Evidence ledger for `tla/{CoordinationScope,ProjectDepsScope,ClientTrackingScope,A2aMailboxBoundary}.tla`, `tla/WorktreeNegotiation.tla`, and the `tool_coordination` / `tool_active_clients` / `tool_a2a_mailbox` / `cross_project_graph_fields` / forthcoming `oracle_coordination` regressions. |
| `WorktreeNegotiation` (protocol model) | Phase-4 worktree-coordination protocol trust boundary: the dependent is unblocked only after the System git scanner observes the dependency stable (`GatekeeperSafety`), never on the Editor agent's `moved` claim alone (`NoUnblockOnClaimAlone`), and an accepted request eventually unblocks (`EventuallyUnblocked`). | TLC + TLAPS + Rocq proof for `tla/WorktreeNegotiation.tla`, `WorktreeNegotiation_proofs.tla`, and `rocq/WorktreeNegotiation.v`; referenced by `coordination-tools-traceability.md`. |

## TLA+ specs

| Spec                                          | Purpose                                                                                 | Code locus                                                  |
|-----------------------------------------------|-----------------------------------------------------------------------------------------|-------------------------------------------------------------|
| `tla/CronStateMachine.tla`                    | Cron task lifecycle: pending → running → completed; `DisjointSets` (every task in exactly one set); `NoDoubleProcessing`; crash-recovery `RecoverInProgressAsFailed`. | `src/cron/scheduler.rs` heavy-cron skip-gate; `src/stats/tracker.rs::CronJobOutcome` |
| `tla/SimilarityScanFkDrift.tla`               | Long-running cron's tolerance to file_chunks deletion mid-pass. Cached chunk_ids that become orphans must not produce FK violations on bulk INSERT. Models `ON DELETE CASCADE` of `similarities` rows when a chunk is deleted. | `src/cron/similarity.rs::run_similarity_scan`; pattern documented in `feedback_long_running_jobs_must_handle_fk_drift.md` |
| `tla/CfsmNetwork.tla`                          | ADR-009 Deliberation CFSM network (O/R/T): synchronous product of the projected machines — sender-driven choice + bounded loop + a bystander. `DeadlockFreedom`, `NoOrphan`, `RoundsBounded`, `EventualTermination`. | `src/csm/machine.rs`, `src/csm/transition.rs`, `src/csm/examples.rs` |
| `tla/A2aLinearPipeline.tla`                    | ADR-009 linear patterns — Sequential / Mixture / Distillation / Recursive via per-pattern `.cfg` (`NStages`). Bounded request/response pipeline; `DeadlockFreedom`, `EventualTermination`. | `src/csm/registry.rs` |
| `tla/RmasRecursionLoop.tla`                    | ADR-009 Track B latent-decode discipline: `LatentNeverDecodedMidLoop` — text is produced only at the final round's last agent. | `src/csm/role.rs::MessageMedium` |
| `tla/SearchToolScoping.tla`                    | Search/read/commit-result scoping: optional project filters only remove rows; file tools never return commit rows; commit search never returns file rows; `read_file` returns only the requested path; telemetry cannot invent a project without an explicit request project. | `src/db/queries/search.rs`, `src/mcp/tools/tool_{text_search,hybrid_search,read_file,search_commits}.rs`, `src/mcp/server.rs` |
| `tla/ProjectInventoryScoping.tla`              | Project inventory identity/scoping: `list_projects` preserves distinct identities, duplicate display names fail closed for `mandate_context` and other name-scoped tools, exact-path `file_info`, boundary-safe `find_project_by_cwd`, and no cross-project path leakage. | `src/db/queries/{files,projects}.rs`, `src/mcp/tools/tool_{list_projects,mandate_context,orient,file_info,project_tree}.rs` |
| `tla/AtomicFileReplacement.tla`                | Indexer file/chunk replacement atomicity: transaction-local NULL hash and partial chunk replacement are never visible; aborts roll back to the old complete state; commits publish the complete replacement. | `src/db/queries/chunks.rs::replace_indexed_file`, `src/embed/pool.rs` |
| `tla/WorkItemProgressLog.tla`                  | Work-item progress trust boundary: MCP progress notes are append-only agent writes, empty notes are rejected, percent values are clamped, and self-reported progress never verifies work. | `src/mcp/tools/work_items/progress.rs`, `src/db/queries/work_items.rs::insert_progress` |
| `tla/ExperimentArtifactCapture.tla`            | Experiment artifact capture boundary: artifact kinds are normalized before parser dispatch/storage; recognized parser output replaces metrics; unparsed metrics are preserved; content hashing tracks content presence. | `src/mcp/tools/tool_experiments.rs::tool_experiment_log_artifact`, `src/db/queries/experiments.rs::insert_experiment_artifact` |
| `tla/ComplexityHotspotsScoping.tla`            | Complexity hotspot request boundary: ambiguous project names fail closed, accepted rows are scoped to a resolved project id, and caller limits are clamped before truncation. | `src/mcp/tools/tool_complexity_hotspots.rs`, `src/db/queries/metrics.rs::get_file_complexity_data_by_project_id` |
| `tla/FuzzySearchBounds.tla`                     | Fuzzy-search request boundary: symbol/path/phonetic-symbol requests clamp edit distance and result windows before querying project-local persistent tries. | `src/fuzzy/limits.rs`, `src/mcp/tools/tool_{fuzzy_symbol_search,fuzzy_path_search,phonetic_symbol_search}.rs` |
| `tla/PhoneticSymbolSearchBoundary.tla`          | Phonetic-symbol request boundary: trimmed query/project validation, finite query cap, shared distance/limit clamps, project-local vocabulary soundness, and exact normalized-distance mode. | `src/mcp/tools/tool_phonetic_symbol_search.rs`, `src/fuzzy/sync.rs::open_symbol_trie` |
| `tla/CorrectQueryBoundary.tla`                  | Correct-query adapter boundary: invalid requests fail closed, edit/LM parameters are bounded, trie and HybridLM artifacts use the resolved project-id key, and slug-colliding project names remain isolated. | `src/mcp/tools/tool_correct_query.rs`, `src/fuzzy/sync.rs`, `src/cron/{fuzzy_sync,ngram_lm_train}.rs` |
| `tla/HybridSearchBoundary.tla`                  | Hybrid-search boundary: signed limits clamp before fetch/truncate, non-finite weights reject before legs, negative weights skip legs, third-leg edit distance is bounded, leg failures degrade, and the direct path performs no DB writes or retained locks. | `src/mcp/tools/tool_hybrid_search.rs`, `src/fuzzy/limits.rs` |
| `tla/MemorySearchNodesScope.tla`                | Memory substring-search SQL boundary: LIKE metacharacters are literal, scope membership does not duplicate observations, active rows are scoped, and limits are bounded. | `src/db/queries/memory_search.rs::memory_search_nodes`, `src/mcp/tools/tool_memory_crud.rs::tool_memory_search_nodes` |
| `tla/CompareFilesResolution.tla`                | Compare-files reference/alignment boundary: duplicate project-qualified references fail closed and successful alignments contain only one-to-one chunk pairs from resolved files. | `src/db/queries/files.rs::resolve_file_reference`, `src/mcp/tools/tool_compare_files.rs` |
| `tla/MemoryAddObservations.tla`                 | Memory observation append boundary: unique active entity-name resolution, duplicate-name rejection, missing/expired no-op behavior, per-entity dedupe, and agent-write provenance. | `src/db/queries/memory_crud.rs::memory_add_observations`, `src/mcp/tools/tool_memory_crud.rs::tool_memory_add_observations` |
| `tla/MemoryCreateEntitiesAtomicity.tla`         | Memory entity-create boundary: invalid no-write behavior, deterministic identity/observation advisory-lock ordering, duplicate active entity prevention/rejection, and per-entity observation dedupe. | `src/db/queries/memory_crud.rs::memory_create_entities_detailed`, `src/mcp/tools/tool_memory_crud.rs::tool_memory_create_entities` |
| `tla/MemoryCreateRelationsAtomicity.tla`        | Memory relation-create boundary: normalized relation fields, ambiguous endpoint rejection, deterministic relation advisory-lock ordering, duplicate active relation prevention/rejection, and actual insert accounting. | `src/db/queries/memory_crud.rs::memory_create_relations_detailed`, `src/mcp/tools/tool_memory_crud.rs::tool_memory_create_relations` |
| `tla/MemoryHybridSearchScope.tla`               | Memory hybrid-search boundary: normalized query/tier/limit handling, 1024d vector guard, scope/tier soundness across dense+sparse legs, duplicate-free fused rows, and no membership-driven RRF inflation. | `src/mcp/tools/tool_memory_ext.rs::tool_memory_hybrid_search`, `src/db/queries/memory_search.rs::memory_hybrid_search` |
| `tla/DependencyGraphScope.tla`                  | Dependency-graph request boundary: format/edge-type/focus validation, duplicate-name fail-closed behavior, project-id-consistent edge rows, stale cross-project edge rejection, and bounded rendering. | `src/mcp/tools/tool_dependency_graph.rs` |
| `tla/CircularDependenciesScope.tla`              | Circular-dependencies request boundary: non-unique project names fail closed, max cycle lengths are clamped, and reported cycles are closed import cycles inside the resolved project. | `src/mcp/tools/tool_circular_dependencies.rs`, `src/mcp/server/handlers/graph_core.rs` |
| `tla/DesignMetricsScope.tla`                     | Design-metrics request boundary: non-unique project names and invalid scopes fail closed, row queries and enrichment use the same resolved project id, path filters are literal, and result limits are clamped. | `src/mcp/tools/tool_design_metrics.rs`, `src/mcp/server/handlers/architecture.rs` |
| `tla/TechnicalDebtAnalysisScope.tla`             | Technical-debt request boundary: non-unique project names fail closed, debt rows are scoped to the resolved project id, TODO inclusion controls marker counts, enrichment uses the same project id, and result limits are clamped. | `src/mcp/tools/tool_technical_debt_analysis.rs`, `src/mcp/server/handlers/prediction.rs` |
| `tla/BugPredictionScope.tla`                      | Bug-prediction request boundary: non-unique project names fail closed, metric rows and bug-prone effect symbols are scoped to the resolved project id, score kind matches training data, and result limits are clamped. | `src/mcp/tools/tool_bug_prediction.rs`, `src/mcp/server/handlers/prediction.rs` |
| `tla/ExperimentMeasurementRecord.tla`             | Experiment measurement boundary: invalid requests do not write, DB failures roll back, run/sample/status writes are atomic, text fields are normalized, warm-ups do not count toward conformance, and the transaction holds one advisory lock. | `src/mcp/tools/tool_experiments.rs::tool_experiment_record_measurement`, `src/db/queries/experiments.rs::record_experiment_measurement` |
| `tla/TestCoverageGapsScope.tla`                    | Test-coverage request boundary: blank/duplicate projects fail closed, topic rows, real coverage, and effect enrichment use the same resolved project id, and status labels match threshold classification. | `src/mcp/tools/tool_test_coverage_gaps.rs`, `src/db/queries/topics.rs::get_test_topic_coverage_by_project_id` |
| `tla/DocCoverageGapsScope.tla`                     | Documentation-coverage request boundary: blank/duplicate projects fail closed, topic rows and effect enrichment use the same resolved project id, unknown projects return no scoped rows, and status labels match threshold classification. | `src/mcp/tools/tool_doc_coverage_gaps.rs`, `src/db/queries/topics.rs::get_doc_topic_coverage_by_project_id` |
| `tla/EngineeringScorecardScope.tla`                | Engineering scorecard boundary: normalized input, duplicate-name fail-closed behavior, project-id-scoped SCC cycle/god-file metrics, and id-based quality-history snapshots. | `src/mcp/tools/tool_engineering_scorecard.rs`, `src/mcp/tools/sota_helpers.rs::import_cycle_file_count`, `src/cron/quality_history.rs` |
| `tla/FindMisplacedCodeScope.tla`                   | Misplaced-code request boundary: normalized input, bounded mismatch thresholds, duplicate-name fail-closed behavior, project-id-scoped file-topic rows, and same-project effect enrichment. | `src/mcp/tools/tool_find_misplaced_code.rs`, `src/db/queries/topics.rs::load_chunk_topic_assignments_for_files_by_project_id` |
| `tla/PublicApiSurfaceScope.tla`                    | Public API surface boundary: strict format validation, bounded full-row limits, summary counts independent of the full limit, language filtering, and read-only project-id scoping. | `src/mcp/tools/tool_public_api_surface.rs` |
| `tla/ArchitectureQualityScope.tla`                 | Architecture-quality request boundary: detail validation, duplicate-name fail-closed behavior, metric/file project-id agreement, stale cross-project edge exclusion, and N/A dimensions excluded from the mean. | `src/mcp/tools/tool_architecture_quality.rs`, `src/mcp/tools/sota_helpers.rs::import_cycle_file_count` |
| `tla/ArchitectureViolationsScope.tla`              | Architecture-violations request boundary: severity validation, duplicate-name fail-closed behavior, project-id-scoped import edges, stale cross-project edge exclusion, bounded reports, and same-project enrichment. | `src/mcp/tools/tool_architecture_violations.rs` |
| `tla/DesignSmellDetectionScope.tla`                | Design-smell request boundary: smell validation, bounded limits, duplicate-name fail-closed behavior, project-id-scoped metrics/co-change rows, stale metric rejection, and requested-smell filtering. | `src/mcp/tools/tool_design_smell_detection.rs` |
| `tla/CouplingCohesionScope.tla`                    | Coupling/cohesion request boundary: sort validation, module-depth clamping, duplicate-name fail-closed behavior, project-id-scoped import edges, stale cross-project edge exclusion, bounded module output, and same-project enrichment. | `src/mcp/tools/tool_coupling_cohesion_report.rs` |
| `tla/FindOrphansScope.tla`                         | Find-orphans request boundary: detail validation, bounded limits, blank supplied project rejection, duplicate-name fail-closed behavior, project-id scoped orphan rows, language filtering, and same-project enrichment. | `src/mcp/tools/tool_find_orphans.rs`, `src/db/queries/topics.rs::{find_orphan_chunks_by_project_id,find_orphan_file_summary_by_project_id}` |
| `tla/HotPathAuditScope.tla`                        | Hot-path request boundary: finite threshold validation, bounded thresholds/limits, duplicate-name fail-closed behavior, metric/file project-id agreement, stale metric rejection, and same-project enrichment. | `src/mcp/tools/tool_hot_path_audit.rs`, `src/db/queries/metrics.rs::find_hot_paths_by_project_id` |
| `tla/MemorySemanticSearchScope.tla`                | Memory semantic-search boundary: blank queries and invalid tiers fail closed, embeddings must be 1024d, limits are clamped, rows match scope/tier filters, and observation ids are unique. | `src/mcp/tools/tool_memory_ext.rs::tool_memory_semantic_search`, `src/db/queries/memory_search.rs::memory_semantic_search` |
| `tla/MemoryRaptorSearchScope.tla`                  | Memory RAPTOR request boundary: blank/scope/level filters fail closed, query embeddings are 1024d, k/ef_search are bounded, results come only from requested levels, and each requested level gets its own top-k cap. | `src/mcp/tools/tool_memory_graph_rag.rs::tool_memory_raptor_search`, `src/db/queries/memory_search.rs::memory_raptor_search` |
| `tla/MemoryUnifiedSearchBoundary.tla`              | Memory unified-search boundary: pre-embed query/filter validation, 1024d vector guard, bounded k/ef_search, closed node-type filters, filter-sound rows, and read-only execution. | `src/mcp/tools/tool_memory_graph_rag.rs::tool_memory_unified_search`, `src/db/queries/memory_search.rs::memory_unified_search` |
| `tla/SessionMandatePromotion.tla`                  | Session-mandate boundary: status filters remain scoped by session/cwd, invalid promotion inputs do not write, repeated promotions are durable-row idempotent, and target-file appends use non-blocking locks. | `src/mcp/tools/tool_session_mandates.rs`, `src/sessions.rs::promote_mandate` |
| `tla/OntologyCreateConceptAtomicity.tla`           | Ontology create boundary: invalid names/facets fail closed, same-name concurrent creates serialize through one advisory lock, agent creates stay candidate-only, canonical metadata is preserved, and responses reflect persisted metadata. | `src/mcp/tools/tool_ontology.rs::tool_ontology_create_concept`, `src/db/queries/ontology.rs::create_concept` |
| `tla/OntologyTreeScope.tla`                        | Ontology tree boundary: invalid roots/facets fail closed, recursive subtree depth is clamped, corrupt cycles and non-concept/inactive endpoints are suppressed, and hierarchy edges are duplicate-free. | `src/mcp/tools/tool_ontology.rs::tool_ontology_tree`, `src/db/queries/ontology.rs::{concept_descendants,concept_hierarchy_edges}` |
| `tla/FindCoupledFilesScope.tla`                    | Co-change request boundary: blank/unknown/duplicate projects fail closed, numeric params are bounded, candidate rows are scoped to the resolved project id, bulk commits are excluded, and the tool remains read-only/no-lock. | `src/mcp/tools/tool_find_coupled_files.rs`, `src/db/queries/topics.rs::{find_coupled_files_by_project_id}` |
| `tla/SendSyncViolationsScan.tla`                   | Send/Sync violation scan boundary: strict project resolution, bounded result limits, Rust/project scoped streaming regex scan, same-project unsafe symbol enrichment, and read-only/no-lock execution. | `src/mcp/tools/tool_send_sync_violations.rs`, `src/mcp/tools/sota_regex_scan.rs` |
| `tla/ConcurrencyAuditScopes.tla`                   | Concurrency-audit boundary: `lockset_races` and `blocking_in_async` reject invalid/ambiguous projects, clamp scan limits, stream file content, scope regex/effect rows to the resolved project, remain read-only, and retain no locks. | `src/mcp/tools/tool_{lockset_races,blocking_in_async}.rs`, `src/mcp/tools/sema_helpers/effects.rs`, `src/mcp/tools/sota_regex_scan.rs` |
| `tla/WorkItemSetStatusAtomicity.tla`               | Work-item status-transition boundary: invalid requests do not mutate state, MCP transitions are agent-only, judgment states are unreachable through this tool, status/history writes are atomic, and concurrent transitions recheck after row lock. | `src/mcp/tools/work_items/lifecycle.rs::tool_work_item_set_status`, `src/db/queries/work_items.rs::set_work_item_status` |
| `tla/WorkItemCreateAtomicity.tla`                  | Work-item creation boundary: invalid requests do not write, bug-only metadata cannot attach to non-bugs, bug sidecars commit atomically with item rows, parent rows are locked before child root derivation, and normalized fields are stored. | `src/mcp/tools/work_items/crud.rs::tool_work_item_create`, `src/db/queries/work_items.rs::insert_work_item_in_tx` |
| `tla/WorkItemAddCriterionBoundary.tla`             | Work-item criterion boundary: invalid criterion metadata rejects before insert, missing items write nothing, successful calls create exactly one criterion, and the tool cannot create evidence or verify work. | `src/mcp/tools/work_items/verify.rs::tool_work_item_add_criterion`, `src/db/queries/work_items.rs::insert_acceptance_criterion` |
| `tla/WorkItemRecordEvidenceBoundary.tla`           | Work-item manual-evidence boundary: invalid evidence metadata rejects before insert, missing criteria write nothing, successful MCP calls create exactly one manual evidence row, counters advance post-insert, and the tool cannot produce trusted evidence or verify work. | `src/mcp/tools/work_items/verify.rs::tool_work_item_record_evidence`, `src/db/queries/work_items.rs::record_verification_evidence` |
| `tla/WorkItemAttemptVerifyGate.tla`                | Work-item attempt-verify boundary: manual-only evidence, absent evidence, wrong status, and failed trusted evidence leave status unchanged, while only sufficient trusted passing evidence can publish verified and increment verification stats. | `src/mcp/tools/work_items/verify.rs::tool_work_item_attempt_verify`, `src/db/queries/work_items.rs::{latest_passing_evidence_id,set_work_item_status}` |
| `tla/WorkItemDeferReinstateAtomicity.tla`          | Work-item defer/reinstate boundary: user token rejects before transaction, local rejects write nothing, negotiation rows and status history commit atomically, transition failures roll back negotiations, and status-change stats advance only post-commit. | `src/mcp/tools/work_items/verify.rs::{tool_work_item_defer,tool_work_item_reinstate}`, `src/db/queries/work_items.rs::{record_scope_negotiation_in_tx,set_work_item_status_in_tx}` |
| `tla/WorkItemLinkExperimentAtomicity.tla`          | Work-item experiment-link boundary: invalid bridge inputs do not write, auto-create/bridge/criterion writes commit atomically, create stats increment post-commit, existing-item links hold an update lock before criterion seeding, and same-item seed races cannot duplicate criteria or deadlock. | `src/mcp/tools/work_items/experiment_link.rs::tool_work_item_link_experiment`, `src/db/queries/work_items.rs::{link_work_item_experiment_in_tx,experiment_verdict_criterion_id_in_tx}` |
| `tla/WorkItemCrudReadUpdate.tla`                   | Work-item read/update boundary: public ids are normalized, list filters fail closed, limits and update numeric fields are bounded, bug metadata is bug-only, and mutable row updates commit atomically with bug sidecar updates. | `src/mcp/tools/work_items/crud.rs::{tool_work_item_get,tool_work_item_list,tool_work_item_update}`, `src/db/queries/work_items.rs::{list_work_items,update_work_item_fields_in_tx}` |
| `tla/CentralityAnalysisScope.tla`                   | Centrality request boundary: blank/duplicate/missing projects and invalid metrics fail closed, metric rows require matching metric/file project ids, limits are clamped, and effect/cross-project enrichment uses the resolved project id. | `src/mcp/tools/tool_centrality_analysis.rs`, `src/mcp/server/handlers/graph_core.rs` |
| `tla/IndexStatsEnvelope.tla`                        | Index-stats response envelope: the live snapshot is always present, DB count success is exact, DB count failure only disables the index block, counts stay nonnegative, and effect enrichment is optional. | `src/mcp/tools/tool_index_stats.rs` |
| `tla/McpToolTelemetryFilters.tla`                   | MCP telemetry request boundary: optional filters and aggregation names are normalized, lookback/raw limits are clamped, invalid aggregations fail closed, and returned rows satisfy the normalized filters. | `src/mcp/tools/tool_mcp_tool_telemetry.rs`, `src/mcp/server/handlers/inventory.rs` |
| `tla/UnsafeClustersScope.tla`                        | Unsafe-clusters request boundary: duplicate/blank projects fail closed, regex rows are scoped Rust files, file rows are bounded/ranked, unsafe effect symbols are project-scoped, and typed collector rows align metric/file/symbol identity. | `src/mcp/tools/tool_unsafe_clusters.rs`, `src/quality/collectors/concurrency.rs`, `src/mcp/tools/sota_helpers.rs` |
| `tla/PanicPathsScope.tla`                            | Panic-path request boundary: invalid projects/filters fail closed, limits are bounded, metric/symbol/file joins agree on the resolved project, visibility filters hold, and `may_panic` effects are project-scoped. | `src/mcp/tools/tool_panic_paths.rs`, `src/mcp/tools/sema_helpers/effects.rs` |
| `tla/OntologyInvariantsFileScope.tla`                | Ontology invariant file-resolution boundary: blank/ambiguous files fail closed, wildcards are literal, missing files return empty, and surfaced invariants belong to the unique resolved file id. | `src/mcp/tools/tool_ontology.rs::tool_ontology_invariants_for_file`, `src/db/queries/ontology.rs::invariants_for_file` |
| `tla/AdoptionReportTelemetry.tla`                    | Adoption-report telemetry boundary: output formats are normalized, lookback windows are clamped, excluded clients and old rows do not contribute, family/session counts are sound, and RLM remains a subset of A2A. | `src/mcp/tools/tool_adoption_report.rs`, `src/adoption.rs` |
| `tla/PredictionReportingScope.tla`                    | Prediction/reporting boundary: `code_on_fire` and `documented_tech_debt` normalize filters, clamp limits, reject invalid requests, scope rows to the resolved project id, and reject stale cross-project metric rows. | `src/mcp/tools/tool_code_on_fire.rs`, `src/mcp/tools/tool_documented_tech_debt.rs`, `src/mcp/server/handlers/prediction.rs` |
| `tla/TriggerCronSerialization.tla`                    | Trigger-cron dispatch boundary: invalid jobs reject before locking, valid jobs acquire the shared heavy-cron lock non-blocking, busy responses do not queue or run bodies, and completed calls release the lock. | `src/mcp/tools/tool_trigger_cron.rs`, `src/context.rs`, `src/cron/scheduler.rs` |
| `tla/CodeSummarizeScope.tla`                          | Code-summarize request boundary: project/scope/detail/path inputs are normalized and validated, file-derived channels share one scoped file set, metric rows agree with file project ids, and topic/effect enrichment is project-scoped. | `src/mcp/tools/tool_code_summarize.rs`, `src/mcp/server/handlers/core_advanced.rs` |
| `tla/WorkItemSearchIngestBulk.tla`                     | Work-item search/ingest/bulk boundary: explicit projects fail closed, search query embeddings are 1024d, plan ingestion is bounded and transactional, and bulk preflights targets/priority while preserving agent-only status transitions. | `src/mcp/tools/work_items/{analysis,ingestion,bulk}.rs`, `src/db/queries/work_items.rs` |
| `tla/ExperimentOpenGetAtomicity.tla`                 | Experiment open/get boundary: invalid open requests do not write, experiment and first hypothesis commit atomically, get lookup keys are validated and normalized, id beats slug when both are supplied, and successful get results are not partial opens. | `src/mcp/tools/tool_experiments.rs::tool_experiment_open`, `src/mcp/tools/tool_experiments.rs::tool_experiment_get`, `src/db/queries/experiments.rs` |
| `tla/ExperimentDecideAtomicity.tla`                 | Experiment decide boundary: invalid requests do not write, DB failures roll back, and result/verdict/status publication is atomic. | `src/mcp/tools/tool_experiments.rs::tool_experiment_decide`, `src/db/queries/experiments.rs::insert_experiment_decision` |
| `tla/FuzzyGrepAdapterBounds.tla`                    | Fuzzy-grep adapter boundary: invalid requests do not scan, distances clamp without wrapping, reported matches are bounded, and the tool remains read-only. | `src/mcp/tools/tool_fuzzy_grep.rs` |
| `tla/FindDuplicatesBounds.tla`                    | Find-duplicates boundary: non-finite thresholds reject, numeric filters clamp, fetch/output windows stay bounded, and cross-language rows satisfy threshold/language/same-repo/project-consistency filters. | `src/mcp/tools/tool_find_duplicates.rs` |
| `tla/RefactoringReportBounds.tla`                 | Refactoring-report boundary: non-finite thresholds reject, numeric/language filters clamp, duplicate-pair fetch windows stay bounded, and candidate output respects the effective limit. | `src/mcp/tools/tool_refactoring_report.rs` |
| `tla/IoHotpathScope.tla`                          | I/O hotpath boundary: invalid projects do not scan, limits and scan/effect outputs are bounded, stale metrics are rejected, and reported rows stay in the resolved project. | `src/mcp/tools/tool_io_hotpath.rs` |
| `tla/ApiStabilityScope.tla`                       | API-stability boundary: invalid projects fail closed, commit windows and output limits are clamped, git commit chunk reads use `content`, reported symbols stay in-project, and effect enrichment reuses the resolved project id. | `src/mcp/tools/tool_api_stability.rs` |
| `tla/CommunityDetectionScope.tla`                 | Community-detection boundary: invalid projects and graph types fail closed, finite resolution is bounded, stale cross-project edges are excluded, output exposes numeric graph fields, and effect enrichment reuses the resolved project id. | `src/mcp/tools/tool_community_detection.rs` |
| `tla/DeadCodeReachabilityScope.tla`               | Dead-code reachability boundary: invalid projects fail closed, no-symbol state soft-fails, limits are bounded, tests and bare-name edges are opt-in, traversed call edges are same-project, and BFS/read-only execution is bounded. | `src/mcp/tools/tool_dead_code_reachability.rs` |
| `tla/ExperimentSearchScope.tla`                   | Experiment-search boundary: blank/invalid filters fail closed, limits are bounded, vector and FTS modes preserve project/kind/active-verdict filters, stale verdicts are ignored, and execution is read-only. | `src/mcp/tools/tool_experiments.rs::tool_experiment_search`, `src/db/queries/experiments.rs::{experiment_search_vector,experiment_search_fts}` |
| `tla/MemoryOpenNodesScope.tla`                    | Memory open-nodes boundary: invalid name lists fail closed, exact names are trimmed/deduped and capped, active entities/observations are returned, inactive relation endpoints are suppressed, and execution is read-only. | `src/mcp/tools/tool_memory_crud.rs::tool_memory_open_nodes`, `src/db/queries/memory_search.rs::memory_open_nodes` |
| `tla/ReindexSerializationScope.tla`                | Reindex operational boundary: invalid language tokens reject before locking, busy locks reject without writes, language mode is scoped, full mode deletes chunks before files, cancellation preserves file rows, and locks are released. | `src/mcp/tools/tool_reindex.rs`, `src/db/queries/files.rs::delete_files_by_language` |
| `tla/WorkItemClaimAtomicity.tla`                   | Work-item claim boundary: explicit blank agents reject, row-CAS ownership cases are no-write or atomic-write, leases clamp, blocked/terminal states do not mutate, and concurrent claim races have one winner. | `src/mcp/tools/work_items/collab.rs::tool_work_item_claim`, `src/db/queries/work_items.rs::claim_work_item` |
| `tla/WorkItemTreeScope.tla`                        | Work-item tree boundary: invalid roots reject, recursive rows are bounded, corrupted parent cycles are suppressed, rows are duplicate-free and depth/priority ordered, and execution is read-only. | `src/mcp/tools/work_items/crud.rs::tool_work_item_tree`, `src/db/queries/work_items.rs::get_work_item_subtree` |
| `tla/DeadlockCandidatesScope.tla`                  | Deadlock-candidates boundary: invalid projects reject, file scans/edges/cycles/effects are resolved-project scoped, and execution is read-only with no held locks. | `src/mcp/tools/tool_deadlock_candidates.rs` |
| `tla/DeadlockAnalysisBoundary.tla`                 | Deadlock-analysis wrapper boundary: `deadlock_cycles`, `lock_order_graph`, and `channel_deadlock` reject invalid/non-finite inputs, clamp analysis bounds, return resolved-project rows, remain read-only, and retain no locks. | `src/mcp/tools/tool_{deadlock_cycles,lock_order_graph,channel_deadlock}.rs`, `src/concurrency/mod.rs`, `src/graph/{lock_order,petri}.rs` |
| `tla/ChangeImpactScope.tla`                        | Change-impact boundary: invalid project/file inputs reject, depth is clamped, import/co-change/semantic/resolved-caller/effect channels are project-scoped, and only dependent-project reporting crosses projects. | `src/mcp/tools/tool_change_impact_analysis.rs` |
| `tla/DocCodeDriftScope.tla`                        | Doc-code-drift boundary: invalid projects reject, trimmed valid projects are accepted, thresholds/limits are bounded, SQL result rows are bounded, drift/effect channels are project-scoped, and execution is read-only. | `src/mcp/tools/tool_doc_code_drift.rs` |
| `tla/ExperimentListScope.tla`                      | Experiment-list boundary: invalid filters reject, blank filters are omitted, valid filters trim, pagination is bounded, project scoping and newest-first order hold, and execution is read-only. | `src/mcp/tools/tool_experiments.rs::tool_experiment_list`, `src/db/queries/experiments.rs::list_experiments` |
| `tla/ExperimentRenderLedgerScope.tla`              | Experiment-ledger boundary: invalid lookups/paths/slugs reject, dry-run does not write, write mode publishes one contained file atomically, and execution does not mutate the DB or hold locks. | `src/mcp/tools/tool_experiments.rs::tool_experiment_render_ledger`, `src/experiment/ledger.rs::render_and_write` |
| `tla/QualityReportBoundary.tla`                    | Quality-report boundary: invalid local inputs reject before work, invalid projects reject before cron/aggregate/history, refresh/trend controls are bounded, and aggregate/history share one resolved project id. | `src/mcp/tools/tool_quality_report.rs`, `src/quality/aggregate.rs`, `src/quality/history.rs` |
| `tla/RenameOracleBounds.tla`                       | Rename-oracle boundary: invalid names and oversized candidate sets reject before DAWG construction, empty sets avoid dictionary work, deduped counts are reported, and best-candidate selection is streaming. | `src/mcp/tools/tool_rename_oracle.rs`, `liblevenshtein-rust/`, `libdictenstein/` |
| `tla/SecretDetectionScan.tla`                      | Secret-detection boundary: finite/clamped numeric params, strict project resolution, one-file streaming scan, bounded findings, stream drop before enrichment, and read-only execution. | `src/mcp/tools/tool_secret_detection.rs` |
| `tla/SecurityAuditBoundary.tla`                    | Security-audit boundary: `taint_analysis`, `injection_candidates`, `crypto_misuse`, and `unsafe_deserialization` reject invalid projects/kinds, clamp limits, apply predicates before caps, stream file content, bound effect enrichment, remain read-only, and retain no locks. | `src/mcp/tools/tool_{taint_analysis,injection_candidates,crypto_misuse,unsafe_deserialization}.rs`, `src/mcp/tools/sema_helpers/effects.rs` |
| `tla/SemverBreakAuditBounds.tla`                   | SemVer break-audit boundary: project validation, public-symbol cap, bounded current-schema commit scans, bounded output, streaming rename selection, and read-only execution. | `src/mcp/tools/tool_semver_break_audit.rs`, `src/db/migrations.rs::git_commit_chunks` |
| `tla/SubstringSearchBounds.tla`                    | Substring-search adapter boundary: invalid caller data rejects before suffix-automaton construction, accepted haystacks are bounded/deduped, empty haystacks avoid index work, and exact membership is preserved. | `src/mcp/tools/tool_substring_search.rs`, `src/fuzzy/suffix_automaton.rs` |
| `tla/CoordinationScope.tla`                        | Coordination tool request + trust boundary: blank/duplicate dependency or dependent project names fail closed (no coordination opened), `coordination_respond` accepts only the closed `{accept,decline,moved}` vocabulary and never records the System-only `resolved` status (xref `WorktreeNegotiation.GatekeeperSafety`), and `suggest_worktree` loads its project row by the single resolved id. | `src/mcp/tools/tool_{coordinate_dependency_block,coordination_respond,suggest_worktree}.rs`, `src/mcp/tools/sota_helpers.rs::project_id_or_err`, `src/deps/coordination.rs::CoordinationStatus` |
| `tla/ProjectDepsScope.tla`                         | Project dependency-edge read boundary: blank/duplicate projects fail closed, and `project_dependents` / `project_dependencies` return exactly the live (`valid_to IS NULL`) reverse / forward edges incident on the one resolved project id (no superseded edge or crossed direction). | `src/mcp/tools/tool_{project_dependents,project_dependencies}.rs`, `src/deps/store.rs::{dependents_of,dependencies_of}` |
| `tla/ClientTrackingScope.tla`                      | Client-tracking read boundary: the optional project filter is trimmed and blank means unfiltered (never an error), the filter only removes rows, and `client_project_matrix` clamps `since_minutes` to `1..=44_640` and `top_files_per_project` to `0..=50`. | `src/mcp/tools/tool_{active_clients,client_project_matrix}.rs`, `src/db/queries` (`active_clients`, `client_project_matrix`) |
| `tla/A2aMailboxBoundary.tla`                       | A2A mailbox boundary: `a2a_send_message` requires a non-blank trimmed body, defaults/validates `kind` against the closed `MessageKind` vocabulary, resolves `to_project` fail-closed, and requires at least one recipient; `a2a_inbox` resolves its project filter fail-closed, keys read receipts to the resolved recipient, and delivers at most once per recipient per channel. | `src/mcp/tools/tool_{a2a_send_message,a2a_inbox}.rs`, `src/a2a/mailbox.rs::MessageKind`, `src/a2a/mailbox_store.rs::{send,inbox,record_receipt}` |
| `tla/WorktreeNegotiation.tla`                      | Phase-4 worktree-coordination protocol gatekeeper: the dependent D is unblocked only after pgmcp's System git scanner observes the dependency U stable (`GatekeeperSafety`), never on the Editor agent's `moved` claim alone (`NoUnblockOnClaimAlone`); liveness `EventuallyUnblocked`. TLC + TLAPS (`WorktreeNegotiation_proofs.tla`) + Rocq (`rocq/WorktreeNegotiation.v`). | `src/mcp/tools/tool_coordination_respond.rs`, `src/deps/coordination.rs`, `src/deps/coord_store.rs`, the git-scanner gatekeeper |

## Rocq proofs

| Proof                                         | Theorem                                                                                  | Code locus                                                  |
|-----------------------------------------------|------------------------------------------------------------------------------------------|-------------------------------------------------------------|
| `rocq/TransducerMandateDedup.v`               | Idempotence + termination of the Phase 3 in-process mandate-dedup pipeline (`sessions::mark_near_duplicate_superseded`'s Transducer query → bulk UPDATE). | `src/sessions.rs::mark_near_duplicate_superseded`           |
| `rocq/CsmMpst.v`                              | ADR-009 CSM/MPST metatheory (one self-contained file — `verify.sh` runs `coqc` per file). T1 RLM termination; T2 deliberation termination; T3 global progress; T4 subject reduction; T5 projection soundness (send/recv/bystander **and** choice sender/receiver/bystander); T6 operational correspondence (bidirectional gstep↔lstep) — all **∀ finite well-formed `G` including `GChoice`** (the plain external-choice merge is the `bystanders_agree` side-condition); `protocol_fidelity` is the choice-free corollary. No `Admitted`/`Axiom`/`Hypothesis`. | `src/csm/mpst/{global,local,project}.rs`, `src/a2a/rlm.rs` |
| `rocq/CsmMedium.v`                            | ADR-009 Phase R1 medium discipline: `medium_discipline` (a well-media-formed protocol never places a black-box role on a latent edge) + the contrapositive corollary. No `Admitted`/`Axiom`. | `src/csm/media.rs::check_media_discipline` |
| `rocq/WorktreeNegotiation.v`                  | Phase-4 worktree-coordination gatekeeper trust boundary: the dependent D is unblocked only after the System git scanner observed the dependency U stable — never on the Editor agent's `moved` claim alone (`gatekeeper_safety`); mirrors the v17 CI-evidence gatekeeper and the tracker's `system_absent_from_judgment_columns`. No `Admitted`/`Axiom`; closes with `Qed`. | `src/mcp/tools/tool_coordination_respond.rs`, `src/deps/coordination.rs`, the git-scanner gatekeeper |

## ADR-009 formal artefacts: complete

Both layers are done — the TLA⁺ specs (Phase 4, all TLC-checked) and the Rocq
metatheory `CsmMpst.v` (Phase 5, T1–T6 to `Qed`). See the Status table.

**On TLAPS:** the TLA⁺ `THEOREM Spec ⇒ []Invariants` statements are present in
each spec but discharged by **TLC** (finite, mechanical) rather than `tlapm` —
TLAPS is not packaged for this host, and the *unbounded* (∀-size) guarantees are
proven independently in the Rocq metatheory (a stronger, general result). The
`THEOREM`s remain tlapm-ready if that prover is installed.

**Scope (honest):** T1–T6 hold for all finite well-formed global types, **including
the `GChoice` (Deliberation) case** — projection is general (sender → `LSelect`,
receiver → `LBranch`, bystander → the merged branch), and the external-choice plain
merge is encoded as the `bystanders_agree` Prop side-condition (every branch
projects identically for a bystander), so `project` stays a total `Fixpoint` with no
decidable-equality/merge machinery. T6 is bidirectional gstep↔lstep correspondence
stated per head construct (`GMsg`/`GChoice` are the only steppable constructors), so
"no behaviour lost or invented" is mechanical, not just TLC-/test-checked. The merge
discipline follows the established MPST result (Honda-Yoshida-Carbone POPL'08;
Scalas-Yoshida POPL'19). The one residual modeling boundary: global types are finite
unrolled trees (bounded runs), matching what pgmcp actually validates — recursion is
the separate well-founded T1/T2 argument, not coinduction.

## Status

| Property                                                                | Defined    | Mechanically checked | Verification log                                                  |
|-------------------------------------------------------------------------|------------|----------------------|-------------------------------------------------------------------|
| CronStateMachine — DisjointSets                                         | 2026-05-23 | 2026-05-23           | `states/cron_state_machine_tlc_2026-05-23.log` (TLC, exit 0)      |
| CronStateMachine — NoDoubleProcessing                                   | 2026-05-23 | 2026-05-23           | `states/cron_state_machine_tlc_2026-05-23.log` (TLC, exit 0)      |
| CronStateMachine — AtMostOneRunning                                     | 2026-05-23 | 2026-05-23           | `states/cron_state_machine_tlc_2026-05-23.log` (TLC, exit 0)      |
| CronStateMachine — LockMatchesRunning                                   | 2026-05-23 | 2026-05-23           | `states/cron_state_machine_tlc_2026-05-23.log` (TLC, exit 0)      |
| CronStateMachine — RecoverInProgressAsFailed (refinement, modeled in `Next`) | 2026-05-23 | 2026-05-23           | `states/cron_state_machine_tlc_2026-05-23.log` (TLC, exit 0)      |
| SimilarityScanFkDrift — NoOrphanFkInsert (under ON DELETE CASCADE)      | 2026-05-23 | 2026-05-23           | `states/similarity_scan_fk_drift_tlc_2026-05-23.log` (TLC, exit 0)|
| SimilarityScanFkDrift — CacheNeverExceedsKnownIds                       | 2026-05-23 | 2026-05-23           | `states/similarity_scan_fk_drift_tlc_2026-05-23.log` (TLC, exit 0)|
| TransducerMandateDedup — Idempotence (running twice yields same rows)   | 2026-05-23 | 2026-05-23           | `states/rocq_compile_2026-05-23.log` (`coqc`, exit 0)             |
| TransducerMandateDedup — Termination (`dedup_run` is structurally recursive over a finite list) | 2026-05-23 | 2026-05-23 | `states/rocq_compile_2026-05-23.log` (`coqc`, exit 0)             |
| CfsmNetwork (Deliberation) — Invariants + DeadlockFreedom + EventualTermination | 2026-05-27 | 2026-05-27 | `states/cfsm_network_tlc_2026-05-27.log` (TLC, no error; 19 states) |
| A2aLinearPipeline — Sequential (NStages=3) — Invariants + DeadlockFreedom + EventualTermination | 2026-05-27 | 2026-05-27 | `states/a2asequential_tlc_2026-05-27.log` (TLC, no error)         |
| A2aLinearPipeline — Mixture (NStages=4)                                 | 2026-05-27 | 2026-05-27 | `states/a2amixture_tlc_2026-05-27.log` (TLC, no error)            |
| A2aLinearPipeline — Distillation (NStages=2)                            | 2026-05-27 | 2026-05-27 | `states/a2adistillation_tlc_2026-05-27.log` (TLC, no error)       |
| A2aLinearPipeline — Recursive (NStages=2)                               | 2026-05-27 | 2026-05-27 | `states/a2arecursiverlm_tlc_2026-05-27.log` (TLC, no error)       |
| RmasRecursionLoop — LatentNeverDecodedMidLoop + EventualTermination     | 2026-05-27 | 2026-05-27 | `states/rmas_recursion_loop_tlc_2026-05-27.log` (TLC, no error)   |
| CsmMpst — T1 `rlm_terminates` (RLM self-recursion well-founded)         | 2026-05-27 | 2026-05-27 | `coqc CsmMpst.v` exit 0 (no `Admitted`/`Axiom`)                   |
| CsmMpst — T2 `deliberation_terminates` (bounded loop well-founded)      | 2026-05-27 | 2026-05-27 | `coqc CsmMpst.v` exit 0                                            |
| CsmMpst — T3 `global_progress` (non-End wf global type steps)           | 2026-05-27 | 2026-05-27 | `coqc CsmMpst.v` exit 0                                            |
| CsmMpst — T4 `wf_preserved` (subject reduction)                         | 2026-05-27 | 2026-05-27 | `coqc CsmMpst.v` exit 0                                            |
| CsmMpst — T5 projection soundness: GMsg `project_send/recv_sound`+`project_bystander` **and** GChoice `project_choice_sender/receiver/bystander` (∀ wf `G`) | 2026-05-27 | 2026-05-27 | `coqc CsmMpst.v` exit 0                                            |
| CsmMpst — T6 operational correspondence: `projection_sound` (soundness) + `gstep_iff_sender_lstep_{msg,choice}` (bidirectional) + `sound_choice_bystander` (merge) — incl. `GChoice`; `protocol_fidelity` choice-free corollary | 2026-05-27 | 2026-05-27 | `coqc CsmMpst.v` exit 0                                            |
| CsmMedium — `medium_discipline` (black-box roles are Text-only)         | 2026-05-27 | 2026-05-27 | `coqc CsmMedium.v` exit 0                                          |
| SearchToolScoping — search/read/commit scoping invariants               | 2026-06-04 | 2026-06-05 | `tlc SearchToolScoping.tla` exit 0; 63 distinct states, 4,032 generated |
| ProjectInventoryScoping — list_projects preserves identities; mandate_context/name-scoped duplicate names fail closed; exact path/cwd-boundary inventory invariants | 2026-06-05 | 2026-06-05 | `tlc ProjectInventoryScoping.tla` exit 0; 19 distinct states, 28 generated |
| AtomicFileReplacement — no visible partial file/chunk replacement        | 2026-06-05 | 2026-06-05 | `tlc AtomicFileReplacement.tla` exit 0; 14 distinct states, 23 generated |
| WorkItemProgressLog — progress rows are agent-write, clamped, append-only self-reports | 2026-06-05 | 2026-06-05 | `tlc WorkItemProgressLog.tla` exit 0; 516 distinct states, 580 generated |
| ExperimentArtifactCapture — normalized kind, parser dispatch, metrics, and content-hash invariants | 2026-06-05 | 2026-06-05 | `tlc ExperimentArtifactCapture.tla` exit 0; 1,292 distinct states, 1,549 generated |
| ComplexityHotspotsScoping — duplicate-name rejection, project-id row scoping, and bounded result limits | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh ComplexityHotspotsScoping.tla` exit 0; 25 distinct states, 50 generated |
| FuzzySearchBounds — bounded edit distance/result windows and project-local hit soundness | 2026-06-05 | 2026-06-05 | `tlc FuzzySearchBounds.tla` exit 0; 4,084 distinct states, 4,084 generated |
| PhoneticSymbolSearchBoundary — normalized query/project boundary, bounded phonetic search, and project-local output | 2026-06-06 | Pending Rust | `scripts/tlc-capped.sh PhoneticSymbolSearchBoundary.tla` exit 0; 14 distinct states, 22 generated. Rust execution blocked while sibling `libdictenstein` is being refactored. |
| CorrectQueryBoundary — normalized correction input, finite bounded LM/edit parameters, project-id keyed trie/model artifacts, and slug-collision isolation | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh CorrectQueryBoundary.tla` exit 0; 9 distinct states, 18 generated |
| HybridSearchBoundary — bounded signed limits, finite/nonnegative weights, bounded third-leg edit distance, normalized optional filters, leg degradation, no DB writes, and no retained locks | 2026-06-07 | Pending Rust | `scripts/tlc-capped.sh HybridSearchBoundary.tla` exit 0; 7 distinct states, 13 generated. Rust execution pending until sibling `libdictenstein` / `libgrammstein` compilation is restored. |
| MemorySearchNodesScope — literal wildcard search, scope-safe aggregation, and exact observation counts | 2026-06-05 | 2026-06-05 | `tlc MemorySearchNodesScope.tla` exit 0; 928 distinct states, 928 generated |
| CompareFilesResolution — duplicate project reference rejection and one-to-one resolved-file alignment | 2026-06-05 | 2026-06-05 | `tlc CompareFilesResolution.tla` exit 0; 1,030 distinct states, 1,030 generated |
| MemoryAddObservations — ambiguous entity-name rejection, dedupe, and provenance | 2026-06-05 | 2026-06-05 | `tlc MemoryAddObservations.tla` exit 0; 193 distinct states, 193 generated |
| MemoryCreateEntitiesAtomicity — normalized create payloads, deterministic lock ordering, active entity/observation dedupe, and invalid no-write behavior | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh MemoryCreateEntitiesAtomicity.tla` exit 0; 68 distinct states, 98 generated |
| MemoryCreateRelationsAtomicity — normalized relation payloads, deterministic lock ordering, active relation dedupe, and invalid/unresolved no-write behavior | 2026-06-06 | Pending Rust | `scripts/tlc-capped.sh MemoryCreateRelationsAtomicity.tla` exit 0; 208 distinct states, 611 generated. Rust execution blocked while sibling `libdictenstein` is being refactored. |
| MemoryHybridSearchScope — normalized hybrid requests, scope/tier soundness, duplicate-free fused rows, and no membership-driven RRF inflation | 2026-06-06 | Pending Rust | `scripts/tlc-capped.sh MemoryHybridSearchScope.tla` exit 0; 19 distinct states, 28 generated. Rust execution blocked while sibling `libdictenstein` is being refactored. |
| DependencyGraphScope — request validation, duplicate project rejection, exact focus-file behavior, stale-edge rejection, and bounded graph rendering | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh DependencyGraphScope.tla` exit 0; 27,399 distinct states, 27,399 generated |
| CircularDependenciesScope — bounded cycle search, non-unique project rejection, and project-scoped closed import cycles | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh CircularDependenciesScope.tla` exit 0; 16 distinct states, 32 generated |
| DesignMetricsScope — bounded design-metric rows, literal scope filters, non-unique project rejection, and project-id-consistent enrichment | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh DesignMetricsScope.tla` exit 0; 9 distinct states, 18 generated |
| TechnicalDebtAnalysisScope — bounded debt rows, non-unique project rejection, TODO-inclusion semantics, and project-id-consistent enrichment | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh TechnicalDebtAnalysisScope.tla` exit 0; 5 distinct states, 10 generated |
| BugPredictionScope — bounded prediction rows, non-unique project rejection, score-kind consistency, and project-scoped effect symbols | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh BugPredictionScope.tla` exit 0; 5 distinct states, 10 generated |
| ExperimentMeasurementRecord — measurement validation, atomic run/sample/status writes, rollback, and single-lock run upserts | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh ExperimentMeasurementRecord.tla` exit 0; 35 distinct states, 50 generated |
| TestCoverageGapsScope — blank/duplicate project rejection, project-id-scoped coverage/effect rows, and threshold classification | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh TestCoverageGapsScope.tla` exit 0; 13 distinct states, 19 generated |
| DocCoverageGapsScope — blank/duplicate project rejection, project-id-scoped doc-topic/effect rows, and threshold classification | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh DocCoverageGapsScope.tla` exit 0; 6 distinct states, 12 generated |
| EngineeringScorecardScope — normalized input, project-id-scoped SCC/god-file metrics, and id-based quality-history snapshots | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh EngineeringScorecardScope.tla` exit 0; 6 distinct states, 12 generated |
| FindMisplacedCodeScope — bounded mismatch thresholds, duplicate project rejection, project-id scoped topic rows, and same-project effects | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh FindMisplacedCodeScope.tla` exit 0; 5 distinct states, 10 generated |
| PublicApiSurfaceScope — strict format validation, bounded full-row limit, uncapped summary counts, and project-id scoped symbol rows | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh PublicApiSurfaceScope.tla` exit 0; 5 distinct states, 10 generated |
| ArchitectureQualityScope — detail validation, project-id-consistent metrics, stale-edge rejection, and N/A mean semantics | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh ArchitectureQualityScope.tla` exit 0; 5 distinct states, 10 generated |
| ArchitectureViolationsScope — severity validation, project-id-scoped graph rows, stale-edge rejection, bounded reports, and same-project enrichment | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh ArchitectureViolationsScope.tla` exit 0; 4 distinct states, 8 generated |
| DesignSmellDetectionScope — smell validation, bounded limits, duplicate project rejection, scoped metric/co-change rows, and stale metric rejection | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh DesignSmellDetectionScope.tla` exit 0; 651 distinct states, 651 generated |
| CouplingCohesionScope — sort validation, bounded module depth/output, project-id-scoped graph rows, stale-edge rejection, and same-project enrichment | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh CouplingCohesionScope.tla` exit 0; 4 distinct states, 8 generated |
| FindOrphansScope — detail validation, bounded limits, project-id-scoped orphan rows, language filtering, and same-project enrichment | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh FindOrphansScope.tla` exit 0; 5 distinct states, 10 generated |
| HotPathAuditScope — finite threshold validation, bounded limits, project-id-consistent metric rows, stale metric rejection, and same-project enrichment | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh HotPathAuditScope.tla` exit 0; 4 distinct states, 8 generated |
| MemorySemanticSearchScope — blank/tier/vector guards, bounded limits, scope/tier filters, and unique observations | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh MemorySemanticSearchScope.tla` exit 0; 17 distinct states, 25 generated |
| MemoryRaptorSearchScope — blank/scope/level/vector guards, bounded k/ef_search, read-only execution, and per-level top-k retrieval | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh MemoryRaptorSearchScope.tla` exit 0; 10 distinct states, 20 generated |
| OntologyCreateConceptAtomicity — normalized create input, per-name advisory-lock serialization, candidate-only agent creates, and canonical metadata preservation | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh OntologyCreateConceptAtomicity.tla` exit 0; 41 distinct states, 47 generated |
| FindCoupledFilesScope — normalized request bounds, duplicate project rejection, project-id scoped co-change rows, and bulk-commit exclusion | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh FindCoupledFilesScope.tla` exit 0; 8 distinct states, 16 generated |
| SendSyncViolationsScan — strict project resolution, bounded streaming scan, Rust/project scoped matches, and same-project unsafe symbols | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh SendSyncViolationsScan.tla` exit 0; 6 distinct states, 12 generated |
| ConcurrencyAuditScopes — scoped lockset/blocking-async scans, bounded streaming content, project-scoped effects, read-only execution, and no retained locks | 2026-06-07 | Pending Rust | `scripts/tlc-capped.sh ConcurrencyAuditScopes.tla` exit 0; 11 distinct states, 16 generated. Focused `nextest` is blocked before pgmcp tests by sibling `libgrammstein` compile errors. |
| WorkItemSetStatusAtomicity — invalid request rejection, agent trust boundary, atomic status/history writes, and serialized concurrent transitions | 2026-06-05 | 2026-06-07 | `scripts/tlc-capped.sh WorkItemSetStatusAtomicity.tla` exit 0; 21 distinct states, 31 generated; rerun after transaction-helper refactor with same state counts |
| WorkItemCreateAtomicity — normalized creation input, bug-only metadata, strict project resolution, parent locking, and atomic item/sidecar writes | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh WorkItemCreateAtomicity.tla` exit 0; 14 distinct states, 28 generated |
| WorkItemAddCriterionBoundary — closed-vocabulary criterion validation, bounded text, no-write rejection, and no evidence/status mutation | 2026-06-07 | Pending Rust | `scripts/tlc-capped.sh WorkItemAddCriterionBoundary.tla` exit 0; 9 distinct states, 17 generated. Rust execution currently blocked before pgmcp by sibling dependency compile error. |
| WorkItemRecordEvidenceBoundary — bounded manual evidence, post-insert evidence stats, no trusted-source evidence, and no status mutation | 2026-06-07 | Pending Rust | `scripts/tlc-capped.sh WorkItemRecordEvidenceBoundary.tla` exit 0; 11 distinct states, 21 generated. Rust execution currently blocked before pgmcp by sibling dependency compile error. |
| WorkItemAttemptVerifyGate — manual evidence rejection, trusted-evidence requirement, status preservation on failure, and post-transition verification stats | 2026-06-07 | Pending Rust | `scripts/tlc-capped.sh WorkItemAttemptVerifyGate.tla` exit 0; 7 distinct states, 13 generated. Rust execution currently blocked before pgmcp by sibling dependency compile error. |
| WorkItemDeferReinstateAtomicity — token-before-transaction behavior, atomic negotiation/status writes, rollback on transition failure, and post-commit status stats | 2026-06-07 | Pending Rust | `scripts/tlc-capped.sh WorkItemDeferReinstateAtomicity.tla` exit 0; 7 distinct states, 13 generated. Rust execution currently blocked before pgmcp by sibling dependency compile error. |
| WorkItemLinkExperimentAtomicity — bounded bridge inputs, atomic item/bridge/criterion writes, post-commit create stats, update-lock criterion seeding, and no same-item seed race deadlock | 2026-06-07 | Pending Rust | `scripts/tlc-capped.sh WorkItemLinkExperimentAtomicity.tla` exit 0; 204 distinct states, 402 generated. Rust execution currently blocked before pgmcp by sibling dependency compile error. |
| WorkItemCrudReadUpdate — normalized read/update ids, fail-closed list filters, bounded updates, bug-only metadata, and atomic row/sidecar writes | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh WorkItemCrudReadUpdate.tla` exit 0; 14 distinct states, 28 generated |
| CentralityAnalysisScope — project/metric validation, bounded rows, project-scoped metric rows, and resolved-project enrichment | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh CentralityAnalysisScope.tla` exit 0; 10 distinct states, 20 generated |
| IndexStatsEnvelope — stable stats envelope, exact DB counts, localized DB-count failure, and optional effect enrichment | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh IndexStatsEnvelope.tla` exit 0; 4 distinct states, 8 generated |
| McpToolTelemetryFilters — normalized filters, aggregation validation, bounded lookback/raw limits, and project-filter soundness | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh McpToolTelemetryFilters.tla` exit 0; 8 distinct states, 16 generated |
| UnsafeClustersScope — unique project resolution, scoped Rust regex rows, bounded/ranked output, and typed collector identity alignment | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh UnsafeClustersScope.tla` exit 0; 5 distinct states, 10 generated |
| PanicPathsScope — closed entry filters, bounded limits, stale metric rejection, scoped effects, and read-only execution | 2026-06-06 | 2026-06-06 | `scripts/tlc-capped.sh PanicPathsScope.tla` exit 0; 5 distinct states, 10 generated |
| OntologyInvariantsFileScope — literal file matching, ambiguous-path rejection, missing-file empty responses, and resolved-file invariant scoping | 2026-06-06 | 2026-06-06 | `scripts/tlc-capped.sh OntologyInvariantsFileScope.tla` exit 0; 6 distinct states, 12 generated |
| OntologyTreeScope — bounded subtree reads, active concept filtering, corrupt-cycle suppression, duplicate-free hierarchy edges, and read-only execution | 2026-06-06 | Pending Rust | `scripts/tlc-capped.sh OntologyTreeScope.tla` exit 0; 15 distinct states, 22 generated. Rust execution blocked while sibling `libdictenstein` is being refactored. |
| AdoptionReportTelemetry — format/window normalization, client/window filtering, family/session counts, and RLM subset accounting | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh AdoptionReportTelemetry.tla` exit 0; 4 distinct states, 8 generated |
| PredictionReportingScope — normalized prediction/report filters, bounded limits, project-id row scoping, and stale metric rejection | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh PredictionReportingScope.tla` exit 0; 12 distinct states, 24 generated |
| TriggerCronSerialization — non-blocking shared heavy-cron lock, invalid dispatch rejection, busy/no-queue behavior, and lock release | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh TriggerCronSerialization.tla` exit 0; 10 distinct states, 20 generated |
| CodeSummarizeScope — validated summary filters, shared path scoping, metric project consistency, and project-scoped topic/effect enrichment | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh CodeSummarizeScope.tla` exit 0; 8 distinct states, 16 generated |
| WorkItemSearchIngestBulk — strict search/project guards, bounded atomic ingestion, and bulk target/priority preflight | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh WorkItemSearchIngestBulk.tla` exit 0; 17 distinct states, 34 generated |
| ExperimentOpenGetAtomicity — normalized open/get inputs, atomic experiment+hypothesis creation, lookup validation, and non-partial get results | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh ExperimentOpenGetAtomicity.tla` exit 0; 12 distinct states, 24 generated |
| ExperimentDecideAtomicity — invalid no-write behavior, rollback on DB failure, and atomic result/verdict/status publication | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh ExperimentDecideAtomicity.tla` exit 0; 227 distinct states, 308 generated |
| FuzzyGrepAdapterBounds — invalid no-scan behavior, non-wrapping distance clamps, bounded reported matches, and read-only execution | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh FuzzyGrepAdapterBounds.tla` exit 0; 1,957 distinct states, 1,957 generated |
| FindDuplicatesBounds — finite request bounds, bounded fetch/output windows, filtered cross-language rows, and read-only execution | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh FindDuplicatesBounds.tla` exit 0; 65 distinct states, 65 generated |
| RefactoringReportBounds — finite request bounds, bounded fetch/output windows, and read-only candidate reporting | 2026-06-06 | 2026-06-06 | `scripts/tlc-capped.sh RefactoringReportBounds.tla` exit 0; 4 distinct states, 8 generated |
| IoHotpathScope — unique project resolution, bounded scan/output/effects, stale metric rejection, and read-only execution | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh IoHotpathScope.tla` exit 0; 326 distinct states, 326 generated |
| MemoryUnifiedSearchBoundary — pre-embed validation, node-type filter normalization, bounded vector search settings, and read-only execution | 2026-06-05 | 2026-06-05 | `scripts/tlc-capped.sh MemoryUnifiedSearchBoundary.tla` exit 0; 9 distinct states, 18 generated |
| ApiStabilityScope — bounded commit windows, migrated commit-chunk reads, resolved-project scoping, and same-project enrichment | 2026-06-06 | 2026-06-06 | `scripts/tlc-capped.sh ApiStabilityScope.tla` exit 0; 5 distinct states, 10 generated |
| CommunityDetectionScope — project/graph-type validation, bounded resolution, stale-edge exclusion, numeric output envelope, and same-project enrichment | 2026-06-06 | 2026-06-06 | `scripts/tlc-capped.sh CommunityDetectionScope.tla` exit 0; 7 distinct states, 14 generated |
| DeadCodeReachabilityScope — bounded output, test-root/bare-name opt-in, same-project edge traversal, bounded BFS, and read-only execution | 2026-06-06 | 2026-06-06 | `scripts/tlc-capped.sh DeadCodeReachabilityScope.tla` exit 0; 6 distinct states, 12 generated |
| ExperimentSearchScope — closed filters, bounded output, vector/FTS fallback parity, active-verdict filtering, and read-only execution | 2026-06-06 | 2026-06-06 | `scripts/tlc-capped.sh ExperimentSearchScope.tla` exit 0; 6 distinct states, 12 generated |
| MemoryOpenNodesScope — bounded exact-name reads, active rows/endpoints, deterministic output, and read-only execution | 2026-06-06 | 2026-06-06 | `scripts/tlc-capped.sh MemoryOpenNodesScope.tla` exit 0; 4 distinct states, 8 generated |
| ReindexSerializationScope — language validation, non-blocking serialization, scoped deletion, cancellation ordering, and bounded batches | 2026-06-06 | 2026-06-06 | `scripts/tlc-capped.sh ReindexSerializationScope.tla` exit 0; 9 distinct states, 18 generated |
| WorkItemClaimAtomicity — agent validation, row-CAS ownership, lease bounds, atomic ledger/presence writes, and concurrent race safety | 2026-06-06 | 2026-06-06 | `scripts/tlc-capped.sh WorkItemClaimAtomicity.tla` exit 0; 14 distinct states, 28 generated |
| WorkItemTreeScope — bounded recursive tree reads, cycle suppression, deterministic ordering, read-only execution, and query stats | 2026-06-06 | 2026-06-06 | `scripts/tlc-capped.sh WorkItemTreeScope.tla` exit 0; 6 distinct states, 12 generated |
| DeadlockCandidatesScope — project-scoped file scans, lock-order edges/cycles, effect enrichment, and read-only execution | 2026-06-06 | 2026-06-06 | `scripts/tlc-capped.sh DeadlockCandidatesScope.tla` exit 0; 2 distinct states, 4 generated |
| DeadlockAnalysisBoundary — finite confidence handling, bounded deadlock/channel parameters, project-scoped rows, read-only execution, and no retained locks | 2026-06-07 | Pending Rust | `scripts/tlc-capped.sh DeadlockAnalysisBoundary.tla` exit 0; 13 distinct states, 19 generated. Focused `nextest` is blocked before pgmcp tests by sibling `libgrammstein` compile errors. |
| ChangeImpactScope — unique project resolution, depth bounds, same-project file-level impact channels, effect scoping, and read-only execution | 2026-06-06 | 2026-06-06 | `scripts/tlc-capped.sh ChangeImpactScope.tla` exit 0; 6 distinct states, 12 generated |
| DocCodeDriftScope — normalized project/threshold/limit input, SQL-bounded rows, project-scoped drift/effect channels, and read-only execution | 2026-06-06 | 2026-06-06 | `scripts/tlc-capped.sh DocCodeDriftScope.tla` exit 0; 6 distinct states, 12 generated |
| ExperimentListScope — enum filter validation, bounded pagination, project scoping, newest-first order, and read-only execution | 2026-06-06 | 2026-06-06 | `scripts/tlc-capped.sh ExperimentListScope.tla` exit 0; 7 distinct states, 14 generated |
| ExperimentRenderLedgerScope — lookup validation, path containment, safe filenames, dry-run/write separation, atomic publish, and no DB mutation | 2026-06-06 | Pending Rust | `scripts/tlc-capped.sh ExperimentRenderLedgerScope.tla` exit 0; 10 distinct states, 20 generated. Rust execution blocked by sibling `libdictenstein` compile error. |
| QualityReportBoundary — local validation before work, single project identity, bounded refresh/trend controls, and canonical envelope format | 2026-06-06 | Pending Rust | `scripts/tlc-capped.sh QualityReportBoundary.tla` exit 0; 10 distinct states, 19 generated. Rust execution pending until sibling `libdictenstein` refactor is ready. |
| RenameOracleBounds — bounded normalized symbol inputs, no-build invalid/empty cases, deduped counts, and streaming best selection | 2026-06-07 | Pending Rust | `scripts/tlc-capped.sh RenameOracleBounds.tla` exit 0; 10 distinct states, 19 generated. Rust execution pending until sibling `libdictenstein` refactor is ready. |
| SecretDetectionScan — finite/clamped thresholds, bounded findings, streaming scan memory, stream-drop enrichment ordering, and read-only execution | 2026-06-07 | Pending Rust | `scripts/tlc-capped.sh SecretDetectionScan.tla` exit 0; 8 distinct states, 15 generated. Rust execution pending until sibling `libdictenstein` refactor is ready. |
| SecurityAuditBoundary — strict project/kind validation, predicate-before-cap filtering, bounded streaming scans, bounded effect enrichment, read-only execution, and no retained locks | 2026-06-07 | Pending Rust | `scripts/tlc-capped.sh SecurityAuditBoundary.tla` exit 0; 10 distinct states, 19 generated. Rust execution pending until sibling `libdictenstein` / `libgrammstein` compilation is restored. |
| SemverBreakAuditBounds — bounded public/commit scans, current commit-chunk schema, bounded output, streaming rename selection, and read-only execution | 2026-06-07 | Pending Rust | `scripts/tlc-capped.sh SemverBreakAuditBounds.tla` exit 0; 8 distinct states, 15 generated. Rust execution blocked by sibling `libgrammstein` compile error. |
| SubstringSearchBounds — bounded caller haystacks, deterministic dedupe, empty-haystack no-build behavior, exact membership preservation, and read-only execution | 2026-06-07 | Pending Rust | `scripts/tlc-capped.sh SubstringSearchBounds.tla` exit 0; 10 distinct states, 19 generated. Rust execution blocked by sibling `libgrammstein` compile error. |
| CoordinationScope — blank/duplicate dependency/dependent rejection, closed `coordination_respond` vocabulary, agent-never-`resolved` trust boundary, and by-id `suggest_worktree` row load | 2026-06-10 | Pending Rust | `scripts/tlc-capped.sh CoordinationScope.tla` exit 0; 14 distinct states, 28 generated. Rust regressions in `tool_coordination` / forthcoming `oracle_coordination` pending until sibling dependency compilation is restored. |
| ProjectDepsScope — blank/duplicate project rejection and live (`valid_to IS NULL`) reverse/forward dependency-edge scoping to the one resolved project id | 2026-06-10 | Pending Rust | `scripts/tlc-capped.sh ProjectDepsScope.tla` exit 0; 9 distinct states, 18 generated. Rust regressions in `cross_project_graph_fields` pending until sibling dependency compilation is restored. |
| ClientTrackingScope — optional trimmed project filter (blank => unfiltered, not an error), filter-only-removes-rows, and clamped `since`/`top_files` windows | 2026-06-10 | Pending Rust | `scripts/tlc-capped.sh ClientTrackingScope.tla` exit 0; 9 distinct states, 18 generated. Rust regressions in `tool_active_clients` pending until sibling dependency compilation is restored. |
| A2aMailboxBoundary — non-blank trimmed body, closed `MessageKind` vocab (default `message`), fail-closed `to_project` / inbox filter, recipient-keyed receipts, and at-most-once delivery per recipient per channel | 2026-06-10 | Pending Rust | `scripts/tlc-capped.sh A2aMailboxBoundary.tla` exit 0; 12 distinct states, 24 generated. Rust regressions in `tool_a2a_mailbox` pending until sibling dependency compilation is restored. |
| WorktreeNegotiation — `GatekeeperSafety` + `NoUnblockOnClaimAlone` + `EventuallyUnblocked` (protocol gatekeeper trust boundary) | 2026-06-04 | 2026-06-04 | `tlc WorktreeNegotiation.tla` exit 0 (gatekeeper safety + liveness); TLAPS `WorktreeNegotiation_proofs.tla` (z3/zenon, `Safety` theorem); Rocq `rocq/WorktreeNegotiation.v` (`coqc` exit 0, `Qed`, no `Admitted`/`Axiom`). |

**P13.5 (2026-05-23) — Status integrity:** the previous version of
this README claimed "Verified 2026-05-23" without any mechanical
artefact under `states/`. P13.5 fixes both the proof (Rocq proof
rewritten with no `Hypothesis` / `Axiom` / `Admitted`, per
CLAUDE.md) and the README (status table now distinguishes "Defined"
from "Mechanically checked YYYY-MM-DD" + cites the log file).

## Inherited verification

pgmcp inherits five Coq/Rocq-verified theorems from `liblevenshtein-rust`
(used wherever Phase 3, Phase 5, Phase 7, Phase 10, P13.3, P13.4 reach
into the phonetic / Transducer framework):

| Theorem                  | What it guarantees                                                                                                    | Provenance                                                  |
|--------------------------|------------------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------|
| Rule well-formedness     | Every Zompist rewrite rule has `length(pattern) ≥ 1` and bounded replacement length.                                   | `liblevenshtein-rust/docs/verification/phonetic/zompist_rules.v:285` |
| Bounded expansion        | `apply_rule_at r s pos = Some s' ⇒ length(s') ≤ length(s) + max_expansion_factor` (= 20).                              | `zompist_rules.v:424`                                       |
| Non-confluence           | Some rules do not commute → caller chooses order; system never claims order-independence.                              | `zompist_rules.v:491` (constructive counterexample)         |
| Termination              | `apply_rules_seq` with a well-formed rule set halts within bounded fuel.                                              | `zompist_rules.v:569`                                       |
| Idempotence              | The fixed point of `apply_rules_seq` is stable: re-applying the rule set to the normalized form yields the same form. | `zompist_rules.v:615`                                       |

These are surfaced to pgmcp users via the `phonetic_normalize` and
`phonetic_symbol_search` MCP tools: their result docstrings cite the
inherited guarantees so operators know the framework's canonical
form is a stable, terminating, bounded-memory operation.

## Compiling proofs

Per `~/.claude/CLAUDE.md`, Rocq compilation uses `systemd-run` to keep
memory and CPU bounded:

```bash
systemd-run --user --scope \
    -p MemoryMax=96G -p CPUQuota=1800% -p IOWeight=30 -p TasksMax=200 \
    coqc docs/formal/rocq/TransducerMandateDedup.v
```

TLA+ model checking uses `scripts/tlc-capped.sh`, not raw `tlc`. The wrapper
defaults to one worker, `-Xmx512m`, `MaxMetaspaceSize=64m`,
`CompressedClassSpaceSize=16m`, `MemoryMax=1536M` when `systemd-run` is
available, a `/tmp` metadir, and cleanup on exit. Override with
`PGMCP_TLC_WORKERS`, `PGMCP_TLC_JAVA_XMX`, `PGMCP_TLC_METASPACE`,
`PGMCP_TLC_CLASS_SPACE`, and `PGMCP_TLC_MEMORY_MAX` only when the host has
enough RAM. Each `.tla` file has a sibling `.cfg` declaring the constants and
invariants:

```bash
cd docs/formal/tla
../../../scripts/tlc-capped.sh CronStateMachine.tla         \
    | tee ../states/cron_state_machine_tlc_$(date +%F).log
../../../scripts/tlc-capped.sh SimilarityScanFkDrift.tla    \
    | tee ../states/similarity_scan_fk_drift_tlc_$(date +%F).log
```

### `scripts/verify.sh` advisory gates

P13.5 wires `coqc` and `tlc` into `scripts/verify.sh` as advisory
gates. If the binaries are on `PATH` the gate runs and fails on
non-zero exit; if either is missing, the gate logs a clear "SKIP:
<tool> not found" line. Explicitly NOT silently skipped per
`feedback_feature_gated_build_verification.md`.
