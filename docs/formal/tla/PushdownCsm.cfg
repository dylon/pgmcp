\* TLC model config for PushdownCsm.tla (ADR-030 pushdown/hierarchical CSM).
\* Small bounds for a finite model; the unbounded ∀ statement is the Rocq theorem
\* CsmPushdown.v `runD_bounded`. The real runtime/conformance bound is large
\* (MAX_STACK_DEPTH = 4096) — ADR-030 §9 documents the model-vs-runtime split.
SPECIFICATION Spec
CONSTANTS
    MaxStackDepth = 2
    MaxSteps = 3
INVARIANTS
    Invariants
    DeadlockFreedom
PROPERTIES
    EventualTermination
\* Done (done = TRUE) is the legitimate terminal; suppress the default deadlock
\* report. DeadlockFreedom is asserted explicitly. As RmasRecursionLoop.cfg.
CHECK_DEADLOCK FALSE
