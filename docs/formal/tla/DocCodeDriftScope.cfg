\* TLC model config for DocCodeDriftScope.tla
\* Boundary model for doc_code_drift validation, bounded SQL results,
\* resolved-project scoping, effect enrichment, and read-only execution.
SPECIFICATION Spec
INVARIANTS
    TypeOK
    InvalidProjectRejects
    TrimmedProjectAccepted
    ThresholdBounded
    LimitBounded
    ReturnedRowsSqlBounded
    DriftRowsProjectScoped
    EffectBreakdownResolvedProjectScoped
    ReadOnlyNoLock
CHECK_DEADLOCK FALSE
