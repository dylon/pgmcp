\* TLC configuration for WorktreeNegotiation.tla.
SPECIFICATION Spec

INVARIANT TypeOK
INVARIANT GatekeeperSafety
INVARIANT NoUnblockOnClaimAlone

PROPERTY EventuallyUnblocked

\* The protocol legitimately terminates (Resolved / Declined are terminal states),
\* so TLC's default deadlock check must be off — those are proper termination, not
\* deadlocks. Matches the convention in the other tool-scope cfgs.
CHECK_DEADLOCK FALSE
