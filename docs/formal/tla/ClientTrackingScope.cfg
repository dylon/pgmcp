\* TLC model config for ClientTrackingScope.tla
\* Request + row-scope boundary for active_clients / client_project_matrix: the
\* OPTIONAL trimmed project filter (blank => unfiltered, not an error) and the
\* clamped since/top_files windows, over negative, zero, in-range, and overflow
\* values.
SPECIFICATION Spec
INVARIANTS
    TypeOK
    BlankFilterIsUnfiltered
    FilterOnlyRemovesRows
    SinceClamped
    TopFilesClamped
    NeverErrorsOnFilter
CHECK_DEADLOCK FALSE
