\* TLC model config for CoordinationScope.tla
\* Request + trust boundary for coordinate_dependency_block / coordination_respond /
\* suggest_worktree over blank, duplicate, unknown, padded-valid, and valid inputs,
\* including the System-only `resolved` status an agent may never set.
SPECIFICATION Spec
INVARIANTS
    TypeOK
    BlankOrDuplicateProjectRejects
    SuggestBlankOrDuplicateRejects
    RespondOnlyClosedVocab
    AgentNeverResolves
    RespondStatusFaithful
    RowLoadedByResolvedId
    RespondPathNeverUnblocks
    RejectedRequestsInert
CHECK_DEADLOCK FALSE
