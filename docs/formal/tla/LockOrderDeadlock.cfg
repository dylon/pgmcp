\* TLC config for LockOrderDeadlock.tla (ADR-011).
\* The model (Procs/Locks/AcqOrder/Rank) is concrete in the spec: 2 processes,
\* 2 locks, both acquiring in the same Rank-increasing order → the lock-order
\* graph is acyclic → []NoDeadlock holds. This is the gated, green soundness
\* check: ordered acquisition prevents circular wait (Havender 1968).
SPECIFICATION Spec
INVARIANTS
    TypeOK
    NoDeadlock
\* The restart-on-release model always has an enabled action, so there is no
\* genuine stuck state in the safe config; suppress TLC's built-in deadlock
\* report (we assert our own circular-wait predicate explicitly). Same rationale
\* as CfsmNetwork.cfg / CronStateMachine.cfg.
CHECK_DEADLOCK FALSE
