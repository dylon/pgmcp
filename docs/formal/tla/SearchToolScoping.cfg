\* TLC model config for SearchToolScoping.tla
\* Full state-space exploration over two projects, two indexed files, and two commits.
SPECIFICATION Spec
CONSTANTS
    Projects = {"project-a", "project-b"}
    NoProject = "no-project"
    NoPath = "no-path"
    NoTelemetryProject = "no-telemetry-project"
INVARIANTS
    Invariants
CHECK_DEADLOCK FALSE
