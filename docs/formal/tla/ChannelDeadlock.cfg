\* TLC config for ChannelDeadlock.tla (ADR-011).
\* The model is concrete in the spec: 2 processes, 2 channels (cap 1); process 1
\* sends first, so neither process is initially blocked → []NoDeadlock holds.
\* Gated, green check that producer-first communication is deadlock-free.
SPECIFICATION Spec
INVARIANTS
    TypeOK
    NoDeadlock
\* The all-done terminal state has no enabled Next; that is legitimate
\* termination, not a bug — we assert our own Deadlock (dead-marking) predicate
\* explicitly. Same rationale as CfsmNetwork.cfg.
CHECK_DEADLOCK FALSE
