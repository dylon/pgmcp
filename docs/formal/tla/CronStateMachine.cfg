\* TLC model config for CronStateMachine.tla
\* Bounded Tasks set; full state-space exploration.
SPECIFICATION Spec
CONSTANTS
    Tasks = {t1, t2, t3}
INVARIANTS
    Invariants
\* Terminal states (every task completed; pending = running = {}) are
\* reachable by design and are not bugs. We retain every safety
\* invariant above; only TLC's default "no enabled Next disjunct"
\* deadlock report is suppressed. Without this, set -e in verify.sh
\* halts the formal-gate for-loop before SimilarityScanFkDrift runs.
CHECK_DEADLOCK FALSE
