\* TLC model config for VpaConformance.tla (ADR-030 conformance acceptance twin).
\* Small bounds for a finite model; the unbounded statement is the Rocq theorem
\* CsmPushdown.v `conformance_sound` / `conformance_correct`.
SPECIFICATION Spec
CONSTANTS
    MaxLen = 4
    MaxDepth = 2
INVARIANTS
    Invariants
    DeadlockFreedom
PROPERTIES
    EventualTermination
\* `finished` is the legitimate terminal; suppress the default deadlock report.
CHECK_DEADLOCK FALSE
