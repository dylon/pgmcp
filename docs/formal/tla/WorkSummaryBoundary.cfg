\* TLC model config for WorkSummaryBoundary.tla
\* Models `work_summary` input validation/normalization/clamping, the bounded
\* per-repo live-git scan, the freshness-gated topic enrichment, and the
\* single canonical-format render — all with zero side effects on any reject.
SPECIFICATION Spec
INVARIANTS
    TypeOK
    LocalRejectsHaveNoSideEffects
    ResolveBeforeWork
    BoundedRepoScan
    ChurnReadsBounded
    EnrichmentGatedByFreshness
    OneRenderPerAccept
    CanonicalEnvelopeFormat
