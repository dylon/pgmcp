\* Default config so the verify.sh glob can model-check the generic library
\* module directly (NStages = 3, the Sequential shape). Per-pattern instances
\* (A2aSequential/Mixture/Distillation/RecursiveRlm) fix NStages via INSTANCE.
SPECIFICATION Spec
CONSTANTS
    NStages = 3
INVARIANTS
    Invariants
    DeadlockFreedom
PROPERTIES
    EventualTermination
CHECK_DEADLOCK FALSE
