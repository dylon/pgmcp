\* TLC model config for RmasRecursionLoop.tla (ADR-009 Track B latent-decode).
SPECIFICATION Spec
CONSTANTS
    NAgents = 3
    MaxRounds = 2
INVARIANTS
    Invariants
    DeadlockFreedom
PROPERTIES
    EventualTermination
\* Done (decoded = TRUE) is the legitimate terminal; suppress the default
\* deadlock report. DeadlockFreedom asserted explicitly. As CronStateMachine.cfg.
CHECK_DEADLOCK FALSE
