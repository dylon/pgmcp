\* TLC model config for SimilarityScanFkDrift.tla
\* Bounded ChunkIds set; full state-space exploration.
SPECIFICATION Spec
CONSTANTS
    ChunkIds = {c1, c2, c3}
INVARIANTS
    Invariants
\* Terminal states (file_chunks emptied + cron_cache drained) are
\* reachable by design and are not bugs. Same rationale as
\* CronStateMachine.cfg.
CHECK_DEADLOCK FALSE
