\* TLC model config for DeadCodeReachabilityScope.tla
\* Boundary model for dead_code_reachability request normalization, root policy,
\* same-project edge traversal, stale-edge rejection, bounded BFS, and read-only
\* execution.
SPECIFICATION Spec
INVARIANTS
    TypeOK
    InvalidProjectsReject
    NoSymbolsSoftFail
    LimitBounded
    TestRootsOptIn
    BareNameOptIn
    AcceptedEdgesSameProject
    AcceptedEdgesClosedKind
    StaleEdgesExcluded
    DeadCandidatesProjectScoped
    EffectEnrichmentUsesResolvedProject
    BoundedTraversal
    ReadOnlyNoHeldLock
CHECK_DEADLOCK FALSE
