\* TLC model config for ProjectDepsScope.tla
\* Request + row-scope boundary for project_dependents / project_dependencies over
\* blank, duplicate, unknown, padded-valid, and valid inputs, with one stale
\* (closed valid_to) edge that must never be returned.
SPECIFICATION Spec
INVARIANTS
    TypeOK
    BlankOrDuplicateProjectRejects
    OnlyLiveEdges
    DependentsEdgesScoped
    DependenciesEdgesScoped
    ResolvedProjectIdReal
CHECK_DEADLOCK FALSE
